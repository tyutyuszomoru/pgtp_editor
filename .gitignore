sample/
.superpowers/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
.pytest_cache/
.coverage

# PyInstaller + Inno Setup release build outputs (see optimized_build.py /
# docs/installer.iss). Regenerated per release, never tracked — the installer
# alone is ~28 MB.
build/
dist/
Output/
*.spec

# Harness-local worktrees/config, never part of the project itself —
# except agent definitions, which are project workflow and must survive clones
.claude/*
!.claude/agents/

# Separate standalone tool with its own git history (vendored into
# pgtp_editor/schema_learning/) - kept on disk for reference, not tracked
# here to avoid embedding its nested .git as a stray gitlink.
pgtp_analytics/
