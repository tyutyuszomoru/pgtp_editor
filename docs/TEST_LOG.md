# Test Log

Committed, append-only record of verified test runs. The `feature-tester` agent
(`.claude/agents/feature-tester.md`) appends one entry here every time a feature
is completed, after personally observing the run. Newest entries go at the **top**
of the table. Entries are never rewritten or deleted — red runs are recorded too.

Result format: `PASS — <passed> passed, <skipped> skipped` or
`FAIL — <failed> failed (<test names>)`.

> **RECONSTRUCTED BLOCK — read this before trusting the rows below.** Every row
> dated **2026-08-08 through 2026-08-10** and marked `(reconstructed)` in its Date
> column was **rebuilt on 2026-08-10 from commit messages**, not written at the
> time by a `feature-tester` that observed the run. Logging fell behind while ~16
> features and ~11 bugfixes shipped; this block closes that debt after the fact.
> The numbers in the "Full suite result" column are quoted **verbatim from the
> commit body that stated them** — trustworthy, because whoever wrote them ran
> the suite at that commit — but they were **not re-observed during the
> reconstruction**. Where a commit stated no figure, the cell reads
> `(not recorded)`; nothing in this block is interpolated, averaged or guessed.
> The "Test files added/extended" column is derived from `git show --stat`, so it
> lists files the commit *touched*, which is not the same as a feature-tester's
> judgement that the coverage is adequate. Rows dated 2026-08-07 and earlier, and
> the 2026-08-09 BUG-038/039/040 row, are contemporaneous and unaffected.

| Date | Feature | Test files added/extended | Targeted result | Full suite result | Commit |
|------|---------|---------------------------|-----------------|-------------------|--------|
| 2026-08-10 | **Register catch-up pass — the six units below were recorded, and TWO CLAIMS WERE VERIFIED RATHER THAN TRANSCRIBED.** The register's newest figure was FQ-033's `6 674 / 51` at `a9efb67` while six units had shipped since, which had become load-bearing rather than cosmetic: `README.md` promises it cites the *last recorded run*, so `spec-maintainer` correctly REFUSED to update the README's test count from a figure supplied in a prompt, because the register did not carry it. **The full suite was run twice in this dispatch and personally observed both times** — once at the clean tree (`a430707`, nothing modified) to check the parent session's `7291 passed, 51 skipped` claim, which **reproduced exactly**, and once after the coverage added here. **Claim 1 — `version.py`'s resolution order — CONFIRMED IN BOTH HALVES, by measurement.** `pyproject.toml` reads `0.4.0`; `importlib.metadata.version("pgtp-editor")` in this venv reads `0.3.0`; `_pyproject_version()` → `0.4.0`, `_metadata_version()` → `0.3.0`, `app_version()` → `0.4.0`. So the inversion of the queue entry's stated order (pyproject FIRST, metadata second) is justified by a real, currently-observable drift, and metadata-first would ship a wrong version in the one environment where it is checkable. **The frozen-PyInstaller path is recorded as UNVERIFIED, not as passing** — its agent declared the gap honestly and it stands: `optimized_build.py` passes `--copy-metadata pgtp-editor` behind `_distribution_metadata_available()` (guarded because the flag is a HARD build error when the distribution is absent), but there is **no build environment in this checkout**, so nothing here demonstrates that a frozen bundle actually resolves its version through `importlib.metadata`. What IS now verified is the probe's own behaviour and that the flag sits behind it. **Claim 2 — `pgtp_editor/vim/`'s purity — CONFIRMED, and it is genuinely more than an AST scan.** `tests/vim/test_package_purity.py` carries a static sweep (`test_vim_package_never_imports_ui_db_or_sql`, banning `pgtp_editor.ui`/`.db`/`.sql`) **and** a fresh-interpreter subprocess check (`test_the_package_does_not_import_sql_at_runtime_either`) that imports the package in a clean `sys.modules` and asserts no `pgtp_editor.sql*` entry appears, plus a sibling asserting no `PySide6`/`PyQt5`/`PyQt6`/`shiboken6` module loads. **Both legs were proved to bite by mutation** (both mutations reverted with `git checkout`; tree confirmed clean afterwards): (a) a lazy `from pgtp_editor.sql import blocks` placed *inside a function body* in `vim/grammar.py` turns `test_vim_package_never_imports_ui_db_or_sql` red — so the static leg is not fooled by nesting; (b) a `__import__('pgtp_editor.sql.blocks')` appended to `vim/__init__.py`, which the AST scan **cannot** see, turns `test_the_package_does_not_import_sql_at_runtime_either` red and the static tests stay green — so the runtime leg is load-bearing on its own, exactly as claimed. **The two named regression pins were checked and both do what was reported.** BUG-260810193333's `confirm=[]` stub really is the teeth: `_Seams.confirm` does `answer.pop(0)` on a list, so `test_precondition_1_never_silently_no_ops_on_a_changed_signature` asserts `pytest.raises(IndexError)` — an implementation that reverted to returning False after one Audit line **without asking** would raise nothing and fail the `raises` block, and the test additionally asserts `seams.confirms` is non-empty and `target_calls == []`. BUG-260810194657's confirmation step is pinned in **three** places plus a formatter golden, i.e. four: `tests/sql/test_blocks.py:150` (`begin\nwork := 1;\nend;` and the `transaction :=` twin), `tests/sql/test_block_spans.py:351` (asserting the outer span is exactly `BEGIN\nwork := 1;\nEND`), `tests/sql/test_formatter_properties.py:113` (in the property corpus), and the golden `test_a_block_assigning_to_a_transaction_phrase_word_is_still_a_block` at `tests/sql/test_formatter.py:294`, parametrized over `work`/`transaction`/`read`/`isolation` and asserting `end;` survives with the body indented. **Coverage assessed, not assumed, and one real gap was found and filled** — see the next column. Every one of the six units carries substantial tests already (counted from `git show --stat`), so no other gap was filled. **No implementation bug found.** One honest note on the quoted figures below: `df8d908` states `6800/51` while the *later* `cde65fa` states a baseline of `6789/51`; the two shipped 21 minutes apart from separate worktrees, so the figures are not on one line of ancestry and neither is wrong — they are recorded verbatim rather than reconciled. | Extended 1: `tests/test_version.py` (+14 → **26 collected**, from 12). **14 tests added**, all for the one gap the shipped suite genuinely left: **the resolution ORDER was not pinned at all.** `test_the_app_version_IS_the_pyproject_literal` passes under *either* order whenever the two sources agree, so it pinned the ordering decision only by accident, for as long as this venv's metadata stays stale — i.e. the load-bearing inversion had no guard. Added: `test_pyproject_WINS_over_metadata_when_the_two_disagree`; `test_the_stale_metadata_drift_is_real_in_this_checkout_and_loses` (asserts the measured drift instead of quoting it, and **skips** rather than fails once someone reinstalls and the two agree — the drift is a property of the checkout, not the code); `test_metadata_answers_only_when_pyproject_is_unreachable`, which counts calls so an implementation that resolved BOTH and preferred metadata could not pass. Then the pyproject reader's failure legs, none previously covered: a `version =` in a **later** table (`[tool.some-plugin]`) not read as ours (the `[project]`-slice is what prevents it), no `[project]` table at all, a **blank** literal (`""` and `"   "`) falling *through* to the next source rather than returning `""` and letting an empty About box out, and the path being the package's sibling and never a walk upward. Then the metadata reader's `except Exception` leg — a **broken** dist-info (`ValueError`), previously only `PackageNotFoundError` was tested — and that *import* of the module cannot raise with both sources broken, since `__version__` resolves at import time and a raise there is an app that does not start. Then the frozen build's guard, which the existing test only greps for: the flag is asserted to sit inside the `if _distribution_metadata_available():` branch with the `else:` leg saying `unknown` out loud, the probe is asserted to answer for this interpreter in agreement with `_metadata_version()`, to return False for **any** metadata failure (`ValueError` as well as `PackageNotFoundError`) since a probe that exists to keep a build working must never stop one, and `optimized_build.py` is asserted to spell no version literal either — the second-copy hazard is not confined to `version.py`. Reviewed as already-adequate and **not** extended: `tests/vim/test_package_purity.py`, `tests/vim/test_grammar.py` (33), `tests/vim/test_words.py` (14), `tests/ui/test_vim_mode.py` (107), `tests/sql/test_block_spans.py` (23), `tests/sql/test_blocks.py` (14), `tests/ui/test_structural_selection.py` (21), `tests/ui/test_new_project_sandbox_provisioning.py` (31), `tests/ui/test_ddl_object_editor.py`, `tests/ui/test_ddl_project_wiring.py`, `tests/sql/test_package_purity.py` (its `glob("*.py")` sweeps FQ-034's new `block_spans.py` by construction — confirmed, not assumed) | PASS — **26 passed in 0.27s** (`tests/test_version.py`), plus `tests/vim/test_package_purity.py` 6 passed, and the two mutation runs above showing 1 failed / 5 passed each with the intended test named | PASS — **7 305 passed, 51 skipped, 19 warnings in 94.73s** (`QT_QPA_PLATFORM=offscreen venv/bin/python -m pytest -q -n 4`, `-n 4` per the owner's standing instruction — `-n 10` can crash this machine through btrfs faults). Exactly the clean-tree **7 291 passed, 51 skipped in 102.30s** plus the 14 tests added here — **no regression, and the parent session's claimed figure reproduced exactly rather than being copied.** Zero failures, zero errors; neither run died without a summary line, so no environment fault was encountered | Verified at `a430707` (clean tree); the 14 added tests are `(uncommitted)` |
| 2026-08-10 | **BUG-260810193333 — quality apply WARNS and runs instead of refusing through a channel nobody watches.** Owner ruling: trust the user, run the SQL. A renamed or signature-changed buffer used to make `Apply to Quality` return False after a single Audit line and **no dialog**, while `Check and commit to sandbox` ran the very same buffer — so the gesture looked like it had succeeded and had done nothing. Now the mismatch is put to the user naming BOTH identities and the second-object consequence, and proceeding is reported (`proceeding despite identity mismatch`); declining aborts but still reports, so even the refusal is not silent. An unparseable signature is still refused outright. Part B adds a context-menu-only **Discard local change** on the DDL Explorer's object rows, gated on a host-injected checked-out predicate so it is absent rather than dead, and absent entirely on §18.7's `browse_only` sandbox tree; it drops all four pieces of a checkout together (open tab, `ddl/<relpath>.sql`, `ProjectSettings.deployed[relpath]`, the tree's unsaved-edit overlay), because a half-dropped link is worse than none. Reverses §18.5's recorded "signature-change refusal — no override, no consent path". **Assessed in this dispatch:** the `confirm=[]` pop-from-empty pin was verified to genuinely bite (see the catch-up row above). | 5 touched by the commit: `tests/ui/test_ddl_project_wiring.py` (+193), `tests/ui/test_ddl_buffer_panel.py` (+90), `tests/ui/test_deployment_menu.py` (+76), `tests/ui/test_ddl_object_editor.py` (+70/-36), `tests/ui/test_mainwindow_surface.py` (+4) | (not recorded) — the commit states no per-file targeted figure | (not recorded) by the commit body. **Covered instead by this dispatch's own observed run**: PASS — 7 291 passed, 51 skipped at the clean tree `a430707`, which includes this commit | `e922a6b` (merge; fix at `1b6bf98`) |
| 2026-08-10 | **BUG-260810174126 + the two-press escape + FQ-260810164455 — three small pieces in one merge.** The mode labels say *mode* rather than something a reader has to decode; `CodeEditorDialog`'s keyboard cancel is restored (it had been lost when `Ctrl+W`/`Ctrl+S` were unbound — the two-press escape is what gives Command mode its own Esc without stealing the dialog's); and **one importable app version** lands as `pgtp_editor/version.py`, with `pyproject.toml`'s `[project] version` as the single literal that `docs/installer.iss` already scans, so the installer and the module read the same fact by two mechanisms rather than two facts. FIVE version numbers exist in the project and only one is this one (`SERVER_VERSION`, `CURATED_BUNDLED_VERSION`, `about.py`'s vendor `22.8` and `SNAPSHOT_VERSION` are all deliberately NOT folded in — the first by owner ruling). A hardcoded literal default was rejected as the very second copy the feature removes; `UNKNOWN_VERSION = "unknown"` is the honest sentinel. **Assessed in this dispatch: the resolution order was CONFIRMED by measurement and the frozen path recorded as UNVERIFIED; 14 tests added, because the ordering decision itself had no guard** (see the catch-up row above). | 6 touched by the commit: `tests/test_version.py` (**new**, 126), `tests/ui/test_vim_mode.py` (+113), `tests/ui/test_mode_indicator_wiring.py` (+70), `tests/ui/test_about.py` (+36), `tests/ui/test_main_window.py` (+8/-8), `tests/ui/test_status_bar_static.py` (+2/-2). **Extended here by 14** — see the row above | (not recorded) — the commit states no per-file targeted figure | PASS — **7 273 passed, 51 skipped** (baseline 7 247/51), as stated by the commit body; **not re-observed at this commit.** The clean-tree run observed in this dispatch (7 291/51 at `a430707`) includes it | `0a801e3` (merge; content at `195449c`) |
| 2026-08-10 | **FQ-032 — vim Command mode: a Qt-free grammar package, `ui/vim_mode.py`, and a new shared editor layer. The largest single addition in the backlog (+307 tests).** Two editing modes, *Edit mode* and *Command mode* — and the phrase *"normal mode"* is written **nowhere** in `pgtp_editor/vim/`, in code, comments or identifiers, because it collides with vim's own NORMAL and would make every sentence ambiguous about which vocabulary it speaks; a test enforces that, over both the raw text and every AST `name`/`id`. `pgtp_editor/vim/` is a pure core (`grammar.py`, `words.py`): the count-prefix grammar, the operators, the word rule by character class. Its ban on `pgtp_editor.sql` is **not incidental tidiness** — no v1 motion may consume `sql/block_spans.py::structure_chain`, because `w`/`b`/`e` are defined by character class while the editing layer serves XML, PHP and JS buffers as well as SQL, so a motion reading a SQL span model would be wrong on four of the six surfaces; that chain's caller is the DEFERRED text objects, out of v1 scope. The spec-required test is named: `test_every_one_of_the_six_exit_triggers_restores_replace_focus`. **Assessed in this dispatch: the package's purity was CONFIRMED as genuinely static AND fresh-interpreter runtime, and BOTH legs were proved to bite by mutation** (see the catch-up row above). | 7 touched by the commit: `tests/ui/test_vim_mode.py` (**new**, 1 271 → 107 test defs), `tests/vim/test_grammar.py` (**new**, 277 → 33), `tests/vim/test_package_purity.py` (**new**, 165), `tests/vim/test_words.py` (**new**, 110 → 14), `tests/ui/test_mode_indicator_wiring.py` (+100), `tests/ui/test_mainwindow_surface.py` (+20), `tests/vim/__init__.py` (**new**) | PASS — **307 new tests**, as stated by the commit body | PASS — **7 190 passed, 51 skipped** (baseline 6 883/51), as stated by the commit body; **not re-observed at this commit.** The clean-tree run observed in this dispatch (7 291/51 at `a430707`) includes it | `b0c42da` (merge; tests at `a5e282b` + `2a31bb6`, feature at `ad42a70`) |
| 2026-08-10 | **BUG-260810194657 — `BEGIN TRANSACTION` / `BEGIN WORK` no longer open a plpgsql block, so a FALSE REFUSAL ON VALID SQL is gone.** Fixed in `blocks.begin_is_transaction` **alone**, the one predicate both the reindenter and the span model call, so FQ-034's anti-fork guarantee holds without touching either consumer. The commit records that only the `;` half of the old guard was ever live and its frozenset was **dead code**, because neither `transaction` nor `work` is in `SQL_KEYWORDS` — and `SQL_KEYWORDS` is deliberately NOT widened, since it drives call-paren gluing and unary minus and is shared *by identity* with the highlighter. A new `next_lowered` helper matches the phrase on `Token.lowered`, following `declare_is_cursor`'s precedent. **The regression the fix could have introduced is the interesting part**: the phrase is believed only when a `;`, the end of input, or a transaction mode word follows it — without that confirmation `BEGIN\nwork := 1;\nEND;` would read as transaction control and have its real `END;` swallowed, which is worse than the bug being fixed. `tests/sql/test_blocks.py`'s dormancy pin flips to `is True`, as its own comment had asked. **Assessed in this dispatch: the confirmation step is pinned in FOUR places, not three — three pins plus the formatter golden — and each was read and confirmed** (see the catch-up row above). | 4 touched by the commit: `tests/sql/test_blocks.py` (+87/-8), `tests/sql/test_formatter.py` (+78), `tests/sql/test_block_spans.py` (+33), `tests/sql/test_formatter_properties.py` (+13) | (not recorded) — the commit states no per-file targeted figure | (not recorded) by the commit body. **Covered instead by this dispatch's own observed run**: PASS — 7 291 passed, 51 skipped at the clean tree `a430707`, which includes this commit | `9c65a4c` (merge; fix at `b8619e7`) |
| 2026-08-10 | **FQ-034 — the repeatable structural ladder, plus a Qt-free span model in `sql/blocks.py` / `sql/block_spans.py` (+83 tests).** Grow/shrink selection stepping outward and back through nested SQL structure, with **shrink answering the claim** rather than being a separate model. The anti-fork guarantee is a **TEST, not a comment**: one predicate set (`BLOCK_STARTERS` and friends) serves both the reindenter and the span model, following the FQ-033 precedent, and a test derives the surface list from the code so a second copy cannot appear unnoticed — which is precisely what let BUG-260810194657 be fixed in one predicate a day later. Grow keeps its rebindable chord. **Assessed in this dispatch:** `tests/sql/test_package_purity.py` sweeps the package with `glob("*.py")`, so the new `block_spans.py` is covered by the static AST sweep and the fresh-interpreter import **by construction** — confirmed by reading the glob, not assumed | 6 touched by the commit: `tests/sql/test_block_spans.py` (**new**, 399 → 23 test defs), `tests/ui/test_structural_selection.py` (**new**, 399 → 21), `tests/sql/test_blocks.py` (+158 → 14), `tests/ui/test_select_menu.py` (+124/-~30), `tests/ui/test_mainwindow_surface.py` (+10/-~4), `tests/ui/test_menus.py` (+4/-~2) | (not recorded) — the commit states no per-file targeted figure | PASS — **6 872 passed, 51 skipped** (baseline 6 789/51), as stated by the commit body; **not re-observed at this commit.** The clean-tree run observed in this dispatch (7 291/51 at `a430707`) includes it | `cde65fa` (merge; content at `5315233`) |
| 2026-08-10 | **BUG-260810174459 / BUG-260810174919 — the checked-out `.pgtp` auto-opens on create, and provisioning NARRATES on the Messages tab.** Two reports in one merge. Creating a project checked the attachment out and then left the user looking at nothing; it now opens. Provisioning was silent while it worked, so the app looked hung; it now narrates — with a start line **before** anything happens rather than after, a close that stops a running heartbeat, and **no roles on the rows**. A collaborator boundary is enforced by test (`test_collaborator_boundaries.py` forbids this lane importing what it must not), so the narration cannot quietly grow a dependency on the layer it reports about. | 2 touched by the commit: `tests/ui/test_new_project_sandbox_provisioning.py` (+177 → 31 test defs), `tests/ui/test_ddl_project_wiring.py` (+67) | PASS — **11 new cases**, as stated by the commit body | PASS — **6 800 passed, 51 skipped**, as stated by the commit body; **not re-observed at this commit.** Note the figure sits *above* the later `cde65fa`'s stated baseline of 6 789/51 — the two shipped 21 minutes apart from separate worktrees, so they are not on one line of ancestry; both are quoted verbatim rather than reconciled. The clean-tree run observed in this dispatch (7 291/51 at `a430707`) includes this commit | `df8d908` (merge; content at `dadc5bc`) |
| 2026-08-10 | **FQ-033 — the configurable SQL formatter, the separate XML indentation engine, and `Autoformatter settings…` (§18.4 parts A–D).** The implementer's ~264 tests were **assessed, not assumed**, and five claims were each verified to actually bite — three of them by temporarily mutating the implementation and confirming named tests go red (mutation reverted with `git checkout`, tree left clean): (1) **defaults byte-identical to the pre-FQ-033 engine** — verified out-of-band by loading the OLD `sql/` package from `b5b2412^` into a scratch namespace and comparing `format_selection(text)` against `DEFAULT_FORMAT_CONFIG` over **6 699 inputs** (every string literal in the three formatter test modules plus 4 000 seeded-fuzz token soups, and again with a two-space unit): **zero divergence in `ok`, `text` and issue messages**; the durable guard is `tests/sql/test_formatter.py`'s ~60 golden-output tests, which the feature commit left untouched apart from the `indent_unit=` migration. (2) **The first-emitted-line idempotence guard is genuinely tested** — deleting it (`self._line_indent = max(level + self._clause_indent(keyword), 0)`) turns `test_the_selections_first_line_is_never_pushed_right` plus `test_a_clause_indent_never_walks_the_block_rightwards[6,8]` and `test_formatting_is_idempotent_for_every_reachable_config[6,8]` red. (3)+(4) **keywords only, opaque regions never** — casing every token instead of `Token.keyword` ones fails 7 tests including `test_identifiers_types_and_functions_are_never_cased` and `test_opaque_regions_are_never_cased`. (5) **`sql/` and `xmlfmt/` purity covers the NEW modules** — both purity modules `glob("*.py")` over the package directory, so `format_config.py`, `scanner.py`, `config.py` and `formatter.py` are swept by construction (static AST sweep + fresh-interpreter import), and `xmlfmt` additionally bans `lxml`/`xml` and pins the one-way `xmlfmt → sql` direction. Coverage **added** where the shipped suite had gaps: the whole 115-member dialect swept one keyword at a time (with balanced snippets for the five block words, so `begin`/`end`/`if`/`loop`/`case` are covered rather than skipped); two literal UPPER-cased goldens (a glue-heavy statement — `%type`, binary minus, subscript chain, call chain, `$1`, JOIN phrase, unary minus — and a plpgsql `BEGIN/IF/EXCEPTION` block) plus a whitespace-skeleton property, because the existing tests pinned the token stream but not that casing shifts **no** whitespace; the removed `indent_unit=` keyword raising `TypeError` and **no caller left in `pgtp_editor/`** (a regex over `format_selection(… indent_unit=`, since a survivor would be a TypeError only on a user's machine); the **host table itself** — all **three** `XmlEditor` instances reached through a real `CenterStage` (Raw XML, Edit XSD, **and the FQ-006 draft fragment tab**, the one FQ-033's queue entry miscounted) each reindent, each read the saved width, and each carry both command forms with the context item shortcut-less; the **absence** rows (`PhpFileTab`, `CodeEditorDialog`: no `format_selection` anywhere in the widget tree, no `Ctrl+Alt+F` `QShortcut`) — gesture absent, not refusing; the XML persistence domain being a **width in spaces, tab not in it**; and the **private real-sample `.pgtp` files** (skip-gracefully) formatted whole and by mid-document slice, asserting refusal-free, non-whitespace bytes preserved, no opening tag containing a newline (rule 1) and every text-bearing element byte-identical (rule 2 — the PHP/JS handler bodies). Those 6 sample tests **skip in this checkout** (no `sample/`), and were exercised once against a synthetic 23 KB `.pgtp` stand-in (multi-line entity-escaped `<Event>` bodies, 30 pages, long attribute lists) — all 6 passed — before the stand-in was deleted. **Two seams are deliberately NOT wired** (`main_window.py` was owned by a concurrent worktree): `Settings ▸ Autoformatter settings…` and `XmlEditor.format_refused(list)` → the `[XML]` Activity Log handler. Tests for both are written and marked `xfail(strict=False)` with the pending-wiring reason — **delete the marker when the wiring lands**; they are the 2 xfails in the figures. **No implementation bug found.** One behaviour worth recording as intended, not a defect: `save_configs` stores the XML side as `len(indent_unit)`, so a tab handed in returns as one space — the dialog's spin box cannot produce one and §18.4 B's domain is 1–8 spaces. | Extended 4: `tests/sql/test_formatter_configured.py` (+7 → 64), `tests/ui/test_autoformat_host_wiring.py` (+6 → 12, of which 2 xfail pending wiring), `tests/xmlfmt/test_formatter.py` (+6 → 91), `tests/ui/test_format_settings.py` (+1 → 12). **20 tests added.** Reviewed as already-adequate and not extended: `tests/sql/test_format_config.py`, `tests/sql/test_formatter.py`, `tests/sql/test_formatter_properties.py`, `tests/sql/test_package_purity.py`, `tests/xmlfmt/test_scanner.py`, `tests/xmlfmt/test_package_purity.py`, `tests/ui/test_autoformat_settings_dialog.py`, `tests/ui/test_xml_editor_format_selection.py` | PASS — **1 498 passed, 6 skipped, 2 xfailed in 18.54s** (`tests/sql` + `tests/xmlfmt` + `test_autoformat_host_wiring.py` + `test_format_settings.py` + `test_autoformat_settings_dialog.py` + `test_xml_editor_format_selection.py` + `test_ddl_object_editor.py` + `test_sandbox_check_console_wiring.py`) | PASS — **6 674 passed, 51 skipped, 2 xfailed in 79.79s** (`QT_QPA_PLATFORM=offscreen venv/bin/python -m pytest -q -n 4`, `-n 4` per the owner's standing instruction). Observed at working tree `a9efb67` — the parent session merged `a9efb67` (BUG-055/056/063/064, +46 tests) **during** this dispatch, which also explains a discarded earlier run: a first full run at `061e973` reported `1 failed — tests/ui/test_launcher_dialog.py::test_the_mode_never_touches_enabled_state`, whose `inspect.getsource(MainWindow._refresh_workflow_mode_affordances)` returned lines 74 short of the real definition. That is a **mid-run rewrite of `main_window.py`** by the concurrent merge (the file's mtime lands inside the run window), not a code failure: the test passes alone and in the clean re-run | FQ-033 merged at `061e973`; verified at `a9efb67`; the 20 added tests are `(uncommitted)` |
| 2026-08-10 | **FQ-030 final slice — the snippet store, its editor, and the maintenance-only `Settings` menu (§18.9).** Verified against the implementation's own claims rather than assumed: (1) a **corrupt store is never written over** — `load_snippets` returns the defaults plus an `error`, the lane goes read-only with the reason in the dialog's note, `save()` refuses, **and a programmatic `accept()` on that read-only dialog still leaves the file byte-identical**, while the defaults are nonetheless pushed live into the SQL editors so a broken file cannot disable Ctrl+Alt+E; (2) **import never applies a collision without an explicit Yes** — including a trigger word differing only in CASE, which must ask because `find_snippet` matches case-insensitively — and lands in the dialog's rows, so Cancel undoes adds, deletes and body edits alike and nothing reaches disk before OK; (3) **nothing in `Settings` carries a shortcut**, swept RECURSIVELY through submenus and over `shortcuts()` as well as `shortcut()` (DEC-006: hiding a `QMenu` leaves its children's chords live); (4) **`Restore Built-ins` never reverts an edited built-in**; (5) **`sql/snippet_store.py` stays Qt-free**, covered automatically by `tests/sql/test_package_purity.py`'s glob over `sql/*.py` (static AST sweep + fresh-interpreter import), confirmed rather than taken on faith. Added coverage for gaps the implementer's suite left: `apply_import` **raising `TypeError` without the explicit `overwrite=`** (the "no default" promise), the whole-set-not-a-diff guarantee across a save/reload (**a deleted built-in stays deleted; an edited one stays edited and is not reported missing; an empty set is not re-read as a fresh install; saving replaces rather than appends**), hand-written-file tolerance (missing `version`/`title`, unknown version, extra keys) versus refusal (JSON scalar, `"snippets"` not a list, a non-object row, a non-text body, a non-UTF-8 file), a non-English body surviving `ensure_ascii=False` round trip, **the export file loading back as a store** (the "mail it to a colleague" claim), and the two unsaid failure legs — a save that could not be written (reported via `QMessageBox`, set in force unchanged) and a failed/unreadable export or import. Every modal is monkeypatched through `pgtp_editor.ui.modals`; no dialog is `exec`d; `config_dir`/`generator_config_dir` is a `tmp_path` in every test, so the developer's real per-user store is never touched. **No implementation bug found.** | Extended 3: `tests/sql/test_snippet_store.py` (+14 → 33), `tests/ui/test_snippet_controller.py` (+9 → 29), `tests/ui/test_edit_snippets_menu.py` (+1 → 14). Reviewed as already-adequate, not extended: `tests/ui/test_edit_snippets_dialog.py`, `tests/sql/test_package_purity.py` | PASS — 1218 passed (`tests/sql` + `test_snippet_controller.py` + `test_edit_snippets_menu.py` + `test_edit_snippets_dialog.py` + `test_center_stage.py` + `test_code_editor.py`, `-n 4`) | PASS — **6287 passed, 45 skipped in 81.20s** (`QT_QPA_PLATFORM=offscreen venv/bin/python -m pytest -q -n 4`; `-n 4` per the owner's standing instruction on this machine). Exactly the implementer's 6263 plus the 24 tests added here — no regression | `229dc11` (feature); the 24 added tests are `(uncommitted)` |
| 2026-08-10 | **Baseline run taken while reconstructing the backlog below — NOT a feature verification.** No test was written, extended or changed for this row: it exists so the reconstructed block is anchored to one figure that was **personally observed** rather than quoted from a commit body. Recorded caveat: the tree was being edited by three concurrent agents (`pgtp_editor/ui/` + `tests/ui/`, `pgtp_editor/sql/` + `tests/sql/`, and the consolidated spec) at the moment of the run, so this figure describes a work-in-progress tree, not any single commit. It is ABOVE the last committed figure (`69557d2`'s 5787) by 38, which is consistent with the sibling agents' in-flight test additions rather than with a regression; nothing here supersedes `69557d2`'s own stated result. | None — documentation-only pass over `docs/TEST_LOG.md` | n/a | PASS — **5825 passed, 45 skipped in 60.84s** (`QT_QPA_PLATFORM=offscreen venv/bin/python -m pytest -q -n 10`), observed once, in this dispatch, on an actively-edited working tree | `(uncommitted)` — working tree on `dev_review_01` at `a5913ab` |
| 2026-08-10 *(reconstructed)* | **FQ-028 — the app-shell chrome redesign, with FQ-018 absorbed into it.** Three surfaces replace the one overloaded Audit dock: a Findings tab in the left dock, a STATIC status bar with a colour-coded mode indicator, and a two-tab bottom dock (Activity Log + Results). `StaticStatusBar` overrides `showMessage` to never paint and journals to the Activity Log instead, so ~40 call sites, `_shell_status` and `busy.py` kept working untouched; `AuditRouter` is a facade that quacks like the old `QListWidget` and routes by prefix, so not one producer changed across a ~430-reference test surface. FQ-018's two connectivity dots ship here on a 30s `isActiveWindow()`-gated timer with an explicit UNKNOWN state. Two commit-recorded corrections to the feature's own text: a TENTH prefix (`[Sandbox]`) missing from its disposition table, and the trailing mode panel having to become `ToolbarController._trailing_widget` because `apply_ids` detaches every action. | 21 files touched under `tests/ui/`: `test_activity_log_wiring.py`, `test_curated_feed_wiring.py`, `test_ddl_object_editor_wiring.py`, `test_ddl_project_wiring.py`, `test_edit_auto_xsd_tab.py`, `test_edit_xsd_tab.py`, `test_findings_surfaces.py`, `test_generation.py`, `test_list_all_bookmarks.py`, `test_main_window.py`, `test_main_window_theme.py`, `test_mainwindow_surface.py`, `test_menus.py`, `test_mode_indicator_wiring.py`, `test_new_project_sandbox_provisioning.py`, `test_project_capability_wiring.py`, `test_sandbox_check_console_wiring.py`, `test_sandbox_setup_wiring.py`, `test_schema_learning_wiring.py`, `test_status_bar_static.py`, `test_toolbar.py` | (not recorded) — the commit states no per-file targeted figure | PASS — 5787 passed, 45 skipped (+105), as stated by the commit body | `69557d2` |
| 2026-08-10 *(reconstructed)* | **FQ-030 slice 1 — the Qt-free FROM/alias scope analyzer; BUG-041 filed.** `analyze_from_scope(text, pos) -> FromScope` never returns None and never raises (an editor calls it per keystroke): alias resolution for every FROM/JOIN spelling, UPDATE/DELETE targets, per-scope paren nesting, quoted identifiers, scope per STATEMENT via the reused `split_statements`. `ALIAS_REF` lands in `caret_context` as a refinement of `DOTTED_PATH`, not a rival. Deliberate boundaries: a CTE reference resolves to its NAME but not its columns; derived tables and SRFs are kept with `is_derived=True`. **BUG-041 was found and filed, not fixed:** completion is structurally dead inside a `$$` body — i.e. most of a DDL object tab — because `_caret_inside_opaque_token` returns None for any opaque token; a latent second defect (`_complete_identifier` falling back to `prefix_len = 0`) rides with it. The consumer side is deliberately unwired. | `tests/sql/test_caret_context.py`, `tests/sql/test_from_clause.py` | PASS — 1964 passed, 13 skipped (`tests/sql` + `tests/db`), as stated by the commit body | (not recorded) — the commit ran the pure layers only and claimed no full-suite figure | `c25debf` |
| 2026-08-09 *(reconstructed)* | **FQ-019 stage 3 — the Activity Log is wired: dock, lifecycle and the call sites.** A real `QDockWidget` beside the Audit panel (`splitDockWidget`, not tabified) with the View toggle and `windowState` persistence its three sibling docks get. TWO host entry points so no call site gates on mode: `record_activity` for DB rows (the caller knows the connection ROLE as a fact) and `record_file_activity`, the single place `Project files` vs `Quality files` is decided. Eleven gestures wired in BOTH their success and failure legs (.pgtp open/save/save-as/discard, merge, DDL object tab save, PHP tab save, Apply to Sandbox, Apply to Target, both Check gestures, console Run); an apply is failed when it did not commit, a CHECK only on real blockers, because a probe rolls back. Three deliberate gaps recorded: refusals where nothing started get no line, `Save XSD` fits none of the four sources, and `FILE_VERB_LINTED` has no producer at all. | `tests/ui/test_activity_log_wiring.py`, `tests/ui/test_apply_changes_to_target.py`, `tests/ui/test_ddl_object_editor_wiring.py`, `tests/ui/test_mainwindow_surface.py`, `tests/ui/test_menus.py` | (not recorded) | PASS — 5682 passed, 45 skipped, as stated by the commit body | `65a0f1b` |
| 2026-08-09 *(reconstructed)* | **Follow-up fix — retire the last claims that provisioning is unreachable.** A USER-VISIBLE Audit line (`_NO_TARGET_BASELINE_NOTE`) told the user "re-provisioning is not reachable with a project open (BUG-040)" — true when written, false since `e79626c`; it now points at the Sandbox provisioning group. Same class of staleness cleared out of `ddl_object_editor`'s KNOWN GAP comment, two in `ddl_check`, and `sandbox.py`'s `applied()` docstring — which surfaced that NOTHING in the app currently shows the working set. Manual corrections ride along, including that Maintenance mode no longer claims to filter what you can DO. | **None** — no file under `tests/` was touched, although a user-visible string changed | PASS — 23 manual tests, and 1302 passed across `tests/db` + affected UI, as stated by the commit body | (not recorded) | `f1d782b` |
| 2026-08-09 *(reconstructed)* | **BUG-040 leg 4 — provisioning moves into Project Settings; `Sandbox Setup…` is DELETED.** Owner ruling. Leg 3 hid `Database ▸ Sandbox Setup…` in project mode on the premise that Project Settings already owned every piece of sandbox configuration; it did not — Provision, Reset and "create a sandbox database for me" existed nowhere else, and projectless that dialog built no controls at all, so all three were unreachable in every mode. This makes the premise true rather than reverting the hiding. The three gestures land on the Connections tab under the sandbox-mode radios as a group REBUILT wholesale rather than enabled/disabled (carve-out 2 travels with them). `Sandbox Setup…` is deleted, not hidden, for the hazard BUG-040 itself named: a hidden action stays pinnable. One quietly false promise fixed — the mode radios said a change takes effect on reset, but `SandboxSession.reset()` re-runs the CREATION mode. | `tests/ui/_sandbox_stubs.py`, `test_ddl_explorer_sandbox.py`, `test_deployment_menu.py`, `test_mainwindow_surface.py`, `test_modals.py`, `test_sandbox_check_console_wiring.py`, `test_sandbox_controller.py`, `test_sandbox_setup_dialog.py` (deleted with its module), `test_sandbox_setup_wiring.py`, `test_toolbar.py` | (not recorded) | PASS — 5653 passed, 45 skipped, as stated by the commit body; the commit explains the negative delta from 5674 as the deleted dialog's test file minus the new ones | `e79626c` |
| 2026-08-09 *(reconstructed)* | **Unbind `Ctrl+S` and `Ctrl+W` in `CodeEditorDialog`.** Owner decision completing the sweep: this modal was the LAST carve-out for either chord. Both hosts were removed, which is the load-bearing part — the pair was implemented TWICE on purpose (a `QShortcut` AND a `keyPressEvent` branch, because `QShortcut` activation is unreliable under the offscreen platform), so a sweep taking only one would have left the keys working in the real app while the suite passed. The replacement test asserts absence at both, with a companion pinning that OK and Cancel are still reachable. | `tests/ui/test_code_editor.py` | (not recorded) | (not recorded) | `9268cad` |
| 2026-08-09 *(reconstructed)* | **Read table comments; BUG-036 closed.** Owner decision: widen introspection so `Set Table Comment…` seeds the existing text. It could not before — `pg_description` was read for columns only and `TableInfo` had no comment field — and because a blank box means `IS NULL`, an untouched OK silently removed a comment the table may have had. The existing per-relation query grew `obj_description(c.oid, 'pg_class')` (same catalog `_COLUMNS_SQL` already reads, so no new round trip); `_build_tables` unpacks with `*rest`, tolerating the suite's canned 3-tuple rows. `SNAPSHOT_VERSION` 2 → 3, justified rather than defaulted. **BUG-036 closes with the requirement DROPPED, not deferred:** the owner ruled Open/Save As stay native pickers. | `tests/db/test_introspect.py`, `tests/db/test_schema_snapshot.py` | PASS — `tests/db`: 1229 passed, 13 skipped, as stated by the commit body | (not recorded) | `9cafb38` |
| 2026-08-09 *(reconstructed)* | **Unbind `Ctrl+W` — and pin a gap in FQ-027 it exposed.** Same reasoning as `Ctrl+O`: the app closes projects, .pgtp documents, PHP tabs, DDL object tabs, the XSD tab and console tabs, and the one "close" `Ctrl+W` meant was the rarest. `CodeEditorDialog`'s own `Ctrl+W` was untouched here (removed later, in `9268cad`). **The real payload is the gap it exposed and PINNED rather than fixed:** Maintenance mode's two halves disagree about what "hidden" means — `File` is trimmed member by member so a hidden File command genuinely loses its key, but View/Database/Tools/Generation are hidden by hiding the top-level `QMenu`, leaving their child actions visible AND enabled, so Qt still dispatches their shortcuts. Not fixed, because the obvious fix collides with FQ-027's Q2 owner ruling ("menu bar only, leave the toolbar alone"); both behaviours carry tests, one named `..._KNOWN_GAP`. Four FQ-012 tests moved specimen to `Navigation ▸ Toggle Bookmark` / `Ctrl+F2`. | `tests/ui/test_launcher_dialog.py`, `tests/ui/test_menus.py`, `tests/ui/test_toolbar.py` | (not recorded) | PASS — 5674 passed, 45 skipped, as stated by the commit body | `150356a` |
| 2026-08-09 *(reconstructed)* | **FQ-025 slice 3 wired — FQ-025 is complete (18 operations across three slices).** The submenu groups what is SCOPED TO THE CLICKED TABLE rather than what literally emits `ALTER TABLE`, so Create Index, Drop Index and Drop Table sit there; Drop Table gets its own final group so it is nowhere near a mis-click on Drop Index. Create Table is the exception and sits at top level in two places, including on table and VIEW nodes because that is the only place in the tree that names a schema — which `CreateTableDialog(schema=…)` consumes. The `indexes == {}` trap was VERIFIED, not assumed: `_ddl_schema` comes from the only fetch that runs `INDEX_SQL`, with a `replace(schema, indexes={})` variant test proving the picker would silently go empty on a `fetch_schema` result. One gap documented rather than hidden: Set TABLE Comment cannot open on the existing comment (closed later by `9cafb38`). | `tests/ui/test_ddl_buffer_panel.py`, `tests/ui/test_ddl_creation_wiring.py`, `tests/ui/test_mainwindow_surface.py` | (not recorded) | PASS — 5673 passed, 45 skipped, as stated by the commit body | `69c95b5` |
| 2026-08-09 *(reconstructed)* | **FQ-025 slice 3 (pure) — indexes, comments, create/drop table.** Six emitters and five dialogs, not yet menu-wired. Slice 1's `_comment_on_column` was PROMOTED rather than duplicated, pinned by a test asserting add-column's comment tail is byte-identical to set-column-comment's output. Three of the queue entry's slice-3 claims were wrong and are recorded: the index introspection had already landed with slice 2; DROP INDEX is not table-scoped and CREATE INDEX is not an ALTER TABLE; and `INDEX_METHODS` is wider than slice 2's `EXCLUDE_METHODS`, with a test pinning the subset relation so the two cannot drift into looking interchangeable. Drop table has NO confirmation by owner ruling, with a test asserting the dialog contains zero `QLineEdit`s. | `tests/db/test_ddl_skeleton.py`, `tests/ui/test_table_dialogs.py` | (not recorded) | PASS — 5648 passed, 45 skipped, as stated by the commit body | `ef46625` |
| 2026-08-09 *(reconstructed)* | **Unbind `Ctrl+O`.** Owner decision: in an app that opens .pgtp files, PHP files, projects, XSDs and database objects, one `Ctrl+O` has to pick a winner among five kinds of "open", and any winner is a guess. UNBOUND, not reassigned — FQ-012 (shipped an hour earlier) lets anyone who disagrees bind it to whichever open they mean. Three tests that used `File ▸ Open` as their specimen precisely BECAUSE it owned a shortcut moved to `File ▸ Close` rather than being deleted: FQ-012's capture test (needs a destroyable default), FQ-012's steal test (needs the LOSER to have had a key), and FQ-027's hidden-shortcut test (needs a key that can be suppressed). | `tests/ui/test_launcher_dialog.py`, `tests/ui/test_menus.py`, `tests/ui/test_toolbar.py` | (not recorded) | (not recorded) | `f621d4c` |
| 2026-08-09 *(reconstructed)* | **FQ-025 slice 2 wired — constraints and foreign keys on the submenu.** `Alter Table >` carries twelve operations: the eight column ones, then the four constraint ones behind a separator. `ALTER_TABLE_ACTIONS` becomes DERIVED rather than authored, joined through a groups table whose only purpose is telling the builder where the one separator goes, while slice 1's flat name still exports all twelve in menu order. ONE signal, not a parallel one — the four constraint ops need precisely the context the eight column ops do and use it. `AddForeignKeyDialog` needed no new injection at all. One rough edge recorded rather than hidden: the menu offers all twelve unconditionally because the panel does not know the constraint list, so Drop/Rename on a table with no constraints open and show their refusal in the error label rather than being greyed out. | `tests/ui/test_ddl_buffer_panel.py`, `tests/ui/test_ddl_creation_wiring.py`, `tests/ui/test_mainwindow_surface.py` | (not recorded) | PASS — 5454 passed, 45 skipped, as stated by the commit body | `b8a005d` |
| 2026-08-09 *(reconstructed)* | **FQ-019 stage 2 — one timestamp format, and the Activity Log panel widget.** Owner decision: the dynamic timestamp is DROPPED for a single fixed `YYYY-MM-DD HH:MM`. The specified format switched on the whole log's calendar span, making it a property of the SET rather than of an entry — so one entry arriving after midnight silently reshaped every row already on screen, and a panel could never cache a rendered row. `TIME_FORMAT_SAME_DAY` is DELETED rather than kept unused; `timestamp_format(entries)` survives with its argument ignored. The tests that pinned the dynamic rule were replaced by their inverse. The panel itself (new, not yet docked): injected entries, 20-char previews, click-through to the full text in a read-only `CodeEditorDialog`, and an `append()` pinned by a cross-midnight test asserting the earlier row is byte-identical. | `tests/db/test_activity_log.py`, `tests/ui/test_activity_panel.py` | PASS — 53 tests here plus 23 manual tests green, as stated by the commit body | (not recorded) | `1aefc53` |
| 2026-08-09 *(reconstructed)* | **FQ-025 slice 2 (pure) — constraint and foreign-key emitters and dialogs.** Four emitters and four dialogs, not yet menu-wired. The unified Drop constraint is honoured literally: ONE emitter, with a guard test asserting `drop_foreign_key_skeleton` does not exist (in Postgres a FK is a constraint and DROP CONSTRAINT is identical for every type). EXCLUDE is expression-shaped, not column-shaped, which the entry's column-picker design did not anticipate, so it shares CHECK's free-expression field plus a `USING <method>` dropdown; supplying the wrong one is REFUSED, not ignored. Constraint names are REQUIRED on both ADDs, settling the entry's open question 2 the opposite way to its own recommendation and flagged as a divergence rather than taken silently. Changing the table collapses the column picker to one row rather than re-pointing rows at same-named columns of a different table. | `tests/db/test_ddl_skeleton.py`, `tests/ui/test_constraint_dialogs.py` | (not recorded) | PASS — 5431 passed, 45 skipped, as stated by the commit body | `c19a09f` |
| 2026-08-09 *(reconstructed)* | **FQ-012 stage 2 — `View ▸ Customize Shortcuts…` wired: capture, apply, persist.** Lives on the host, not on `ToolbarController`, because the registry is Qt-free and the dialog holds no `QAction`. Default capture is capture-ONCE, and that is load-bearing: after an override is installed the `QAction` no longer knows its built-in key, so re-reading on a re-walk would enshrine the override AS the default and turn "Reset to Default" into a permanent no-op. Applying is TWO passes — clear every enumerated action, then set the non-empty ones — because one pass could leave a stolen chord on two enabled actions mid-loop, which Qt answers by firing NEITHER: exactly the ambiguity the steal rule exists to prevent. On rebuild, defaults are re-captured and the stored map is pruned through `resolve_shortcut_overrides`. Cancel writes nothing. | `tests/ui/test_mainwindow_surface.py`, `tests/ui/test_menus.py`, `tests/ui/test_toolbar.py` | (not recorded) | PASS — 5246 passed, 45 skipped, as stated by the commit body | `c4a838d` |
| 2026-08-09 *(reconstructed)* | **FQ-027 fix — `Exit` survives Maintenance mode, and the hidden-shortcut question is answered.** `_MAINTENANCE_FILE_ITEMS` trimmed `File` to `New Session` alone, so the mode hid the way out of the APPLICATION — the same trap `New Session` exists to prevent, one level up. The spec's membership table already said `Exit` stays; the code disagreed. Found by manual-maintainer, which discovered it had to tell the reader to quit with the title bar. Also pins by test the question the previous pass refused to answer from inference: `Ctrl+O` does NOT fire while `File ▸ Open…` is hidden, because `QAction.isEnabled()` returns False for a hidden action in PySide6 and Qt dispatches a shortcut only to an enabled action. | `tests/ui/test_launcher_dialog.py` | (not recorded) | PASS — 5239 passed, 45 skipped, as stated by the commit body | `23c5cb6` |
| 2026-08-09 *(reconstructed)* | **FQ-025 slice 1 wired — `Alter Table >` on the tree, into an editable tab.** Eight column operations behind a submenu, each opening a dialog whose output lands in an editable tab the user runs or does not; generation executes nothing. FQ-025's premise was wrong in a way that changed the work: column nodes did not exist at all, so the tree gained a `Columns (N)` group per table, placed after the trigger leaves. An ALTER is not an object and `DdlObjectRef` cannot express one without lying (`qualified` would render `pr.orders()`), hence `AlterDdlRef`, duck-typed, `kind="alter"`, with `name=""` DELIBERATELY so tier 3's plpgsql_check statements are honestly absent rather than failed. A serial counter gives each generation its own tab — without it `open_ddl_object_tab` would focus the first and SILENTLY DISCARD the second statement. Save-to-object is suppressed STRUCTURALLY rather than disabled. Two consequences recorded rather than papered over: `Save in Project` on an ALTER tab opens Save As…, and the sandbox `applied` row is per-table so successive ALTERs overwrite each other's bookkeeping. | `tests/ui/test_ddl_buffer_panel.py`, `tests/ui/test_ddl_creation_wiring.py`, `tests/ui/test_mainwindow_surface.py` | (not recorded) | PASS — 5238 passed, 45 skipped, as stated by the commit body | `532da30` |
| 2026-08-09 *(reconstructed)* | **FQ-012 stage 1 — the shortcut registry and the Customize Shortcuts dialog (pure layers).** New files only, so it could run beside the FQ-025 wiring that owns `main_window.py`; the host wiring is the separate pass logged above. `shortcut_registry.py` is the Qt-free twin of `toolbar_registry.py` and imports its alias tables rather than growing a second copy. `normalize_sequence` is not tidiness: Qt spells "esc" as `Esc` and treats `Ctrl+Enter` and `Ctrl+Return` as different strings, so without one canonical spelling every reserved-key and conflict check is defeated by capitalization. The conflict rule is STEAL for editable commands, REFUSE for anything the dialog does not own — because two enabled shortcuts on one chord are AMBIGUOUS and Qt fires NEITHER, so a duplicate does not degrade, it deletes both commands from the keyboard. 27 reserved bindings transcribed with the sentence each refusal shows. | `tests/ui/test_customize_shortcuts_dialog.py`, `tests/ui/test_shortcut_registry.py` | PASS — 56 tests, as stated by the commit body | (not recorded) | `5f53583` |
| 2026-08-09 *(reconstructed)* | **FQ-027 — the launcher becomes three modes, and Maintenance filters the menu bar.** A 1x3 row (Standalone / Project / Maintenance) replacing FQ-010's four wrapped groups, and picking Maintenance drops View, Database, Tools and Generation from the window menu bar, trims File, and leaves Schema and Help whole. This is the app's first INTENT-based hiding, and the spec (`cb58629`) carries rule A (capability) and rule B (intent) apart with a test for telling them apart. Session-only, so there is no key to get stuck in: one in-memory attribute, no QSettings read or written. `launcherSuppressed` and the "don't show again" checkbox are DELETED outright — an unconditional starting gate cannot strand a user in a mode they cannot see the way out of. One refresh entry point, `setVisible` only, build-once so `collect_menu_commands()` is identical in and out of the mode (asserted, because a rebuilt menu would drop saved `toolbarIds`); it walks `menuBar()` ONLY, which is what keeps `Deployment ▸ Save XSD` reachable inside the mode. Note for later test authors recorded in the commit: `QAction.isEnabled()` returns False for a hidden action, so "hidden, not greyed" is asserted via the refresh source containing no `setEnabled`. | `tests/ui/test_launcher_dialog.py`, `tests/ui/test_mainwindow_surface.py`, `tests/ui/test_menus.py` | (not recorded) | PASS — 5162 passed, 45 skipped, as stated by the commit body | `da80b1a` |
| 2026-08-09 *(reconstructed)* | **FQ-025 slice 1 UI — the column-operation dialogs.** New files only, so it could run beside the FQ-027 work that owns `main_window.py`. Five classes over a shared base, split by WHAT EACH COLLECTS rather than one per menu item: `ColumnActionDialog` covers the four operations whose emitters take exactly table+column, and Add / Rename / Change type / Set DEFAULT get their own. Validity is decided by CALLING THE EMITTER and catching `SkeletonError` / `UnsafeIdentifierError`, never by a parallel validator that could drift from it. The USING and DEFAULT provenance rule is STRUCTURAL, not documentary: both widgets are built by one helper that accepts no initial text, no completer and no model, and a test asserts passing a seed raises `TypeError`. | `tests/ui/test_alter_column_dialogs.py` | PASS — 30 tests, including an AST check that the module imports no `db/introspect` or `psycopg`, as stated by the commit body | (not recorded) | `8a88da4` |
| 2026-08-09 *(reconstructed)* | **FQ-021c — Navigation gains the three mode-only Compare/Merge members. Closes a LIVE REGRESSION.** FQ-020 removed `Apply Changes to Target` from Tools expecting this leg to re-home it, and this leg never shipped — so a user could enter Compare/Merge mode, step differences and check them, with NO gesture anywhere in the app that writes them to the target file. The implementation was intact and tested the whole time; it was simply unreachable. Navigation now carries `Next Difference`, `Previous Difference` and `Apply Changes to Target`, all three `setVisible(False)` outside the mode, deliberately not on Deployment and carrying no shortcut. The gate is the MODE, not the tab; `CenterStage` exposes the mode plus a signal and the host connects it ALONGSIDE `currentChanged`, because `leave_diff_merge_mode` emits nothing when Raw XML is already current — a reachable state via the panel's Close button, now covered by a test asserting zero `currentChanged` emissions while the members still hide. `set_bookmarks_enabled` stops disabling the whole QMenu, as its own docstring predicted. Three `RENAMED_ID_ALIASES` rows. | `tests/ui/test_bookmarks_caption_gating.py`, `test_diff_merge_entry_points.py`, `test_diff_merge_mode.py`, `test_menus.py`, `test_toolbar.py`, `test_toolbar_registry.py` | (not recorded) | PASS — 5105 passed, 45 skipped, as stated by the commit body | `1ccfe9d` |
| 2026-08-09 *(reconstructed)* | **Fix — the two provisioning notes stop naming a hidden menu entry.** Missed from `bc02d9c`'s staging though its message describes them. Same class as `9b9aef7`: both fire in project mode, and BUG-040 hid `Sandbox Setup…` there. The empty-baseline note now says re-provisioning is not reachable with a project open rather than pointing at it; the no-target-clone note points at Project Status's sandbox data node, which is genuinely wired in project mode. | **None** — no file under `tests/` was touched, although two user-visible strings changed | (not recorded) | (not recorded) | `1eaf829` |
| 2026-08-09 *(reconstructed)* | **FQ-019 stage 1 + FQ-025 slices 1–2 pure layers, built in parallel** by three agents on disjoint file sets — the only safe shape here, since every UI wiring path collides in `main_window.py`. **FQ-019 core** (`db/activity_log.py`, new): the entry dataclass, JSONL round-trip keeping FULL ddl and error text, and the timestamp formatter; the four source labels double as the persistence indicator so callers never gate on mode; previews are derived, not stored, because a second copy can only drift. **FQ-025 slice 1** (`db/ddl_skeleton.py`): eight pure emitters, same allowlist-or-raise posture; Set/Drop NOT NULL and DEFAULT are separate functions rather than one boolean-flagged one, since a `not_null=False` call site reads as a modifier when it is the choice of statement; USING and DEFAULT expressions cannot be allowlisted so they are checked for staying ONE statement. **FQ-025 slice 2 introspection:** `ConstraintInfo` and `IndexInfo`; the constraint query widens strictly additively (the first five values keep meaning AND position) and the conkey joins become LEFT so a table-level `CHECK (true)` yields a row instead of vanishing; constraint-backed indexes are captured and MARKED, never filtered. Widening `DatabaseSchema` tripped `schema_snapshot`'s drift guard by design: `SNAPSHOT_VERSION` → 2, REFUSING v1 rather than loading it with the sections empty, since "loaded, but every constraint is missing" is exactly what makes `diff_schemas` emit DROPs. | `tests/db/test_activity_log.py`, `tests/db/test_ddl_check.py`, `tests/db/test_ddl_skeleton.py`, `tests/db/test_introspect.py` | PASS — 46 + 196 + 88 = 330 tests across the three layers, as stated by the commit body | PASS — 5091 passed, 45 skipped, as stated by the commit body | `bc02d9c` |
| 2026-08-09 *(reconstructed)* | **BUG-040 fallout — the refusals stop naming a menu entry they cannot reach.** A sandbox exists only in project mode, so both refusals fire only there — and both named `Database ▸ Sandbox Setup…`, which leg 3 had just hidden exactly there. Naming a hidden entry is the same dead end as naming a deleted one, which is the failure the console test was already written to catch two wordings ago. Both now name Project Settings: `DESTINATION_UNAVAILABLE_REASONS[DEST_SANDBOX]` (reused verbatim by `_refuse_sandbox_gesture`, so this is the Apply/Check refusal too) and `sql_console_panel.NO_SESSION_TEXT`. The console test's rule was SHARPENED with them — a refusal may only name a way back that is reachable FROM WHERE IT FIRES; "exists in the menu somewhere" was not strong enough to catch this. | `tests/ui/test_ddl_object_editor.py`, `tests/ui/test_sandbox_setup_wiring.py` | (not recorded) | PASS — 4870 passed, 45 skipped, as stated by the commit body | `9b9aef7` |
| 2026-08-09 *(reconstructed)* | **BUG-040 leg 3 — `Sandbox Setup…` is projectless-only.** The third leg, which reached the queue after the first two shipped: in project mode the session connects itself and the sandbox connection and mode live in Project Settings, so the menu entry is a second door onto the project's own surface. HIDDEN, not deleted — unlike Open/Close Sandbox Session it still has a mode where it means something, and projectless it is the only way to get a sandbox at all. Also drops a dead `has_session` local left in `_refresh_sandbox_affordances`. **Recorded honestly by the commit: the entry's premise was VERIFIED AND NOT TRUE** — Project Settings had no Provision, no Reset and no "create a database for me", so a project-mode user was left with no path to re-provision or reset. Reported to the owner rather than silently widened, and closed later by `e79626c`. | `tests/ui/test_sandbox_check_console_wiring.py`, `tests/ui/test_sandbox_setup_wiring.py` | (not recorded) | PASS — 4870 passed, 45 skipped, as stated by the commit body | `4e36162` |
| 2026-08-08 *(reconstructed)* | **FQ-020 follow-up — `Save XSD` refuses off-tab instead of truncating `curated.xsd`.** The one fall-through the new Deployment menu still had: `XsdController.save` wrote `stage.xsd_editor.toPlainText()` to `curated.xsd` unconditionally, so triggering `Save XSD` with the Edit XSD tab closed overwrote the file with the empty buffer. Unreachable by click (Qt will not trigger a hidden QAction), but FQ-012 enumerates hidden actions by design, which is exactly how a shortcut lands on it. Now gated like every sibling entry, stating the reason. This is the commit that carried the FQ-020 feature-tester row already in this log (2026-08-08), its +14 tests and the manual fold-in; the row is logged here separately because the FIX itself post-dates that row's verification. | `tests/ui/test_deployment_menu.py`, `tests/ui/test_mainwindow_surface.py`, `tests/ui/test_menus.py`, `tests/ui/test_toolbar.py` | (not recorded) | PASS — 4844 passed, 45 skipped, as stated by the commit body | `7e35f03` |
| 2026-08-08 *(reconstructed)* | **FQ-021b — the `Bookmarks` menu becomes `Navigation`.** Command ids derive from the menu label AND path, so `bookmarks.*` becomes `navigation.*` — five rows, one per member. They go in `RENAMED_ID_ALIASES`, never `LEGACY_ID_ALIASES`: `ICON_ID_BY_COMMAND` is that dict inverted, so a rename row there would resolve the new id back to the old one as an ICON id, raising `KeyError` inside `icons.load_svg_text` — swallowed by `_set_action_icon`'s bare `except Exception: pass`. Silently wrong, and it would permanently defeat any future default icon. A toolbar button pinned before the rename survives, proven end to end with a real pre-rename `.ini` carrying both `toolbarIds` and `toolbarIconIds`. **The guard was verified to bite** — deleting the five rows fails exactly four tests; a table wired into only `resolve_ids` would have passed the id test alone, which is why the icon-resolver test exists. Only the MENU was renamed, not the action group. | `tests/ui/_menu_helpers.py`, `test_bookmarks_caption_gating.py`, `test_list_all_bookmarks.py`, `test_menus.py`, `test_select_menu.py`, `test_toolbar.py`, `test_toolbar_registry.py` | (not recorded) | PASS — 4801 passed, 45 skipped (+7, all new), as stated by the commit body | `1d53abd` |
| 2026-08-08 *(reconstructed)* | **FQ-023 — the sandbox gestures state their reason instead of vanishing.** Check Object in Sandbox, Check Object Without Applying and Sandbox SQL Console… were absent from the menus without a live session, so a user with an all-green project could not see them or learn they existed. Carve-out 2 is NARROWED, not overturned — that distinction is the whole feature: no sandbox configured means still absent; sandbox configured but no session means present, and clicking states why and offers the fix. One `_refuse_sandbox_gesture` shared by all five gestures, reusing `DESTINATION_UNAVAILABLE_REASONS[DEST_SANDBOX]` imported rather than re-typed, so picker and menus cannot drift into two vocabularies. The session stays EXPLICIT: an Open/Cancel dialog, no auto-retry, and a test asserting the prober is never called when the user declines. Check refuses on `not can_check` BEFORE the "no open DDL object tab" message, so the user is not sent to fix the wrong prerequisite. (Superseded a day later by BUG-040, which made the session auto-open.) | `tests/ui/test_mainwindow_surface.py`, `tests/ui/test_sandbox_check_console_wiring.py`, `tests/ui/test_sandbox_setup_wiring.py` | (not recorded) | PASS — 4794 passed, 45 skipped, verified in the main tree together with FQ-021a, as stated by the commit body | `1df4ecf` |
| 2026-08-08 *(reconstructed)* | **FQ-021a — Compare/Merge is a mode, and read-only is a SET of reasons.** The precondition first: read-only was a single boolean and both `leave_*` methods cleared it unconditionally, so once Compare/Merge also locked Raw XML, leaving either mode would unlock the editor while the other was still active. BUG-037's `_set_raw_xml_read_only(reason)` seam is generalized into a set — `active=True` adds, `active=False` discards only that reason, and flag and title are both re-derived on every call, making BUG-037's "flag and title cannot drift" invariant structural rather than remembered. With two locks the tab reads `Raw XML (read only in caption mode + compare/merge mode)`, sorted so one state cannot render two ways. Only the SUCCESS path leaves the mode, so "uncheck and retry" still works after a refused Apply. **Fixes an undocumented pre-existing defect:** all three entry points called `setCurrentIndex` without `setTabVisible` on a tab hidden at construction, so a user who switched away from a loaded comparison had no way back — existing tests asserting `currentIndex` were passing over a tab the user could not see. | `tests/ui/test_diff_merge_mode.py` | PASS — 60 + 174 passed across the diff, caption and center-stage suites, as stated by the commit body | (not recorded) — the commit explicitly declines to claim a full-suite figure because FQ-023's in-flight work shared the tree; it states a worktree measurement of 4782 passed, 45 skipped, +14 and no other deltas | `75e2cdb` |
| 2026-08-08 *(reconstructed)* | **FQ-022 — a sandbox-scoped DDL Explorer beside the quality one.** Implements §18.7, settled 2026-08-05 and unbuilt until now: you could see the quality database but not the sandbox you were applying to. Two Database entries and two dock tabs, both driven by ONE role-parameterized fetch/open/lockstep path. SESSION-FREE by design — §18.5 D2 exempts reads from the ownership gate, so the predicate is `bool(sandbox.host)`, never `has_session`, with tests pinning that opening the Explorer opens no session and the session-keyed refresh does not close one. The Sandbox entry exists at startup but is hidden until a project with a sandbox host opens, so `_walk_menu_actions` keeps enumerating it for Customize Toolbar. Three things deliberately NOT built because the foundation is target-shaped: the sandbox tree renders unmarked (drift markers compare against a deployed-to-TARGET reference and borrowing them would be a lie), §18.6's completion index is not repointed, and no second reachability probe was invented. The sandbox tree is browse-only, suppressed at build time rather than by leaving a signal dangling. `RENAMED_ID_ALIASES` was created here, early, because §18.7 asked for a row in a table FQ-021 had not built yet. | `tests/ui/test_center_stage.py`, `test_ddl_explorer_sandbox.py`, `test_ddl_explorer_wiring.py`, `test_mainwindow_surface.py`, `test_toolbar_registry.py` | (not recorded) | PASS — 4768 passed, 45 skipped (baseline 4743, +25 tests), as stated by the commit body | `aa7a0e1` |
| 2026-08-08 *(reconstructed)* | **FQ-024 — one `Edit DDL` on the DDL Explorer right-click.** `Edit <obj>…` and `Check Out for Versioning` collapse into a single entry; what differs is what the opened tab can do, driven by project state, not which of two labels the user guessed at. **Fixes a data-divergence bug nothing covered:** checkout keyed the tab on the resolved `ddl/*.sql` path while Edit looked it up by `ref.key`, so checkout-then-Edit opened TWO tabs for one object, identically titled, with two different save destinations — and whichever you saved last did not win, they just diverged. The key is now `ref.key` always, **proven by temporarily restoring the old keying and watching both new regression tests fail** with two distinct panel objects. Two latent bugs fall out of the one-key rule: a checked-out tab's title never updated after save, and `_run_apply_to_sandbox_async` returned None for a checked-out tab so an async sandbox-apply outcome had nowhere to land. `ddl_object_close_requested` is honest again (`Signal(tuple)`, not a `str`). The FQ-002 creation path is explicitly routed to the live branch. | `tests/ui/test_ddl_buffer_panel.py`, `test_ddl_creation_wiring.py`, `test_ddl_editor_panel.py`, `test_ddl_object_editor_wiring.py`, `test_ddl_project_wiring.py`, `test_ddl_schema_index_wiring.py`, `test_mainwindow_surface.py`, `test_sandbox_check_console_wiring.py` | (not recorded) | PASS — 4743 passed, 45 skipped (baseline 4737; the commit notes its earlier 4736 figure was one low), as stated by the commit body | `7b8ec7b` |
| 2026-08-08 *(reconstructed)* | **BUG-037 — say on the tab why Raw XML refuses keystrokes.** Entering Caption Mode made the Raw XML editor read-only but left its tab titled plainly "Raw XML", so the editor silently swallowed every keystroke with nothing nearby to explain it; the only cue was `_mode_label` at the far bottom of the window. Root cause: the flag and the title were two views of one fact set in different places. Both now go through `_set_raw_xml_read_only(reason)`, which takes the REASON rather than a bare `True`, because Raw XML can be locked by more than one mode and a user who cannot type needs to know which one to leave. The plain title is a module constant shared with the `addTab` site so the restore path and construction cannot drift. (Generalized into a set of reasons by FQ-021a, logged above.) | `tests/ui/test_center_stage.py` — the two existing transition tests extended rather than duplicated, plus a both-directions guard that a future read-only mode cannot set one of the pair without the other | PASS — 46 passed (`tests/ui/test_center_stage.py`), as stated by the commit body | (not recorded) | `0f84e62` |
| 2026-08-09 | **BUG-038 + BUG-039 + BUG-040 — the three-bugfix batch on `dev_review_01`** (spec: `CONSOLIDATED_SPEC.md` §7, §26, §18.5 D3a + carve-out 2, and the two 2026-08-09 Supersession Ledger rows in §28). **BUG-038:** `CheckRequest` gains `relation_schema`/`relation_table` (option (b) — `table` stays trigger-only, so `working_set_ref`/`trigger_drop_target` are untouched), `regclass_text` honours them for `kind == "function"`, and `MainWindow._trigger_relation_for` reads `RETURNS trigger` off the BUFFER and the relation from `SchemaIndex.trigger_for_function`, merged into both `CheckRequest.from_ref` call sites — so checking a trigger FUNCTION tab binds `relid` instead of erroring *"missing trigger relation"*. **BUG-039:** the `Parsing` menu becomes TAB-KIND-GATED with FOUR members built once and only `setVisible`-toggled; the two check gestures MOVE off Database onto Parsing (Parsing only — no twins), visibility composes as `ddl_tab_active AND _sandbox_check_present()` in the shared `_refresh_parsing_menu_affordances()` called from BOTH refreshers, the XML pair is hidden by tab kind alone, the `Validate Project` default-toolbar-button blink is owner-ACCEPTED, and two `RENAMED_ID_ALIASES` rows re-home the moved ids. **BUG-040:** the sandbox session AUTO-CONNECTS on project bind (best-effort, async, never fatal, never modal — reversing FQ-023's "don't open lazily"), and `Database ▸ Open/Close Sandbox Session` are DELETED outright (actions, attributes, visibility block; no alias row — a deletion is not a move), leaving the refusal dialog's inline `Open` button as the only manual acquisition; `DESTINATION_UNAVAILABLE_REASONS[DEST_SANDBOX]` is reworded to stop naming the deleted entry. | Updated 20 tests encoding the old contracts and added new coverage across **9 files, net +23 tests**. `tests/ui/test_sandbox_check_console_wiring.py` (the authoritative home; 57→70): `_project_window` gains a `reachable` flag and a `_RecordingOpener` (so the sessionless state is now an auto-open that FAILED, and opens can be COUNTED) plus an `_open_ddl_tab` helper; renamed `…binds_the_controller_without_connecting` → `…binds_the_controller_and_opens_the_session` (one open, no lifecycle attributes left) and `…names_open_sandbox_session_and_runs_nothing` → `…states_the_missing_session_and_runs_nothing`; new: the Database menu offers no session-lifecycle entries, no session opens projectless, a project with no sandbox dials nothing, an unreachable sandbox degrades to the no-session state with its reason in the Audit panel, recovery through the refusal's `Open`, the check gestures hidden off a DDL tab / revealed on one (with the XML pair hidden), the Parsing entries ARE the check handlers, and three BUG-038 end-to-end pins (check, probe and apply all carry the relation into the request). `tests/ui/test_menus.py` (+2, `test_parsing_menu_contents` rewritten to the four-member list): XML face on a non-DDL tab, DDL tab hides the XML pair and leaves the menu legitimately EMPTY with no sandbox, and the flip is not one-way. `tests/ui/test_toolbar.py` (+2): a toolbar saved before BUG-039 follows both check gestures to `parsing.*` (resolving even though the actions are hidden), and a pinned `database.open/close-sandbox-session` is DROPPED, not left dead. `tests/ui/test_sandbox_setup_wiring.py` (3 updated): the console refusal names only entries that EXIST (`Sandbox Setup…`, asserted present) and not the deleted one; the Setup-dialog path ends with exactly ONE provisioned session and ZERO host opens (no double-open). `tests/ui/test_new_project_sandbox_provisioning.py` (3 updated + `_refuse_uncreated_databases` helper pinning that the create-path auto-open dials `database=""` and can only fail, with provisioning supplying the live session). Plus `tests/ui/test_mainwindow_surface.py` (the two deleted attributes removed from the literal), `tests/ui/test_ddl_object_editor.py` and `tests/ui/test_deployment_menu.py` (the reworded refusal, asserted both ways), `tests/ui/test_sandbox_controller.py` (`set_project` is a MECHANISM that still opens nothing — the policy moved to the host). | PASS — 341 passed (`test_sandbox_check_console_wiring.py` 70, `test_menus.py`, `test_mainwindow_surface.py`, `test_sandbox_setup_wiring.py` 15, `test_new_project_sandbox_provisioning.py`, `test_ddl_object_editor.py`, `test_deployment_menu.py`, `test_toolbar.py`, `test_toolbar_registry.py`, `test_sandbox_controller.py`, `tests/db/test_ddl_check.py`) | PASS — 4867 passed, 45 skipped in 36s (`-n 10`, offscreen; five green runs — baseline before the batch was 4844/45). One PRE-EXISTING, unrelated xdist-distribution flake observed twice en route: `tests/ui/test_theme.py::test_apply_theme_light_keeps_hand_rolled_palette_under_the_qss` (app-global `QPalette` `Link` colour leaking between workers) — reproduced identically with this batch `git stash`ed away, and no file in this batch touches theming. | `(uncommitted)` — working tree on `dev_review_01` at `7e35f03` |
| 2026-08-08 | **FQ-020 — the `Deployment` menu on the Editor menu bar, and the deletion of `File ▸ Save` / `Save As…`** (shipped via merge `04c3591`, merging worktree commit `7a683e6`). The Editor bar's fifth menu, nine actions built ONCE at startup and only `setVisible`-toggled by active-tab kind (Raw XML / DDL object / Edit XSD / PHP file, and an explicit **none** group for the six tab kinds with no destination). `File ▸ Save`, `Save As…`, `Ctrl+S`, `Ctrl+Shift+S` and the four-way `_save_active_tab` router are deleted; `save`/`file.save` retired from `toolbar_registry`; `tools.compare-merge-two-files`, `file.deploy-pgtp` and `file.revert` re-homed via `RENAMED_ID_ALIASES`. Existing coverage (536-line `test_deployment_menu.py` plus edits to seven sibling files) was already thorough; this dispatch **extended** it at the negative/seam gaps. | Extended 3 files, +14 tests: `tests/ui/test_deployment_menu.py` (+11 — wiring of the four save entries one by one; *no entry other than the two `.pgtp` saves can write the `.pgtp`*, triggering all seven others including the hidden ones; the `.pgtp` writers absent on a PHP tab; `save_active_tab()` off-tab writes nothing; `Run on sandbox`/`Run on quality` off-tab report, quality without resolving a target or prompting; the group returns to Raw XML when the object tab is CLOSED; the group follows the tab both ways; no shown entry is merely greyed out), `tests/ui/test_toolbar.py` (+2 — a toolbar saved before FQ-020 follows the three moved commands while `file.save` is dropped, and the buttons ARE the live menu actions; no live menu command answers to `file.save`/`file.save-as`/`tools.compare-merge-two-files`/`file.deploy-pgtp`/`file.revert`), `tests/ui/test_menus.py` (+1 — no `QShortcut` anywhere in the window's widget tree claims Ctrl+S or Ctrl+Shift+S, complementing the existing QAction sweep). | PASS — 168 passed (`test_deployment_menu.py` 33, `test_menus.py`, `test_toolbar.py` 50, `test_toolbar_registry.py`) | PASS — 4843 passed, 45 skipped in 39s (`-n 10`, offscreen) | `04c3591` (feature); test extensions uncommitted at `c8e9318` |
| 2026-08-07 | **BUG-036 — default opening sizes for four windows** (retrospective verification: shipped on `dev_review_01` overnight with tests but with no `TEST_LOG.md` entry). Project Settings 560x760, Sandbox Setup 660x1000, Project Status 720x440, New Function/Procedure 560x200 — all via `resize()`, never `setFixedSize`, each paired with resizability assertions so a fixed-size regression fails rather than passes. `main_window.py` was deliberately not touched (other lanes held it), so Project Status sizes in its own constructor and is verified to survive `setWindowFlag(Window, True)`. **Brief-vs-code discrepancy:** the dispatch brief bundled the `Open Recent` / double-click-to-open removals into BUG-036; they are **FQ-010's** (commit `02e47e0`), and BUG-036 explicitly SKIPPED the file pickers (native `QFileDialog` statics, OS-owned geometry) and the Caption Filter dialog (FQ-017 was deleting it). | Verified existing, none added: `tests/ui/test_project_settings_dialog.py`, `tests/ui/test_sandbox_setup_dialog.py`, `tests/ui/test_project_status_panel.py`, `tests/ui/test_new_routine_dialog.py` (168 tests across the four; the sizes are asserted three ways — exact `QSize`, no visible child below its own `minimumSizeHint`, no wrapping label needing more height than it has). Judged adequate; nothing added. | PASS — 168 passed across the four BUG-036 files (`-n 10`, 1.7s, five consecutive runs). **One caveat recorded honestly:** `test_new_routine_dialog.py::test_a_validation_message_does_not_push_the_buttons_out_of_view` is ORDER-DEPENDENT — it fails deterministically (`assert 55 <= 52` on `error_label.heightForWidth(538)`) when run under `--dist load` interleaved with the other 17 files of this batch, and passes in all 761 when the same set runs under `--dist loadfile`. Left untouched (not weakened, not skipped) and reported to the caller as a fragile font-metric assertion over app-global QSS state | PASS — **4736 passed, 45 skipped** (`QT_QPA_PLATFORM=offscreen venv/bin/python -m pytest -q -n 10`, ~31s), observed **three consecutive clean runs** after my additions; the floor before them was 4726 passed / 45 skipped (so all 10 added tests are accounted for). Two intermittent, PRE-EXISTING flakes were seen during this dispatch and are recorded rather than hidden — neither belongs to this batch and neither reproduced in the three gate runs: (a) a teardown ERROR in `tests/ui/test_ddl_project_wiring.py` (a different test each time — `…creates_the_folder_and_settings_file`, `test_compare_page_with_dialog_defaults_to_the_project_folder`, `test_compare_merge_two_files_target_dialog_also_defaults_to_the_project_folder`), always `RuntimeError: Signal source has been deleted` from `ddl_project_controller.py:538`'s async capability probe landing after the window is gone (~1 in 8 runs); (b) `tests/ui/test_theme.py::test_apply_theme_light_keeps_hand_rolled_palette_under_the_qss` failed once in 6 repeats of `tests/ui`, an app-global QPalette/QSS leak between xdist-interleaved tests | `b799f21` (tests for it: pre-existing; my dispatch added none) |
| 2026-08-07 | **FQ-010 — the startup launch modal, and the removal of `Open Recent` + double-click-to-open** (retrospective verification). `ui/launcher_dialog.py`: four groups (open a `.pgtp` / New-Open Project / open other files / Maintenance mode = XSD + the four §20 re_phpgen entries, with §19 vendor generation excluded), each entry dispatching the **existing** menu QAction; shown from `main.py` **after** `window.show()` behind an injectable `launcher=` seam and never from `MainWindow.__init__` (49 test files construct one); suppression persists as the `launcherSuppressed` QSettings bool; Escape/close lands in the empty app and never quits; `File ▸ Show Launcher…` forces it past the suppression flag so the checkbox is not a one-way door. `recentFiles` and its whole store are gone, as is the GUI's `argv` open branch — while `args.file` SURVIVES as `--mcp`'s default project. | Verified existing: `tests/ui/test_launcher_dialog.py` (29) and `tests/test_main.py` (19, including the `--mcp`-returns-before-any-Qt-import invariant asserted against `main.py`'s SOURCE ORDER). **Gap I filled — the store deletion had no guard:** the menu (`test_menus.py`) and the pinnable command (`test_toolbar.py`) were asserted gone, but **nothing asserted the QSettings store is never written**, so a re-added `remember_recent_file()` call would silently start recording file history again with every test still green. Added **3 tests** to `tests/ui/test_save_project.py` driving the store's two and only two former writers: `test_save_as_records_nothing_recent_shaped_in_settings`, `test_open_file_records_nothing_recent_shaped_in_settings` (both against a real INI-backed `QSettings` injected into `MainWindow`, asserting the save/open really happened AND that no `*recent*` key exists) and `test_the_document_controller_has_no_recent_files_api_left` (the reader, the writer, the menu rebuilder and both key constants are absent, not inert). | PASS — 61 passed (`test_launcher_dialog.py` 29, `test_main.py` 19, `test_save_project.py` 13 — 10 before my 3) | PASS — **4736 passed, 45 skipped** (`QT_QPA_PLATFORM=offscreen venv/bin/python -m pytest -q -n 10`, ~31s), observed **three consecutive clean runs** after my additions; the floor before them was 4726 passed / 45 skipped (so all 10 added tests are accounted for). Two intermittent, PRE-EXISTING flakes were seen during this dispatch and are recorded rather than hidden — neither belongs to this batch and neither reproduced in the three gate runs: (a) a teardown ERROR in `tests/ui/test_ddl_project_wiring.py` (a different test each time — `…creates_the_folder_and_settings_file`, `test_compare_page_with_dialog_defaults_to_the_project_folder`, `test_compare_merge_two_files_target_dialog_also_defaults_to_the_project_folder`), always `RuntimeError: Signal source has been deleted` from `ddl_project_controller.py:538`'s async capability probe landing after the window is gone (~1 in 8 runs); (b) `tests/ui/test_theme.py::test_apply_theme_light_keeps_hand_rolled_palette_under_the_qss` failed once in 6 repeats of `tests/ui`, an app-global QPalette/QSS leak between xdist-interleaved tests | `02e47e0` (+ `aebb43c` docs); my 3 added tests are **(uncommitted)** |
| 2026-08-07 | **FQ-016 — the second, fixed Editor menu bar above the central pane, the dissolution of the Edit menu, and the permanently visible Find/Replace bars** (retrospective verification). The bar holds History / Select / Parsing / Bookmarks and **hides wholesale on the Caption Management and Manual tabs** while its QActions stay alive and pinnable; the window's Edit menu is DISSOLVED, not emptied (History…/Undo/Redo → History, the five Find/Replace entries → the permanent bar, `Auto Parse XML` → Parsing, the two selection commands → FQ-015's Select, and Cut/Copy/Paste/Delete/Preferences… deleted as stubs); the `FindReplaceBar` has no `self.hide()` and no `show_find`/`show_replace` mode, so `Ctrl+F`/`Ctrl+R` merely FOCUS a field (installed per editor tab, never window-level — a window-level `Ctrl+F` would be *ambiguous* against FQ-017's caption pair and neither would fire); Escape returns focus to the editor instead of hiding; `F3` is a window-level, menu-less action; `Ctrl+Shift+F` (Find All) and `Ctrl+Alt+Return` (Replace All) are DELETED while both commands survive as bar buttons. **Brief-vs-code discrepancy:** the dispatch brief called the always-visible-bars work "FQ-013"; in `docs/FEATURE_QUEUE.md` it is part of **FQ-016**, and FQ-013 is bookmark persistence (see its own row). | Verified existing: `tests/ui/test_menus.py` (49 — bar placement/contents/order, the hide on caption+manual, the dissolved Edit menu, the deleted chords, `F3` as a window action driven as a real key press), `tests/ui/test_main_window.py` (63), `tests/ui/test_find_replace_bar.py` (22 before my additions). **Gap I filled — the "every editor site" claim was only sampled:** `test_menus.py::test_every_editor_bar_is_permanently_visible_and_expanded` says "all six construction sites" in its docstring but asserts on **three** (Raw XML, Edit XSD, the read-only DDL Explorer panel), omitting exactly the three DYNAMIC tabs built far from `CenterStage` — a DDL object editor tab, a PHP file tab and an FQ-006 draft tab — which are the likeliest to be added later with a hidden bar. Added **5 tests** to `tests/ui/test_find_replace_bar.py`, each looping over all six real construction sites built from the real classes: `test_all_six_bar_sites_ship_a_visible_expanded_bar`, `test_no_bar_site_retains_a_hideable_mode`, `test_every_bar_site_owns_the_ctrl_f_and_ctrl_r_focus_shortcuts` (present AND `WidgetWithChildrenShortcut`-scoped, so a site that forgot `install_focus_shortcuts` fails), `test_escape_returns_focus_to_the_editor_at_every_bar_site` and `test_no_bar_site_has_a_close_button` (the button set is exactly Find Next / Find All / Replace / Replace All — a resurrected `Close` is a hide path by another name). Note for future readers: the helper carries each site's TOP-LEVEL widget in its tuple because `qtbot.addWidget` holds only a weak reference — returning a child alone let the parent be collected and every Qt object under it deleted mid-test (this cost me one red iteration). | PASS — 139 passed (`test_menus.py` 49, `test_main_window.py` 63, `test_find_replace_bar.py` 27 — 22 before my 5) | PASS — **4736 passed, 45 skipped** (`QT_QPA_PLATFORM=offscreen venv/bin/python -m pytest -q -n 10`, ~31s), observed **three consecutive clean runs** after my additions; the floor before them was 4726 passed / 45 skipped (so all 10 added tests are accounted for). Two intermittent, PRE-EXISTING flakes were seen during this dispatch and are recorded rather than hidden — neither belongs to this batch and neither reproduced in the three gate runs: (a) a teardown ERROR in `tests/ui/test_ddl_project_wiring.py` (a different test each time — `…creates_the_folder_and_settings_file`, `test_compare_page_with_dialog_defaults_to_the_project_folder`, `test_compare_merge_two_files_target_dialog_also_defaults_to_the_project_folder`), always `RuntimeError: Signal source has been deleted` from `ddl_project_controller.py:538`'s async capability probe landing after the window is gone (~1 in 8 runs); (b) `tests/ui/test_theme.py::test_apply_theme_light_keeps_hand_rolled_palette_under_the_qss` failed once in 6 repeats of `tests/ui`, an app-global QPalette/QSS leak between xdist-interleaved tests | `c327c9d` (+ `9146524`); my 5 added tests are **(uncommitted)** |
| 2026-08-07 | **FQ-015 — the `Select` menu on the Editor menu bar** (retrospective verification): `Select All` (Ctrl+A), `Select Enclosing Block` (Ctrl+Shift+B), `Select Parent Block` (Ctrl+Shift+A), sitting between History and Parsing, dispatching to the ACTIVE tab's editor and never to Raw XML behind the user's back (Select Parent Block is absent on code-editor tabs and a no-op there rather than a jump). Includes the real bug fix in `ui/xml_editor.py::_is_text_modifying_key`: a Ctrl/Meta chord is now classified as a COMMAND before the printable-text test, so a **read-only** editor stops swallowing Ctrl+A (and every other Ctrl+letter) while flashing the "this editor is read-only" hint — Ctrl+V is checked first and keeps its hint, because paste really is an edit attempt. | Verified existing, none added: `tests/ui/test_select_menu.py` (23) and `tests/ui/test_xml_editor.py` (the regression is directly covered by `test_readonly_ctrl_a_selects_all_instead_of_emitting_the_hint`, which asserts no signal AND that the whole document really is selected AND that the buffer is unchanged; `test_select_menu.py` additionally covers it end-to-end twice — `test_ctrl_a_really_selects_all_in_a_read_only_editor_of_either_family` and `test_ctrl_a_selects_all_in_raw_xml_while_caption_mode_holds_it_read_only`, which is the exact user-visible symptom). The brief asked me to add a regression test if one was missing; it was not. | PASS — 136 passed (`test_select_menu.py` 23, `test_xml_editor.py` 113) | PASS — **4736 passed, 45 skipped** (`QT_QPA_PLATFORM=offscreen venv/bin/python -m pytest -q -n 10`, ~31s), observed **three consecutive clean runs** after my additions; the floor before them was 4726 passed / 45 skipped (so all 10 added tests are accounted for). Two intermittent, PRE-EXISTING flakes were seen during this dispatch and are recorded rather than hidden — neither belongs to this batch and neither reproduced in the three gate runs: (a) a teardown ERROR in `tests/ui/test_ddl_project_wiring.py` (a different test each time — `…creates_the_folder_and_settings_file`, `test_compare_page_with_dialog_defaults_to_the_project_folder`, `test_compare_merge_two_files_target_dialog_also_defaults_to_the_project_folder`), always `RuntimeError: Signal source has been deleted` from `ddl_project_controller.py:538`'s async capability probe landing after the window is gone (~1 in 8 runs); (b) `tests/ui/test_theme.py::test_apply_theme_light_keeps_hand_rolled_palette_under_the_qss` failed once in 6 repeats of `tests/ui`, an app-global QPalette/QSS leak between xdist-interleaved tests | `9146524` |
| 2026-08-07 | **FQ-017 — the Caption Filter modal is DELETED and the caption Find/Replace bar is permanent, with a scope dropdown** (retrospective verification). `ui/caption_find_replace_dialog.py` is gone as a module (importing it raises), so are its host-side attributes and the `Tools ▸ Caption Filter…` entry and the two mode-gated `Ctrl+F`/`Ctrl+R` `QShortcut`s (which FQ-016's focus ruling needed gone). The bar is permanent — find/mode/case/Filter/Clear filter over replace/**scope**/Replace All/status/error, `Close` removed — with the scope dropdown offering "in filtered results" (default) / "in all project". `Unify`'s `_confirm_unify_scope` and the active-filter banner are deliberately untouched (retiring the banner would re-create BUG-020, since it also describes row-predicate filters a text field cannot express). **Brief-vs-code discrepancy:** the brief called this "FQ-014"; FQ-014 is `List All Bookmarks` (own row). | Verified existing, none added: `tests/ui/test_caption_management_panel.py` (143). The deletions are guarded (`test_main_window.py` asserts the module import raises, the host attributes are absent and the menu entry is None; `test_menus.py` covers the Tools menu without it; `test_panel_owns_panel_scoped_focus_shortcuts_for_ctrl_f_and_ctrl_r` covers the surviving pair) and so is the semantic decision the author corrected mid-flight — `test_a_hand_edit_after_replace_all_is_never_silently_reverted` and `test_touching_a_field_after_replace_all_re_arms_a_reversible_preview` pin the `is_active()` fix. Both scopes, the default, and "choosing the project scope does not drive the live preview" are covered. Judged adequate. | PASS — 206 passed (`test_caption_management_panel.py` 143, `test_main_window.py` 63) | PASS — **4736 passed, 45 skipped** (`QT_QPA_PLATFORM=offscreen venv/bin/python -m pytest -q -n 10`, ~31s), observed **three consecutive clean runs** after my additions; the floor before them was 4726 passed / 45 skipped (so all 10 added tests are accounted for). Two intermittent, PRE-EXISTING flakes were seen during this dispatch and are recorded rather than hidden — neither belongs to this batch and neither reproduced in the three gate runs: (a) a teardown ERROR in `tests/ui/test_ddl_project_wiring.py` (a different test each time — `…creates_the_folder_and_settings_file`, `test_compare_page_with_dialog_defaults_to_the_project_folder`, `test_compare_merge_two_files_target_dialog_also_defaults_to_the_project_folder`), always `RuntimeError: Signal source has been deleted` from `ddl_project_controller.py:538`'s async capability probe landing after the window is gone (~1 in 8 runs); (b) `tests/ui/test_theme.py::test_apply_theme_light_keeps_hand_rolled_palette_under_the_qss` failed once in 6 repeats of `tests/ui`, an app-global QPalette/QSS leak between xdist-interleaved tests | `02e47e0` |
| 2026-08-07 | **FQ-013 — bookmark persistence, project mode only** (retrospective verification). `db/bookmark_store.py` writes `<project>/.ddlproject/bookmarks.json` as a SIBLING of `settings.json` (never a `ProjectSettings` key, so a corrupt bookmark file can never cost the user their deploy-manifest settings), keyed by project-relative POSIX paths with both sides resolved; load never rewrites, an empty set removes its key, every malformed input degrades to "no bookmarks". The UI wiring gates on the CAPABILITY fact `DdlProjectController.folder`, keeps the gutter mixin ignorant of projects (it publishes `(editor, reason)` through a module-level observer registry and the host decides what it means), saves on a **debounce** plus flushes on project transition and on `closeEvent`, and restores at the one moment the set is wiped — a document load. **Brief-vs-code discrepancy (two):** the brief called this "FQ-017" and credited it "28 tests" — the store's file holds 28 collected tests but `FEATURE_QUEUE.md` says 29; and **FQ-013's queue Status line is STALE**, still reading `PARTIALLY PROCESSED (adf9bfb) … STILL TO DO: the gutter hookup and the project-open gate`, although `9146524` wired exactly that. | Verified existing: `tests/db/test_bookmark_store.py` (28, the pure store), `tests/ui/test_editor_gutter_observers.py` (8, the notification registry including GC'd/deleted observers and "a real bug in an observer is not swallowed"), `tests/ui/test_bookmark_persistence.py` (15 before my additions — the projectless path writes nothing at all, the debounce, restore on reload and on app restart, clear-all persisted as an empty set, out-of-range lines surviving a shortened document, the flush on project transition and on window close). **Gap I filled — the session-only families were half-covered:** `main_window.py::_bookmark_file_path` documents **four** families that structurally cannot persist, and only **two** were tested (the read-only DDL Explorer buffer and an FQ-006 draft tab). Added **2 tests** to `tests/ui/test_bookmark_persistence.py`: `test_the_xsd_editors_stay_session_only` (Edit XSD / Edit AutoXSD — their schema files live in the app-level schema storage directory, under no project, so `relative_key` could not key them; asserted **with a project open**, the only state in which it could go wrong, and asserted to keep the session-only status quo of being wiped by a document load) and `test_an_editor_the_host_does_not_own_has_no_bookmark_key` (the general rule the unreachable `Edit code…` modal relies on — an unknown editor keys to None and is never written — tested without entering the modal). The XSD one needed the `window._xsd_ui.confirm_close` seam: `setPlainText` dirties the tab and teardown would otherwise reach a real `QMessageBox` (it did, and hit the suite's 60s timeout, on my first iteration). | PASS — 53 passed (`test_bookmark_store.py` 28, `test_editor_gutter_observers.py` 8, `test_bookmark_persistence.py` 17 — 15 before my 2) | PASS — **4736 passed, 45 skipped** (`QT_QPA_PLATFORM=offscreen venv/bin/python -m pytest -q -n 10`, ~31s), observed **three consecutive clean runs** after my additions; the floor before them was 4726 passed / 45 skipped (so all 10 added tests are accounted for). Two intermittent, PRE-EXISTING flakes were seen during this dispatch and are recorded rather than hidden — neither belongs to this batch and neither reproduced in the three gate runs: (a) a teardown ERROR in `tests/ui/test_ddl_project_wiring.py` (a different test each time — `…creates_the_folder_and_settings_file`, `test_compare_page_with_dialog_defaults_to_the_project_folder`, `test_compare_merge_two_files_target_dialog_also_defaults_to_the_project_folder`), always `RuntimeError: Signal source has been deleted` from `ddl_project_controller.py:538`'s async capability probe landing after the window is gone (~1 in 8 runs); (b) `tests/ui/test_theme.py::test_apply_theme_light_keeps_hand_rolled_palette_under_the_qss` failed once in 6 repeats of `tests/ui`, an app-global QPalette/QSS leak between xdist-interleaved tests | `adf9bfb` (store) + `9146524` (wiring); my 2 added tests are **(uncommitted)** |
| 2026-08-07 | **FQ-014 — `List All Bookmarks`: the active editor's bookmarks as clickable Audit/Problems rows** (retrospective verification). Reuses Find All's row grammar and its two-role payload verbatim, scopes strictly to the ACTIVE editor, reveals a hidden Audit dock, reports its count in the status bar, replaces its own rows on a repeat listing while leaving other prefixes alone, sweeps stale rows on a document load, and deliberately carries **no shortcut**. Rows for identity-less editors (the DDL Explorer buffer, a draft tab) are role-less and click to nothing rather than mis-navigating. **Brief-vs-code discrepancy (two):** the brief folded this into its "FQ-014 — Caption Filter dialog deleted" item (that is FQ-017), and **FQ-014's queue Status line is STALE, still reading `QUEUED`** although `9146524` implemented it with tests. | Verified existing, none added: `tests/ui/test_list_all_bookmarks.py` (16), which covers each editor family's row payload (Raw XML, the `"xsd"` discriminator, a DDL object tab's ref-key tuple, a PHP tab's target + tab key, role-less rows for the two identity-less editors), the empty case, click routing including the unroutable-row no-op, and the negative `test_the_command_has_no_shortcut`. It drives the REAL menu action rather than the method, so the wiring is covered. Judged adequate. | PASS — 16 passed (`test_list_all_bookmarks.py`) | PASS — **4736 passed, 45 skipped** (`QT_QPA_PLATFORM=offscreen venv/bin/python -m pytest -q -n 10`, ~31s), observed **three consecutive clean runs** after my additions; the floor before them was 4726 passed / 45 skipped (so all 10 added tests are accounted for). Two intermittent, PRE-EXISTING flakes were seen during this dispatch and are recorded rather than hidden — neither belongs to this batch and neither reproduced in the three gate runs: (a) a teardown ERROR in `tests/ui/test_ddl_project_wiring.py` (a different test each time — `…creates_the_folder_and_settings_file`, `test_compare_page_with_dialog_defaults_to_the_project_folder`, `test_compare_merge_two_files_target_dialog_also_defaults_to_the_project_folder`), always `RuntimeError: Signal source has been deleted` from `ddl_project_controller.py:538`'s async capability probe landing after the window is gone (~1 in 8 runs); (b) `tests/ui/test_theme.py::test_apply_theme_light_keeps_hand_rolled_palette_under_the_qss` failed once in 6 repeats of `tests/ui`, an app-global QPalette/QSS leak between xdist-interleaved tests | `9146524` |
| 2026-08-06 | **FQ-007 — the New Project sandbox step CREATES and provisions the sandbox database (auto-named) instead of asking for an existing one.** The "Database:" field is gone from `NewProjectDialog` (replaced by a caveat label explaining the app creates the database itself); `sandbox_admin_params()` points the typed creds at `MAINTENANCE_DATABASE` and `sandbox_database_names()` generates distinct `pgtp_sandbox_<stem>_<8 hex>` candidates; `SandboxController.provision_new_database` creates the **first free** candidate (duplicate-database SQLSTATE `42P04` → next candidate, never a reuse and never a drop, so `confirm_destructive` is deliberately not consulted), provisions from the target's `snapshot_for_baseline` (or an empty `BaselineSnapshot` with `_NO_TARGET_BASELINE_NOTE` when there is no target yet), installs `plpgsql_check` behind the pure `install_gate` and **degrades** (ok=True + the gate's own sentence) when that is refused or fails; `MainWindow._on_new_project_sandbox_provisioned` records the created name into `ProjectSettings.sandbox.database` **on success only** and re-probes capability status either way. Gaps I filled on top of the author's own tests: the generated name round-trips through the **real** `create_sandbox_database` (validated-not-sanitized regex + `is_app_owned` on the stamped marker) for 5 pathological project names, stem-budget truncation leaving no trailing underscore, punctuation-only/nameless fallback stem, over-long pinned suffix truncation, `DEFAULT_NAME_ATTEMPTS`/`count<=0` candidate-list contract, two same-named projects never colliding, collision detection via psycopg `diag.sqlstate` and the negative cases (`relation … already exists`, wrong SQLSTATE) that must **not** trigger a retry loop, refusal with no sandbox configured, post-install re-probe being what the result/`capability_status()` carry, `database_name` empty for every other operation, the maintenance-database constant being one shared object with `sandbox_setup_dialog.DEFAULT_MAINTENANCE_DATABASE`, and at window level: full admin-connection field fidelity, every-candidate-taken recording nothing while surfacing "no existing database was touched", a refused extension install still recording the created database (tier-2 is a working project), the recorded name passing `is_app_owned`, and provisioning touching **only** the database it created. No implementation bugs found. | `tests/ui/test_sandbox_controller.py` (extended: +20, new section 6b), `tests/ui/test_new_project_sandbox_provisioning.py` (extended: +7), `tests/ui/test_sandbox_setup_dialog.py` (extended: +4) | PASS — 150 passed (`test_sandbox_controller.py` 86, `test_new_project_sandbox_provisioning.py` 23, `test_sandbox_setup_dialog.py` 41) | PASS — 4478 passed, 45 skipped (`-n 10`, 27s; floor before this dispatch was 4447 passed / 45 skipped) | (uncommitted) |
| 2026-08-06 | **Backlog row 13/13 — refactor waves (pure motion): completion-popup mixin, `ui/modals.py`, UiShell + guards + ToolbarController, GenerationController + CoherenceController.** Logged as a refactor row recording the gate, not as features: no user-visible behavior was added by any of the four. `f3c00f6` collapsed three byte-identical copies of the completion-popup wiring (XmlEditor, SqlConsolePanel, DdlObjectEditorPanel) into a mixin with one pluggable `_completion_editor()` hook — deliberately adds no tests, since a pure extraction is proved by the existing suite. `8ee6efe` routed all ~60 modal call sites in `main_window.py` through `ui/modals.py` by **module-attribute access**, which is the load-bearing form: 203 test patch targets keep working for both whole-object and method-level patches, where a `from … import QMessageBox` would rebind a local name and silently patch nothing while still passing. `9ecc68f` added `UiShell` (frozen dataclass of **bound** callables, `window` usable only as a Qt parent), `test_collaborator_boundaries.py` (glob-discovered per-collaborator rules, comment/string-stripped so a module's own docstring explaining the rule does not fail it), `test_mainwindow_surface.py` (313-name pinned host surface, `_LEGACY_DELEGATES == {}`), and extracted ToolbarController. `6f009ad` extracted GenerationController and CoherenceController (`main_window.py` 4876 → 3803 lines over the four waves). **One genuine gap found and closed** — the `ui/modals.py` invariant had no test at all: `test_modals.py` pinned only the re-export surface, so nothing prevented `main_window.py` or a freshly extracted collaborator from re-binding a modal name locally, which is exactly the failure mode 8ee6efe exists to prevent and the one that passes silently while testing nothing (and can let an offscreen run reach a real modal). Added a tokenize-based guard over `main_window.py` + every `*_controller.py`, glob-discovered so later waves are covered without editing the test, plus a companion asserting the glob is non-vacuous. The guard was verified to actually fire by running its logic against `ui/about.py`, one of the seven leaf dialogs deliberately outside the rule (2 offenders found), so it is not a tautology. Note recorded: the four extracted controllers have **no test files of their own** (`test_xsd_controller.py`, `test_toolbar_controller.py`, `test_generation_controller.py`, `test_coherence_controller.py` do not exist) — their coverage is the renamed refs inside the pre-existing feature files, which is consistent with "pure motion" but means a controller's seams have no home of their own to grow into | `tests/ui/test_modals.py` (extended 5 → 7: `test_the_host_and_its_collaborators_reach_modal_qt_only_through_modals`, `test_the_guard_covers_main_window_and_the_extracted_controllers`) | PASS — 7 passed (`tests/ui/test_modals.py`); PASS — 485 passed across the 9 files this backlog pass was allowed to touch (`tests/db/test_ddl_check.py tests/db/test_apply.py tests/db/test_sandbox.py tests/sql/test_statements.py tests/mcp/test_cli.py tests/ui/test_modals.py tests/ui/test_sandbox_setup_dialog.py tests/ui/test_php_tab_controller.py tests/ui/test_lint_controller.py`, 3.59s) | PASS — 4328 passed, 45 skipped (`QT_QPA_PLATFORM=offscreen venv/bin/python -m pytest -q -n 10`, 26.51s). **Retrospective gate, and the run history matters:** this whole batch of 13 rows closes an accumulated logging debt after the fact, so the gate was observed at the working tree as it stood during the backlog pass — HEAD `6f009ad` **plus two in-flight refactor lanes' uncommitted work** (FindController/DiffMergeController extraction, `DbCheckPanel`/`TableReferencesPanel` removal) — never at each row's own commit. Three runs were taken: 4285 passed / 45 skipped at the start; then, mid-write, a transient **620 failed / 3612 passed / 45 skipped / 72 errors**, every failure a MainWindow-constructing test dying on the half-extracted `_show_find_bar` (the lanes', not this batch's — the 9 allow-listed files were green throughout, and the red vanished once the lanes' own test rewrites landed); then 4328/45 green, recorded here | f3c00f6, 8ee6efe, 9ecc68f, 6f009ad |
| 2026-08-06 | **Backlog row 12/13 — §21 custom PHP editing and §22 PHP lint wired** (`57b5e6f`, committed together with the XsdController extraction because both had to edit `_save_active_tab`/`_on_audit_item_clicked`). Both subsystems were complete, tested and unreachable: nothing but a test could open a PHP tab, and `php_file_close_requested` was emitted and connected to nothing, so a PHP tab's ✕ silently did nothing. Now reachable: File ▸ Open PHP File… (multi-select, one tab per file), drag-and-drop onto the window, the close button with dirty tabs prompting through an injected confirm seam and a failed/cancelled save **aborting** the close, and Tools ▸ Lint Current File / Lint on Save (persisted, applied to already-open tabs on restore) / Locate PHP Linter… writing through `lint/config.py` into §19's existing `generator_config.json` rather than a second store. The three dispatcher branches are the user-visible half — `_save_active_tab`, `_active_find_bar` and `_active_bookmark_editor` had XSD/DDL branches but no PHP one, so Ctrl+S with a PHP tab focused saved the *project* and Ctrl+F yanked focus to Raw XML. `[Lint]` findings append verbatim from `lint/findings.py` and navigate on the `[Check]` channel's conventions (line on `UserRole`, `LINT_AUDIT_TARGET` on `UserRole+1`) plus the **tab key on `UserRole+2`**, which a `[Lint]` row needs and `[Check]` does not because the latter's `UserRole+1` tuple is itself the key; that slot is also Verify XSD's, but the host reads it only on the `"xsd"` branch, so they cannot collide. Drop classification refuses rather than guesses (folder, unreadable file, NUL byte in the first 8 KiB, invalid UTF-8 — the last deliberately, since an `errors="replace"` decode would write replacement characters over the user's file on first Ctrl+S; a `.pgtp` goes to an injected `open_pgtp` seam and is refused outright if unwired). Coverage reviewed clause by clause against the commit and judged complete: all four Tools entries, all three dispatcher branches, every drop-refusal reason, the reopen-short-circuits-before-the-read rule, the abort-on-failed-save path and the `UserRole`/`+1`/`+2` role conventions each have their own test. No gap worth filling in the two allow-listed files and no implementation bug found, so neither was modified. Known gaps recorded by the commit and **not** closed here (matching existing behavior rather than inventing inconsistency): `closeEvent` prompts for the XSD tab but not for dirty DDL-object or PHP tabs, and `QPlainTextEdit` accepts drops itself so a file dropped directly onto an editor still pastes as text | `tests/ui/test_php_tab_controller.py` (43, pre-existing — read and re-verified, not modified), `tests/ui/test_lint_controller.py` (30, pre-existing — read and re-verified, not modified) | PASS — 73 passed (`tests/ui/test_php_tab_controller.py` + `tests/ui/test_lint_controller.py`, 11.56s). Recorded honestly: an earlier run of these two files in this dispatch was red (7 + 6 failures) purely from the in-flight FindController extraction having removed `MainWindow._show_find_bar` while those files' "real window" tests still called it; the lane's own rewrites landed and both files went green untouched by me | PASS — 4328 passed, 45 skipped (`-n 10`, 26.51s) — retrospective gate at the backlog-pass tree state (HEAD `6f009ad` + two in-flight lanes' uncommitted work), not at this commit; see row 13/13 for the full run history | 57b5e6f |
| 2026-08-06 | **Backlog row 11/13 — BUG-032, both facets** (`596f109` fix, `ae37270` queue closure). Facet A: `_on_db_jump_requested` picked its search token with `kind == "table"`, written for the removed `DbCheckPanel`; FQ-003's `CoherencePanel` emits the internal node kind instead (a DB relation is `kind="relation"`) and `113fbfa` re-pointed the connection without touching the slot body, so **every** relation-row double-click searched the XML for `fieldName="pr.v"` instead of `tableName="pr.v"` and always reported "not found in the buffer" — deterministic, not the intermittent degradation the report suggested, and provably not a sandbox reset (nothing in the Check path resets the sandbox, and the sandbox's own messages say "not found in the sandbox"). Fixed at both ends: one module-level `_HOST_KIND = {"relation": "table"}` in `coherence_panel.py` is now the single place internal kinds map to MainWindow's host-facing vocabulary, `_ROW_ROLE`'s carried tuple stores the normalized kind too (latent-bug removal — §17 binds that tuple to `DbCheckPanel`'s shape), and the slot accepts both spellings. A genuine miss now names the token actually searched, which is the one fact that would have made the bug self-diagnosing. Also restores Properties for `kind="reference"` rows (anything under a relation's "Referenced by"), which facet B's unknown-kind degrade had turned from a crash into a silently empty panel: `CoherenceNode` gains `node_kind` from `TableReference.kind` — data `_reference_node` was already carrying and dropping — so those rows render through the existing page/detail/column builders, with facet B's degrade left as the net. Every `emit` in `coherence_panel.py` was audited against its slot; three were wrong and all three are fixed. **Verified in this pass, and it is the interesting part:** the two assertions that *pinned* this bug (they asserted the wrong token) are flipped rather than deleted, and the gap that let it ship is closed by a test that drives the real slot body end to end with **no patching**, asserting the Find bar, the editor selection and the active tab. Reviewed as complete; the relevant test files (`tests/ui/test_coherence_panel.py`, `test_coherence_wiring.py`, `test_db_check_wiring.py`, `tests/db/test_coherence.py`) are all outside this pass's allow-list, so they were read only — see the report's gap list for what I could not add | none added — all four affected files are off-limits in this pass (in-flight refactor lanes are editing `test_db_check_wiring.py` and `test_coherence_wiring.py`); pre-existing coverage read and re-verified only | NOT RUN in this pass by targeted invocation — the four files carrying this fix's tests were outside the allow-list and are being edited concurrently; they were covered by the full-suite gate in the next column | PASS — 4328 passed, 45 skipped (`-n 10`, 26.51s) — retrospective gate at the backlog-pass tree state, not at this commit; see row 13/13 | 596f109, ae37270 |
| 2026-08-06 | **Backlog row 10/13 — §18.5 MainWindow gets a SandboxController; the sandbox half becomes reachable** (`c1d5e25`). Until this commit MainWindow never constructed a `SandboxController`, never obtained a `SandboxSession`, never called `set_apply_seams` and connected neither `check_reported` nor `check_findings`. D3a's **two Audit channels**: `DdlObjectEditorPanel` stops folding findings into the narrative channel as pre-formatted strings (a string cannot carry the `UserRole` line and `UserRole+1` object key click-to-navigate needs); findings travel as objects on the new `check_findings(list)` signal and MainWindow renders `[Check] SEVERITY line N: message`, resolving navigation by `panel.ref.key` **identity** rather than by tab key (a checked-out object's tab is keyed on its `ddl/*.sql` path, so a tab-key lookup gets exactly that case wrong), with an unmappable line rendering with **neither** role so it is inert rather than navigating somewhere wrong. Apply to Sandbox gains a production caller; Apply-to-Target stays unwired and the panel correctly hides it. Open/Close Sandbox Session are explicit gestures — a deliberate deviation, because `set_project` is explicit that opening a project "opens nothing and provisions nothing" and auto-opening would fire the real opener against localhost inside four existing tests. **CORRECTION FOLDED IN, and it overrides the earlier record and the implementer's own reporting: the wired Check gesture does NOT run tiers 0-2.** `SandboxController.run_check`'s default `checker` is `ddl_check.recheck` (`sandbox_controller.py:332`), which applies nothing; tiers 0-2 compile only through `apply_and_check`/`probe_check`, and neither has any gesture (`grep` of `pgtp_editor/ui/` finds `run_apply` called nowhere, and the wired Apply-to-Sandbox seam goes through `SandboxSession.apply`, not the ladder). So today's Check reports tier 1 `unavailable` and tier 2 `passed` from the bookkeeping table only. Verified by reading the code, not by trusting the message. **Gap found and closed at the module level** (the wiring's own test file is off-limits this pass): nothing asserted that the reachable Check gesture claims no compile — `test_recheck_applies_nothing` checked only `committed is False`. Added a behavioural lock (tier 1 `unavailable` with a stated reason, tier 2's `passed` reason containing "not re-compiled in this run", report never `green`) and a **structural** one (`recheck` has no `applier` parameter while both compiling entry points do), so "Check cannot run tiers 0-2" is now a property of the API rather than of one canned session | `tests/db/test_ddl_check.py` (extended: `test_recheck_has_no_write_seam_at_all_so_it_cannot_compile`, `test_recheck_claims_no_lint_pass_and_says_it_recompiled_nothing`) | PASS — 131 passed (`tests/db/test_ddl_check.py`, 0.5s; 128 before this pass's additions) | PASS — 4328 passed, 45 skipped (`-n 10`, 26.51s) — retrospective gate at the backlog-pass tree state, not at this commit; see row 13/13 | c1d5e25 |
| 2026-08-06 | **Backlog row 9/13 — §18.5 D3 validation tiers 0-2 actually run** (`d28f114`). `apply_and_check`/`probe_check` compose ONE statement list via `build_ladder()` and call the injected applier once: `SET plpgsql.extra_warnings='all'` → optional `DROP TRIGGER IF EXISTS` → the DDL → the `applied` upsert (apply only) → the resolve SELECT → the guarded check SELECT; `probe_check` is the same list minus the bookkeeping row with `commit=False` — one boolean, not a second code path. **The notice channel**, without which tier 1 could never work: tier 1's entire output arrives as PostgreSQL NOTICEs, so `apply.py` gains `Notice`/`normalize_notice`/`RunOutcome` and `ApplyOutcome` carries `notices` **plus** `notices_captured` separately, because "no warnings" and "nobody was listening" must differ — a runner returning a bare list records `notices_captured=False`, which is what makes tier 1 report `unavailable` instead of a false `passed`. `severity_nonlocalized` is read in preference to `severity`, since plain severity is localized by `lc_messages` and any match on "WARNING" would silently miss on a non-English server. The in-transaction check SELECT is **oid-guarded** (oids resolve in a subquery whose `WHERE … IS NOT NULL` yields zero rows when the object is absent, and a LATERAL over an empty left side never invokes the SRF), without which a signature edit would abort the transaction and silently discard a DDL that applied fine. PG<14 trigger gating reads `SandboxCapabilities.server_version` and emits the drop when major < 14 **or** the version is unknown, because the two errors are asymmetric. `plpgsql.extra_errors` is deliberately never set. `SandboxOperation.APPLY` is deliberately **not** in `DESTRUCTIVE_OPERATIONS`: that set means "wipes the sandbox wholesale" (PROVISION/CLONE_DATA/RESET), an apply destroys nothing, and gating it would demand a prompt on every edit/validate iteration — training the user to click through the one prompt that protects the three operations that really do wipe. Coverage reviewed against every clause and judged thorough (~100 tests). **One gap found and closed:** the commit's claim that "the ladder writes the identical row `SandboxSession.apply` does" was asserted only as `"INSERT INTO" in …`; added an equality test against `sandbox.applied_upsert_sql` (timestamp literal normalized, since that is the one field two composers of the same row legitimately differ on) driven with a `ddl_text` that differs from the buffer, so the recorded `text_sha1` is provably the hash of what was applied rather than of whatever the tab happened to hold. Restated here because row 10/13 corrects it: these tiers **run** but are reachable from **no gesture** | `tests/db/test_ddl_check.py` (extended: `test_the_ladders_bookkeeping_row_is_the_shared_upsert_verbatim`); `tests/db/test_apply.py` (36, pre-existing — the notice-channel, `notices_captured`, `diagnose` and `result_at`/`reached` cases read and re-verified, not modified); `tests/db/test_sandbox.py` (100, pre-existing — `applied_upsert_sql`, `SandboxExecutor.fetch`, `text_sha1` read and re-verified) | PASS — 131 passed (`tests/db/test_ddl_check.py`); PASS — 267 passed (`tests/db/test_ddl_check.py` + `test_apply.py` + `test_sandbox.py`) | PASS — 4328 passed, 45 skipped (`-n 10`, 26.51s) — retrospective gate at the backlog-pass tree state, not at this commit; see row 13/13 | d28f114 |
| 2026-08-06 | **Backlog row 8/13 — §18.5 D2/D2a Sandbox Setup dialog (built, deliberately NOT wired)** (`296eb0c`). The biggest hole in §18: nothing in `ui/` had ever called `create_sandbox_database`/`provision_sandbox`/`clone_data`/`reset`, so no sandbox database was ever created and the whole sandbox half of the app had no way to come into existence. The dialog is a **view over the controller** — no SQL, no connection, no duplicated controller logic, and every user-facing sentence sourced rather than retyped (`degraded_reason` verbatim, `ForeignDatabaseError`'s own sentence, `install_gate`'s refusal, `ddl_check.not_installed_reason`, `determine_project_tier`, `SandboxController.destructive_warning`). Controls whose precondition does not hold are **absent with the reason stated in their place**, not disabled dead controls. **The friction point this row exists to record:** the controller's `confirm_destructive` is constructor-only and private, so the dialog cannot install itself as the prompting surface — hence a two-mode confirmation contract, `confirm=None` (default) letting the controller own the single prompt with a decline surfacing here as its "cancelled" result, versus passing `confirm=` to have the dialog prompt instead; passing the same callable to both asks twice. **Genuine gap found and closed: only the `confirm=`-supplied mode was tested.** Every existing confirmation test injects a seam, so mode 1 — the default, and the one an integrator gets if they wire nothing — had no coverage at all: nothing proved the dialog pre-confirms nothing and lets the gesture through to the controller, and nothing proved the controller's own refusal surfaces in the dialog's result text. Added both, the second driven through a **real** `SandboxController(confirm_destructive=None)` so the cancellation wording is the shipped one rather than a copy (provision confirms after its params checks, so this needs no session and opens no connection). Added a third guard that the module never names a modal Qt call, since the injected seam is the only reason no test here can reach a modal | `tests/ui/test_sandbox_setup_dialog.py` (extended 34 → 37: `test_with_no_confirm_seam_the_gestures_reach_the_controller_unprompted`, `test_a_controller_declined_operation_surfaces_as_its_cancelled_result`, `test_the_dialog_module_never_reaches_a_modal_qt_call`) | PASS — 37 passed (`tests/ui/test_sandbox_setup_dialog.py`, 0.4s; 34 before this pass's additions) | PASS — 4328 passed, 45 skipped (`-n 10`, 26.51s) — retrospective gate at the backlog-pass tree state, not at this commit; see row 13/13 | 296eb0c |
| 2026-08-06 | **Backlog row 7/13 — §23 MCP server reachable headlessly via `--mcp` and `python -m pgtp_editor.mcp`** (`b2e106d`). The server, all six tools, both providers and ~600 lines of tests shipped earlier in `09db778`, but no gesture or flag could start it: `parse_args` accepted only `--debug` and a positional file, there was no `mcp/__main__.py`, and no menu entry — the layer was correct and completely dark. `--mcp` runs the server **instead of** the GUI, which the existing code had already committed to (`serve_stdio`'s docstring calls itself "THE headless `--mcp` entry point" while `start_server_thread` is the GUI one) and which stdout ownership forces anyway, since stdout is the protocol channel and a stray `print` from a GUI process would corrupt the session; the branch returns before any Qt import, asserted by a test that patches `QApplication` with a class raising on construction. One start path, not two: `run_mcp_server(path, *, serve=None, stderr=None)` is the only construction site, and `mcp/__main__.py` parses argv with the app's own `parse_args` and calls the same function, so the two invocations cannot drift. **Note worth recording: `pgtp_editor --mcp` is NOT a command** — `pyproject.toml` declares no `[project.scripts]`, so the two real invocations are `python -m pgtp_editor --mcp`-style module execution and `python -m pgtp_editor.mcp`. Coverage reviewed and judged thorough, including the missing/unreadable/directory argument paths verified both in-process and through a real subprocess asserting no `Traceback` and empty stdout. **One gap found and closed:** `python -m pgtp_editor.mcp` with **no** file argument — a supported invocation, since the per-tool `path` argument then carries the project — was only covered end-to-end by the subprocess test and never asserted to reach `run_mcp_server(None)`; a regression making a pathless invocation an error would have shown up only as a subprocess that still somehow worked. Also verified (not added — already covered) that an explicit per-call path beats the CLI default, which is what keeps `diff_projects` working | `tests/mcp/test_cli.py` (extended 18 → 19: `test_module_main_with_no_arguments_serves_the_default_provider`) | PASS — 19 passed (`tests/mcp/test_cli.py`, 0.45s; 18 before this pass's addition) | PASS — 4328 passed, 45 skipped (`-n 10`, 26.51s) — retrospective gate at the backlog-pass tree state, not at this commit; see row 13/13 | b2e106d |
| 2026-08-06 | **Backlog row 6/13 — §18.5 D4 Sandbox SQL Console: the panel, its CenterStage registration, the §22 lint injection seam and §27's Ctrl+Return** (`174469a`, `5691108`, `e143a29`). The panel is a `QSplitter` over `CodeEditor(language="sql")` and the shipped `SqlResultsPanel` with a row-limit spin box, reusing rather than rebuilding (`sql/keywords.py` stays the one dialect source, `DEFAULT_ROW_LIMIT` is an alias of `sandbox_query.DEFAULT_MAX_ROWS`, `status_line` reused verbatim). **The safety boundary is structural**, as D4 requires: execution goes through `run_sandbox_query`, which takes a `SandboxSession` and never a `ConnectionParams`, so an ad-hoc statement can only reach a database this app created and can wipe — there is no "run against target" affordance, not even a disabled one. `truncated` is read from the field `sandbox_query` sets by fetching `max_rows + 1`, never inferred from `len(rows) == row_limit`, so a result exactly at the cap stays distinguishable from one that was cut off. `5691108` files the console in the **existing** `_ddl_object_tabs` key→widget map under `CONSOLE_TAB_KEY == ("sandbox-sql",)` (collision impossible: object keys are 5-tuples or `str`), reached through three sibling methods rather than by giving `open_ddl_object_tab` a `ref is None` special case, and needs **both** new guards or the app crashes — an explicit console branch ahead of the object loop in `_on_tab_close_requested` (otherwise the ✕ emits `ddl_object_close_requested(("sandbox-sql",))` → `panel.is_dirty()` → `AttributeError`) and `ddl_object_panels()` filtering to `DdlObjectEditorPanel`. `e143a29` adds `lint_service`/`lint_on_save` to `open_php_file_tab` (a single omission that blocked all four remaining §22 wiring items) and binds §27's Ctrl+Return to the panel's existing `run()` as a `WidgetWithChildrenShortcut` `QShortcut`, so there is no second execution path. Two spec'd controls are **absent rather than faked** and one drift is recorded: no statement-timeout spin box (the shipped `sandbox_query` has no timeout parameter to pass), no multi-statement splitting/per-statement status/ddl confirmation (`sql/statements.py` did not exist yet — see row 5/13, where it now does and the console still does not use it), and D4's `QueryResult`/`QueryError` shape is really a `QueryOutcome` tri-state. Coverage reviewed and judged complete; the three carrying test files (`test_sql_console_panel.py`, `test_center_stage.py`) are outside this pass's allow-list — being edited concurrently — so they were read only and not extended | none added — `tests/ui/test_sql_console_panel.py` (42) and `tests/ui/test_center_stage.py` (37) are off-limits in this pass; read and re-verified only | NOT RUN in this pass by targeted invocation — both carrying files were outside the allow-list and are being edited concurrently; covered by the full-suite gate in the next column | PASS — 4328 passed, 45 skipped (`-n 10`, 26.51s) — retrospective gate at the backlog-pass tree state, not at this commit; see row 13/13 | 174469a, 5691108, e143a29 |
| 2026-08-06 | **Backlog row 5/13 — §18.5 D4 `split_statements` + `classify_statement` (built, deliberately NOT wired)** (`74abb7f`; not on the dispatch's list, found by walking `e8f853f..HEAD` and logged so the backlog is actually closed). Built on the existing `sql/tokenizer.py` rather than a second lexer: the splitter breaks only on a top-level PUNCT `";"`, so semicolons inside strings, `E'…'`, quoted identifiers, `--`/nested block comments and dollar-quoted bodies are not tokens at that level at all, and differently-tagged nested dollar quotes nest correctly for free because the tokenizer closes on the matching tag — Postgres' actual rule. Returns a frozen `Statement{text, start, end, start_line, start_col, terminated}` rather than `list[str]`, because text alone cannot be located in the buffer (two identical statements would be indistinguishable and `str.find` would attribute a failure to the wrong one); it composes with `db/apply.py` exactly, and `line_offset` — the one thing a caller cannot recompute — is why the dataclass exists. Classifier rulings are all biased away from "read": plain `EXPLAIN` is a read but `EXPLAIN ANALYZE` is `unknown` because it executes the inner statement; `TRUNCATE` is `write`, not `ddl`, because the ddl prompt asserts the applied working set may no longer match and TRUNCATE leaves every definition intact; `WITH` follows its body, with a bare identifier spelling a write keyword only ever pushing toward `write`; `CHANGES_OBJECTS = {"ddl", "unknown"}` is the one home of "unknown is treated as ddl". Coverage reviewed against every ruling and judged complete (36 tests, including the `line_offset` composition against the real `line_of_position` and slice round-trips). No gap worth filling, so the file was not modified. **Reported, not fixed: still unwired** — `pgtp_editor/ui/sql_console_panel.py` contains no reference to `sql.statements`, so the console continues to send the whole buffer as one execute with no per-statement status and no ddl/unknown confirmation, which is the reason this module was written | `tests/sql/test_statements.py` (36, pre-existing — read and re-verified, not modified) | PASS — 36 passed (`tests/sql/test_statements.py`, part of the 485-passed allow-list batch) | PASS — 4328 passed, 45 skipped (`-n 10`, 26.51s) — retrospective gate at the backlog-pass tree state, not at this commit; see row 13/13 | 74abb7f |
| 2026-08-06 | **Backlog row 4/13 — §18.5 D3a `check_working_set` + `SandboxOperation.CHECK`** (`20dc5cf`). `check_working_set` is the spec's "pure loop, not a second mechanism": it iterates `SandboxSession.applied()` and calls the same recheck entry point per row, returning `{ref: CheckReport}` with no SQL, no tier logic and no second reporting path — and gets no menu entry deliberately, since it exists for Generate Deployment SQL's future "is everything green?" question and a control now would be a dead control. **The row-derived degradation is the point of this entry:** `AppliedObject` carries only kind/schema/name/table/applied_at/text_sha1 — no argument types, no buffer text, and for a trigger no referenced function — so a request built from a bookkeeping row necessarily degrades, and each degradation is routed onto an **existing** reason rather than a new path (an overloaded routine resolves as `schema.name()` → `REASON_OBJECT_ABSENT`; a trigger row → `REASON_TRIGGER_FUNCTION_UNKNOWN`; empty buffer text → every finding line `None`, rendered line-less per D3's never-guess rule). That is why the row→request adapter is an injected seam. Keyed on the bookkeeping table's PRIMARY KEY 4-tuple, not on `AppliedObject` (whose `applied_at`/`text_sha1` change on every re-apply), and `REASON_SWEEP_ROW_ERRORED` means one failing row neither aborts the sweep nor silently shrinks the result, since a shorter dict would be indistinguishable from a smaller working set. `SandboxOperation.CHECK` is non-destructive **by construction**: absent from `DESTRUCTIVE_OPERATIONS`, `destructive_warning` is `""`, and `run_check` never calls `_confirmed`, so no confirmation path exists for it even with a `confirm_destructive` seam wired. `SandboxOperationResult.report` is set only by CHECK so "no report" and "clean report" stay distinct; `can_check` is a visibility predicate and deliberately does **not** gate on `plpgsql_check_state`. Coverage reviewed clause by clause against all of the above and judged complete — including the sweep's empty case, per-row seam call, the trigger/overload degradations, the erroring row, and the default `recheck` seam. No gap worth filling in the allow-listed file for this item and no implementation bug found | `tests/db/test_ddl_check.py` (the sweep block, pre-existing — read and re-verified; this pass's 3 additions to the file are recorded in rows 9/13 and 10/13); `tests/ui/test_sandbox_controller.py` (50, off-limits this pass — read only) | PASS — 131 passed (`tests/db/test_ddl_check.py`) | PASS — 4328 passed, 45 skipped (`-n 10`, 26.51s) — retrospective gate at the backlog-pass tree state, not at this commit; see row 13/13 | 20dc5cf |
| 2026-08-06 | **Backlog row 3/13 — §18.5 D3a `db/apply.py`, the ladder's write seam (built, deliberately NOT wired)** (`8da2fa9`). D3a names `apply_ddl` as the single call the ladder runs through and the module did not exist; confirmed a genuine gap rather than a renamed seam (`ddl_check.py` said so in four places, and the closest existing thing — `SandboxSession.apply` — runs a statement list in one transaction but returns `None`, discards every result set so the check SELECT's rows can never come back, and reports no failing-statement index). Why one call is required: `SET plpgsql.extra_*` → the DDL → the `plpgsql_check_function_tb` SELECT must share one session and transaction, and `ApplyOutcome.statement_index` is what attributes a failure **in the check call** to the check rather than reporting it as "your DDL is broken". `ApplyRunner` is a sibling Protocol of `introspect.Runner`/`sandbox.SandboxExecutor`/`sandbox_query.QueryRunner`; `StatementFailure(statement_index, statement, cause)` exists because the runner owns the loop and is therefore the only place that knows how far the list got; `line_of_position()` puts D4's position→line rule in one place and returns `None` rather than a guessed line; `commit=False` serves D3's probe without a second code path and `committed` is a stated fact, never inferred from `ok`; `target` accepts a `ConnectionParams` **or** anything with `.params`, because this is Apply-to-Target's seam too. Never raises for a database problem, always raises for a caller bug. Coverage reviewed and judged thorough (32 tests at the time, 36 now, no psycopg, no connection) — including the two cases that carry the design: a failure in the check SELECT attributed to the check rather than the DDL, and a connection failure carrying `statement_index=None` rather than `0`. No gap worth filling and no implementation bug found, so the file was not modified. Left unwired at this commit; row 9/13 is where the tiers actually started calling it | `tests/db/test_apply.py` (36, pre-existing — read and re-verified, not modified) | PASS — 36 passed (`tests/db/test_apply.py`, part of the 485-passed allow-list batch) | PASS — 4328 passed, 45 skipped (`-n 10`, 26.51s) — retrospective gate at the backlog-pass tree state, not at this commit; see row 13/13 | 8da2fa9 |
| 2026-08-06 | **Backlog row 2/13 — BUG-029: Project Status art rendered from vector at device size** (`acb813f`, closed in `da2f126`). Three compounding problems. **Blurry:** the bundled PNGs were tiny (Quality 36×72, Sandbox1 20×20, connectors as small as 23×5) and `_scaled_pixmap` unconditionally magnified them to `source.size() * ASSET_SCALE(2.0) * dpr`, so the softness was inherent to the pipeline; the owner then supplied SVGs and deleted all 32 PNGs, leaving `ASSET_EXTENSION` pointing at files that no longer existed and the window broken outright. Each asset now renders through `QSvgRenderer` into a `QImage` sized `logical × dpr` with `setDevicePixelRatio(dpr)`, bytes read via `importlib.resources` so it stays zip/wheel-safe. Logical size is `defaultSize() * 2.133`, not raw: Qt reads these SVGs' millimetre dimensions at 90 dpi while the deleted PNGs were 96 dpi exports of the same drawing, so the 96/90 correction lands every new logical size within 1–2 px of the old one — **only sharpness changes, not layout**. **Clipped:** `_boxed_pixmap` padded to `box` at `max(0, (box - size)//2)`, so an icon larger than `box` had its offset clamped to 0 and the overflow silently cut; the pad is now `box.expandedTo(icon)` with the clamps gone, `_make_node` passes the same expanded box on so the label cannot crop the grown pad back, `_rebuild` captures **one** dpr and threads it through instead of `_make_node` re-reading `pixmap.devicePixelRatio()` (which could disagree with the ratio the pixmap was built at), `_make_connector` sizes from a ceiling plus `AlignCenter` because some connector art is only 5 px tall, and `_logical_size` **ceils** rather than rounds so a reported logical size is never smaller than the true extent. A `devicePixelRatioChanged`/`screenChanged` hook rebuilds when the ratio really changes. **Known-bad asset, documented not papered over:** `resources/status/sandbox_offline.svg` is not an SVG — it is a 40×34 PNG saved under an `.svg` name, which `QSvgRenderer` rejects and which would have left the offline sandbox node a blank gap; `_scaled_pixmap` falls back to `QPixmap.loadFromData` for non-SVG content at 2×, so **that one state renders soft while its `_drk` counterpart is crisp**, awaiting a real re-export from the owner (no code change will follow). Packaging verified rather than assumed: the `resources/status/*` glob is extension-agnostic and no `MANIFEST.in` exists. Coverage reviewed and judged complete, notably the box ≥ icon assertion per family, boxes asserted **identical** at 1× and 2× (sharpness, not layout), device == logical × dpr, a sweep rendering every bundled asset, and an on-disk test that now sniffs content so a PNG-named-`.svg` or an empty file still fails. The carrying files (`tests/ui/test_project_status_panel.py`, `test_project_status_model.py`) are outside this pass's allow-list, so they were read only | none added — the two carrying files are off-limits in this pass; read and re-verified only | NOT RUN in this pass by targeted invocation — both carrying files were outside the allow-list; covered by the full-suite gate in the next column | PASS — 4328 passed, 45 skipped (`-n 10`, 26.51s) — retrospective gate at the backlog-pass tree state, not at this commit; see row 13/13 | acb813f, da2f126 |
| 2026-08-06 | **Backlog row 1/13 — §18.5 D3a level→severity drifts corrected** (`db3d100`). Drift 1: `compatibility` was folded into the warning branch and returned `"warning"` where D3a's table says INFO; it is now its own `return "notice"` ahead of the prefix test — the one deliberate downward map, documented as such. Drift 2: the spec requires an unknown level to map to WARNING **and** have its raw level appended to the message in parentheses, never dropped, which was implemented nowhere; since `severity_for_level` returns a severity and structurally cannot append to a message, the parenthetical now lives in `parse_findings` via new `KNOWN_LEVELS`/`is_known_level`/`message_for_level` — deliberately **not** in the UI layer, which would put plpgsql_check level knowledge in the renderer and create the second mapping the spec forbids. **The boundary ruling worth recording:** `warning <unknown-suffix>` is treated as UNKNOWN and therefore gets the parenthetical, even though the prefix rule already maps it to WARNING correctly — the spec names exactly four warning variants, the Audit renderer shows only severity/line/message, so the parenthetical is the only place a new warning class becomes visible, and getting the severity right by luck of a prefix is not the same as not swallowing the level. A **blank** level is treated as known: there is no raw text to preserve and `"m ()"` is noise. Also corrects text that `8da2fa9` made false (`REASON_TIER_NOT_BUILT` and two docstrings said `db/apply.py` "does not exist yet"; they now say it exists but is not wired into this driver — and nothing was wired), and `REASON_NOT_INSTALLED` now carries the spec's install locations verbatim plus a superuser passthrough via new `not_installed_reason`, which substitutes `install_gate`'s **own** refusal sentence so `sandbox.REASON_REQUIRES_SUPERUSER` is never retyped. Coverage reviewed and judged complete: the existing severity parametrization was extended rather than duplicated, and totality over odd inputs, the unknown-suffix boundary, the blank-level case, the "mapping lives in exactly one place" guard and the install-locations/gate-passthrough pair each have a test. No gap worth filling for this item and no implementation bug found. Note: at that commit nothing uppercased severity yet, so `compatibility` only started displaying as INFO once `c1d5e25`'s renderer landed (row 10/13) | `tests/db/test_ddl_check.py` (the severity/level block, pre-existing — read and re-verified; this pass's 3 additions to the file belong to rows 9/13 and 10/13) | PASS — 131 passed (`tests/db/test_ddl_check.py`, 0.5s) | PASS — 4328 passed, 45 skipped (`-n 10`, 26.51s) — retrospective gate at the backlog-pass tree state, not at this commit; see row 13/13 | db3d100 |
| 2026-08-06 | Bugfix batch of 3 (docs/BUGFIX_QUEUE.md BUG-031, BUG-030, BUG-032 **facet B only** — facet A deliberately excluded, being re-triaged) — verification pass on implementations that shipped with their own tests. **BUG-031** (Project Status window could not be reopened after a close): `main_window.py::_open_project_status`'s reuse branch now calls `existing.show()` plus a defensive unminimize before `raise_()`/`activateWindow()` — closing only hides (no `WA_DeleteOnClose`, so `destroyed` never fires and the cached `_project_status_window` is never reset), and raise/activate on a hidden window are silent no-ops. Code-vs-spec only (CONSOLIDATED_SPEC §18.8 already specifies non-modal single-instance). **BUG-030** (Quality node green for any *configured* target, `probe_error` hardcoded `None`): new `_ddl_target_probe_error` field, new `_refresh_target_connection_status()` running `db_test_connection` off the GUI thread via `_run_async`, called from `refresh_project_capability_status()` as the single "re-probe everything the window shows" entry point; new extracted `_project_status_target()` single-sources BUG-024's target selection for the diagram, the summary line and the probe; reset in `_close_ddl_project`. **BUG-032 facet B** (`KeyError: 'lookup'`): `properties_panel.py` maps `"lookup"` to the existing `_rows_for_column` builder and `show_node` now degrades to the empty state on an unknown kind instead of raising. Existing tests reviewed clause by clause against each entry's "Test impact" and found to cover the stated cases; **4 gap tests added** — reopen-after-close still re-probes and re-renders (the reuse branch's re-probe is load-bearing because `ProjectStatusPanel.showEvent` probes only on first show); projectless mode probes the app-level saved connection (the second, untested branch of `_project_status_target`); the panel's own `refresh()` seam (Re-check button / on-open) re-probes the target, not just a direct `refresh_project_capability_status()` call; and a net that selects **every** row in a built coherence tree so no node kind can crash the Properties panel again. All 13 new/extended tests verified to fail against the pre-fix code (path-limited stash, restored) — they are real regression locks, not tautologies. **Assessment of the 4 flagged items:** (1) **real remaining hole, reported not tested** — `quality_state(configured=target is not None …)` is a constant `True` whenever a project is open (`ProjectSettings.target` defaults to `ConnectionParams()`, never None), so a **blank-host target renders green "Connected"** and `not_set_up` is unreachable for any open project; verified empirically (`quality_connection_ok`), and inconsistent with the sandbox node's own `bool(sandbox_params.host)` convention plus §18.8's "not_set_up (never configured) / error (configured but unreachable)" wording. Out of scope for BUG-030's reported symptom, but a genuine follow-up. (2) **covered** — `test_closing_a_project_clears_the_target_probe_error`, plus the host-less clear case. (3) **the queue entry's premise is stale** — `run_queries` already takes `connect_timeout: int = 10` and `test_connection` uses the default, so the green→red correction is bounded at ~10s per probe, not the OS TCP timeout; nothing to add. (4) **keeping both coherence tests is right** — the `show_node`-patched one pins the `(node, kind)` contract at the signal boundary (which the unpatched one cannot distinguish), the new unpatched one is the end-to-end regression lock; replacing rather than adding would have lost coverage. Second finding reported: coherence `kind="reference"` rows (Tables branch ▸ "Referenced by") also had no `_ROW_BUILDERS` entry, so they crashed with `KeyError: 'reference'` pre-fix and now render an **empty** Properties panel where the retired Table References panel showed the owning page/detail/column (`TableReference.kind` carries the right kind in `payload`) — a functional gap the graceful degrade masks. | Extended 4 files, 13 new tests total (9 pre-existing from the implementers + 4 added here): `tests/ui/test_ddl_creation_wiring.py` (+2: reopen-after-close lifecycle, reopen re-probes/re-renders), `tests/ui/test_project_capability_wiring.py` (+9: offline/reachable/not-configured Quality states, same-params-as-summary, host-less never probed & stale error cleared, async result pushes a corrected diagram into the open window, close-project clears the error, projectless app-level probe, panel `refresh()` seam re-probe), `tests/ui/test_properties_panel.py` (+2: `"lookup"` renders identically to `"column"`, unknown kind → empty state), `tests/ui/test_coherence_wiring.py` (+2: unpatched lookup selection really populates Properties, every-row no-crash net) | PASS — 72 passed (`QT_QPA_PLATFORM=offscreen venv/bin/python -m pytest tests/ui/test_ddl_creation_wiring.py tests/ui/test_project_capability_wiring.py tests/ui/test_properties_panel.py tests/ui/test_coherence_wiring.py -q`) | PASS — 3828 passed, 45 skipped (`QT_QPA_PLATFORM=offscreen venv/bin/python -m pytest -q -n 10`, 25.7s; working tree also carried a parallel session's in-progress BUG-032 facet A work in `db/ddl_check.py`/`ui/sandbox_controller.py`, which was green too and is not part of this entry) | (uncommitted) |
| 2026-08-06 | FQ-005 "Give the light theme the same professional QSS polish as the dark theme" (docs/FEATURE_QUEUE.md FQ-005; CONSOLIDATED_SPEC §7 theme subsection) — completion pass on work whose tests were written in a session cut short by a machine crash before this log entry existed. Implementation: `pgtp_editor/ui/theme.py`'s `_dark_stylesheet()`/`_dark_qss_cache: str \| None` replaced by `_qdarkstyle_stylesheet(light: bool)` with `_qss_cache: dict[bool, str]`, passing `palette=LightPalette if light else DarkPalette` explicitly, and `apply_theme` now calling `app.setStyleSheet(_qdarkstyle_stylesheet(light))` unconditionally for both themes instead of `"" if light else …`. Zero new dependencies (`qdarkstyle>=3.2` already pinned). `light_palette()`/`dark_palette()` unchanged. Existing tests reviewed clause by clause against the queue entry and found to cover: both themes get a real non-empty QSS; the two variants are distinguished by palette-specific hex markers (`DarkPalette.COLOR_ACCENT_1` present only in dark, `LightPalette.COLOR_BACKGROUND_1` only in light) rather than mere non-emptiness; the light↔dark↔light round-trip re-applies each cached variant with no cross-contamination; Fusion is still genuinely requested. The documented `QStyleSheetStyle` side-finding (Qt wraps a styled app's style in a proxy whose `objectName()` is `''`, so post-stylesheet `style().objectName() == "fusion"` read-backs are unreliable — already silently true for dark, merely never asserted) is handled correctly by spying on `QApplication.setStyle` **patched at class level**; the test docstring records that instance-level patching of the singleton leaves shiboken's method resolution broken past monkeypatch teardown and leaks into later test files. **One genuine gap found and closed:** the light branch asserted only `Window.lightness() > 200` where dark asserted exact rgb, so nothing verified that the newly-applied light QSS does not replace/override the hand-rolled `light_palette()` roles — the invariant `XmlEditor.apply_theme_colors` depends on (it keys off palette Base lightness). Also added explicit coverage that `_qss_cache` holds one live entry per theme (the BUG-010 single-string cache became a dict), so neither toggle direction evicts the other. No implementation bugs found. **Riding along, not part of FQ-005:** a test-harness fix in tests/ui/conftest.py — new autouse `_flush_deferred_deletes` calling `QCoreApplication.sendPostedEvents(None, QEvent.Type.DeferredDelete)`, because pytest-qt never runs a loop that delivers `DeferredDelete`, so every widget every test made leaked for the whole worker process (a measured constant 13 top-level widgets per `MainWindow`, accumulating linearly and making the suite progressively slower until unusable). Confirmed to change no test's outcome other than by freeing widgets: the full suite is green with zero newly-failing, newly-skipped, or newly-passing tests, and it now completes in 20.18s — the previous sessions' "full suite is unusably slow, 10-30 min with no conclusion" note (see the two FQ-004/FQ-003 rows below, where the full suite was recorded NOT RUN for exactly that reason) no longer applies, so this is the first row in a while able to record a real full-suite gate | tests/ui/test_theme.py (extended 20 → 22 this pass: `test_apply_theme_light_keeps_hand_rolled_palette_under_the_qss`, `test_qss_cache_holds_one_entry_per_theme`; the reworked round-trip/symmetry tests, `test_apply_theme_light_and_dark_get_distinct_qdarkstyle_stylesheets` and the `setStyle`-spy Fusion assertion were read and re-verified, not rewritten); tests/ui/test_main_window_theme.py (symmetric `setStyle`-spy assertions in 3 tests, re-verified); tests/ui/conftest.py (autouse `_flush_deferred_deletes` harness fixture) | PASS — 28 passed (tests/ui/test_theme.py + tests/ui/test_main_window_theme.py, 3.56s; 26 before this pass's 2 additions) | PASS — 3793 passed, 45 skipped, 0 failed (`-n 10` xdist, 20.18s) | e8f853f (theme+tests+spec); conftest harness fix in b1b0bb4 |
| 2026-08-06 | FQ-004 "Choose a Breeze icon for any toolbar button" (docs/FEATURE_QUEUE.md FQ-004) — verification pass on an implementation that shipped with its own tests. 99 further Breeze SVGs vendored (106 total) behind an enumerable catalog in `ui/icons.py` (`search_catalog`, `human_name_for`, `catalog_filename`, legacy ids still resolving); pure persistence helpers in `ui/toolbar_registry.py` (`icon_id_for` — assignment wins over the legacy default, and gives a previously icon-less command an icon; `resolve` dropping assignments whose SVG is no longer vendored); new `ui/icon_picker_dialog.py` (searchable grid + an always-present "Default" cell at row 0 yielding the `None` sentinel, initial selection = current icon, graceful fallback to Default when the current id is unknown or filtered away, double-click accepts); an icon column in the Customize Toolbar dialog (`assign_icon`/`icon_assignments`/`effective_icon_id`/`result_icon_assignments`, the last pruning assignments for buttons removed from the toolbar); and `_set_action_icon` consulting assignments before the legacy default, round-tripping through QSettings. Coverage reviewed clause by clause: catalog/disk agreement in both directions, sorted + duplicate-free, every catalog icon renders non-null, the legacy seven unchanged; assignment-over-default at both the pure-helper and live-toolbar level; every picker behavior including the two fallback paths. **One genuine gap found and closed:** `CustomizeToolbarDialog._choose_icon` — the *only* seam joining the picker to the dialog, and the only `.exec()` path — had a test for its "nothing selected" no-op branch but none for either outcome branch; its own docstring said "never reached from a test". A regression there (applying to the wrong row, applying on Cancel, or storing a null assignment instead of removing it) would have shipped silently. Closed with 4 tests driven through a `_patch_picker` stub that replaces `icon_picker_dialog.IconPickerDialog` (resolved at call time by the function-local import, so the patch genuinely takes effect — confirmed by the stub-observed seed value, not just by the run not hanging). No implementation bugs found; all 4 passed against the implementation as written on the first run | tests/ui/test_customize_toolbar_dialog.py (extended 22 → 26 this pass: `test_choose_icon_applies_the_pickers_choice_to_the_selected_row`, `test_choose_icon_seeds_the_picker_with_the_rows_existing_assignment`, `test_choose_icon_reset_to_default_clears_the_assignment`, `test_cancelling_the_picker_leaves_the_assignment_untouched`, plus a shared `_patch_picker` helper); tests/ui/test_icons.py, tests/ui/test_toolbar_registry.py, tests/ui/test_icon_picker_dialog.py, tests/ui/test_toolbar.py (pre-existing FQ-004 coverage read and re-verified, not duplicated) | PASS — 131 passed (tests/ui/test_icons.py + test_toolbar_registry.py + test_customize_toolbar_dialog.py + test_icon_picker_dialog.py + test_toolbar.py, `-n 8` xdist, 9.24s; 127 before this pass's 4 additions) | NOT RUN — standing user instruction for this session: never run the full pytest suite (broken/unusably slow in this checkout, 10-30 min with no usable conclusion). Verification is the targeted run in the previous column only. Consequence, named honestly: `_set_action_icon` and the toolbar-rebuild path live in `main_window.py`/`toolbar_registry.py`, which many other UI tests construct a `MainWindow` through — a regression in icon resolution that only manifests during some other feature's window setup would not be caught here. The vendored-SVG count change (7 → 106) also widens `resources/` packaging assumptions that no targeted file in this list asserts | a12b522 |
| 2026-08-06 | FQ-003 slice 1 — pure coherence model `db/coherence.py` (docs/FEATURE_QUEUE.md FQ-003). Model only: builds the two-branch coherence tree (XML-side pages/details/lookups against DB-side tables/views), rolls reference roles up into per-relation badges, flags dangling references at the reference point only, flags dangling page bindings on the page, flags unreferenced relations and failing/missing column checks, shows calculated columns without ever flagging them, and offers `filter_flagged` which preserves the ancestors of flagged leaves, drops unflagged subtrees while keeping branch roots, and returns `None` when nothing is flagged. Verification pass on an implementation that shipped with its own tests; coverage checked against each of those behaviors and judged complete for a pure model — every flagging rule, both nesting depths, and all three `filter_flagged` outcomes are covered. **Scope note: the `ui/coherence_panel.py` view is deliberately NOT covered by this entry** — it was still being built by a concurrent session at the time of this run and is therefore not "done"; it needs its own feature-tester dispatch and its own log row. No implementation bugs found; no additional tests judged necessary, so no test file was modified in this pass | tests/db/test_coherence.py (15 tests, pre-existing, read and re-verified — not modified) | PASS — 15 passed (tests/db/test_coherence.py, `-n 8` xdist, 1.17s) | NOT RUN — standing user instruction for this session: never run the full pytest suite (broken/unusably slow in this checkout, 10-30 min with no usable conclusion). Consequence here is the mildest of tonight's five entries: `db/coherence.py` is a new, pure, Qt-free module with no existing importer other than the in-flight panel, so it has almost no regression surface — but by the same token nothing verifies that its data-shape assumptions still match whatever `compare.py`/`introspect.py` actually produce today beyond this file's own hand-built fixtures | 4abaf61 |
| 2026-08-06 | §18.3 groundwork — deterministic schema snapshots, the deploy-bundle ambiguity gate, and the schema compare viewer (CONSOLIDATED_SPEC.md §18.3). `db/schema_snapshot.py`: git-diffable JSON snapshots (indented, sorted, newline-terminated, byte-identical across two dumps of equal schemas and independent of dict insertion order) with a declared format marker + version and **exact-key validation** — a missing, unrecognized, or wrongly-typed key at any level is rejected by name rather than silently defaulted, so a snapshot from a future or foreign writer can never be half-read; round-trip is verified to be equality-preserving (including argument tuples staying tuples and column order surviving) and to diff clean through `diff_schemas`. `db/deploy_bundle.py`: the `*`-candidate / `!`-blocker gate, all-or-nothing **by construction** — a blocked plan exposes no bundle at all rather than a filtered one, every blocker is named in the refusal (not just the first), an empty candidate set is an approved empty bundle distinguishable from a block without exceptions, order is stable/alphabetical and re-orderable only by a genuine permutation, the plan and bundle are frozen with no edit API, and the module is statically verified to never execute SQL or shell out (the git step is an explicit no-op placeholder). `ui/schema_compare_panel.py`: grouped diff viewer with **default-unchecked review discipline**, group rows non-checkable, migration text including only checked entries, save refused with nothing checked, unsupported/table differences surfaced rather than hidden, "nothing compared yet" distinguishable from "no differences", injected source seams that raise rather than pretending when missing, and **no execute path at all** (pinned by its own test). Coverage reviewed against every clause above and judged complete; no gaps worth filling and no implementation bugs found, so no test file was modified in this pass | tests/db/test_schema_snapshot.py (30), tests/db/test_deploy_bundle.py (20), tests/ui/test_schema_compare_panel.py (17) — all pre-existing, read and re-verified, not modified | PASS — 88 passed (tests/db/test_schema_snapshot.py + test_deploy_bundle.py + tests/ui/test_schema_compare_panel.py, `-n 8` xdist, 1.48s) | NOT RUN — standing user instruction for this session: never run the full pytest suite (broken/unusably slow in this checkout, 10-30 min with no usable conclusion). Consequence: all three modules are new and self-contained, so the direct regression risk is low — but `schema_snapshot.py` serializes the `DatabaseSchema`/`RoutineInfo`/`TriggerInfo`/`ColumnInfo` dataclasses from `db/introspect.py`, and its exact-key validation is precisely the thing that will break loudly the next time one of those dataclasses gains a field. That coupling is covered here only by this file's own "snapshot covers every dataclass field" test, and a change made elsewhere in the same session would not have been caught by any run in this row | dffb59b |
| 2026-08-06 | §18.8 "Project Status window" (CONSOLIDATED_SPEC.md §18.8). Assets moved out of the repo-root `images/` into `pgtp_editor/resources/status/` (so they ship with the package) plus the pure `ui/project_status_model.py`: classification of the real degradation reasons emitted by the sandbox probe — verified **verbatim against the producing code**, not against a guessed string — with an unrecognized reason deliberately classifying as "other"/red rather than falling through to healthy; the app/sandbox/quality node state machines (three visual states for the sandbox node; `WITH_DATA` filled only once the clone actually landed; the four plpgsql_check values collapsing onto two icons, with an unrecognized value never claiming "installed"); theme-suffixed asset filenames resolved into the bundled resources dir, with tests asserting **every asset the model can possibly emit exists on disk** and that `all_asset_stems` covers every family and connector — the class of bug that would otherwise ship as a blank diagram. `ui/project_status_panel.py`: the diagram widget — five-node full render, the absence rule hiding the sandbox trio only when never configured (a broken-but-configured sandbox still renders), icons aligned on one centre line, per-node detail windows with click-through detail (tools-missing names the missing tools and offers help; a healthy sandbox offers no help button; sandbox1 is a two-step clone/redo action; sandbox2 offers Install only when not installed), keyboard activation, safe behavior with no callbacks and with no diagram, and a theme flip swapping every asset. MainWindow wiring (Database ▸ Project Status…, re-probe on open, Reconnect, reuse of the same window on reopen) covered in tests/ui/test_ddl_creation_wiring.py. Coverage reviewed against every spec clause and judged complete; no gaps worth filling and no implementation bugs found, so no test file was modified in this pass | tests/ui/test_project_status_model.py (33), tests/ui/test_project_status_panel.py (22) — pre-existing, read and re-verified, not modified; tests/ui/test_ddl_creation_wiring.py (the shared FQ-002/§18.8 wiring file, counted in the FQ-002 row's run) | PASS — 90 passed (tests/ui/test_project_status_model.py + test_project_status_panel.py, `-n 8` xdist, 1.52s); the MainWindow wiring tests passed as part of the FQ-002 run below (155 passed) | NOT RUN — standing user instruction for this session: never run the full pytest suite (broken/unusably slow in this checkout, 10-30 min with no usable conclusion). Consequence: this feature moves files out of the repo-root `images/` directory, and nothing in the targeted list proves that no *other* part of the app (or the packaging config) still points at the old location — that is exactly the kind of breakage a full run would surface and this run cannot. The new Database-menu entry also changes the menu bar, which BUG-027's menu-walk toolbar enumeration reads at runtime; `tests/ui/test_toolbar.py` was run tonight (FQ-004 row) and is green, but no menu-contents test outside this list was re-run | 9aa14ca, 7b0588d, 484ef64, 4abaf61 |
| 2026-08-06 | FQ-002 "Create a new trigger/function/procedure from the DDL Explorer" (docs/FEATURE_QUEUE.md FQ-002). Pure renderer `db/ddl_skeleton.py` (golden-text skeletons for trigger/function/procedure; canonical event ordering independent of call order and stable from an unordered set; duplicate events collapsed; every declared timing/level/event accepted; `void` detected case-insensitively and omitting the return stub; precision/array return types allowed; procedures never emitting a RETURNS clause; and **hostile identifiers refused rather than escaped** — including a return type crafted to escape the statement — with deterministic output). `NewTriggerDialog` + `NewRoutineDialog`: the offered timings/levels/events are asserted to be **exactly** the emitter's, so the dialog cannot drift from the renderer; the dialog's rendered skeleton is asserted to equal the emitter's for the current field state rather than being re-specified; OK gated on completeness (empty/whitespace-only name, no event selected, unchecking the last event, function without a return type), accept refused while invalid, hostile input surfacing as inline validation rather than a raised exception, and the skeleton raising rather than emitting half-formed SQL. Tree context-menu creation entries via the new non-modal `_menu_for_item` seam, which **carves out §18.1's "table nodes have no context menu" invariant precisely**: a table node still offers no Edit… (the §18.1 test is unchanged and still green) but now offers Add Trigger; the Routines branch offers New Routine; the Tables branch root offers nothing. MainWindow wiring: only trigger-returning routines offered as trigger functions (empty is empty, not an error), the created ref taking its schema from the table / the dialog's kind / defaulting to `public` when unqualified, the accepted dialog opening a tab holding the skeleton, and the new object registered in `ProjectSettings.deployed` with the empty-`content_hash` sentinel so §18.3's drift pipeline sees it — plus the negative case, no project means no manifest write. Coverage reviewed against every clause and judged complete; no gaps worth filling and no implementation bugs found, so no test file was modified in this pass | tests/db/test_ddl_skeleton.py (29), tests/ui/test_new_trigger_dialog.py (21), tests/ui/test_new_routine_dialog.py (17), tests/ui/test_ddl_creation_wiring.py (12), tests/ui/test_ddl_buffer_panel.py (extended by the implementer with the three `_menu_for_item` cases alongside the retained §18.1 no-Edit test) — all pre-existing, read and re-verified, not modified | PASS — 155 passed (tests/db/test_ddl_skeleton.py + tests/ui/test_new_trigger_dialog.py + test_new_routine_dialog.py + test_ddl_buffer_panel.py + test_ddl_creation_wiring.py, `-n 8` xdist, 2.73s) | NOT RUN — standing user instruction for this session: never run the full pytest suite (broken/unusably slow in this checkout, 10-30 min with no usable conclusion). Consequence, and this is the highest-exposure of tonight's five: `849d4ae` restructured `BrowserPanel`'s context-menu construction into `_menu_for_item`, and the DDL Explorer tree is consumed by §18.1/§18.2/§18.5 wiring tests (`test_ddl_explorer_wiring.py`, `test_ddl_project_wiring.py`, `test_ddl_object_editor_wiring.py`) that were **not** re-run in this pass; `484ef64` additionally writes to `ProjectSettings.deployed`, whose readers (`compute_drift_markers`, the §18.2 deploy reminder on close) are covered only in `tests/db/test_ddl_project.py`, also not re-run. A regression in either seam would be invisible to this row | 9f7c7c2, 11d230d, 849d4ae, 484ef64 |
| 2026-08-06 | FQ-001 "Test buttons for both connections in the Project Settings dialog's Connections tab" (docs/FEATURE_QUEUE.md FQ-001; CONSOLIDATED_SPEC.md §18.2, the block inserted after the "Layout: a QTabWidget, four tabs" paragraph) — verification pass on an implementation that arrived with its own tests. Each of the two `QGroupBox`es on the Connections tab gains a `Test` button + inline colored `QLabel`, added by the new static helper `_add_test_row(group, slot)`; the two groups deliberately wire **different** slots, because the two connections have different success conditions. **Target** runs the generic connectivity check (`db/introspect.py::test_connection`), green `Connected.` / red driver message, identical to `ConnectionSetupDialog.test`. **Sandbox** runs the superuser capability probe (`db/sandbox.py::probe`) through `_apply_sandbox_probe_result`, reproducing `NewProjectDialog`'s four-way ladder verbatim in order — `probe_error` → not-superuser → `WITH_DATA` with `pg_dump`/`pg_restore` missing (naming which binary) → green "Connected — superuser." — minus the `_last_probe` retention, which has no reader in this dialog. A single shared generic connectivity test for both groups was considered and explicitly rejected in the spec: it would false-green a sandbox that connects fine as a non-superuser and then fails at `CREATE EXTENSION` provisioning time (§18.5 D2). Both tests read the **currently typed** field values via the new public seams `target_params()` / `sandbox_params()` / `sandbox_mode()`, never the saved `ProjectSettings`; `settings()` was refactored to reuse the same three, so the round-trip path and the test path cannot drift apart. Injection follows the two source dialogs: `tester`/`prober` are defaulted keyword arguments and `self._run_async = run_async` is a plain attribute, so the single construction site (`main_window.py:3038`) needed no change. Coverage reviewed against every spec clause: target green/red/raised-error and reading live fields; each of the four sandbox ladder rungs including `SCHEMA_ONLY` correctly *ignoring* missing `pg_dump`/`pg_restore` (the branch most likely to regress into a false red) and the raised-error path; sandbox reading live fields; in-flight Test-button disabling; and both new widget pairs being descendants of the Connections tab rather than merely existing. `_apply_sandbox_probe_result` was diffed line-by-line against `new_project_dialog.py:252-278` and is character-identical apart from the dropped `_last_probe` assignment. No implementation bugs found; no additional tests judged necessary, so no test file was modified in this pass | tests/ui/test_project_settings_dialog.py (extended by the implementer with 13 tests + a `_sync_run` synchronous `_run_async` stub, taking the file 23 -> 36; not further modified by this verification pass) | PASS — 115 passed (tests/ui/test_project_settings_dialog.py 36 + tests/ui/test_ddl_project_wiring.py 79, `-n 4` xdist, 18.75s) | NOT RUN — standing user instruction for this session: never run the full pytest suite (broken/unusably slow in this checkout, 10-30 min with no usable conclusion). Verification for FQ-001 is the targeted run in the previous column only. Consequence: regressions this change could cause **outside** those two files are unverified — the realistic exposure is narrow, since `project_settings_dialog.py` is the only implementation file touched and its one construction site (`main_window.py:3038`) is unchanged by design (both new constructor parameters are defaulted), but the `settings()` refactor onto the new `target_params()`/`sandbox_params()`/`sandbox_mode()` seams is on the project-save path and is only covered here by this file's own round-trip tests | (uncommitted) |
| 2026-08-06 | Bugfix batch of 4 (docs/BUGFIX_QUEUE.md BUG-021 reopened, BUG-026, BUG-027, BUG-028) — verification pass on an already-substantially-tested implementation. **BUG-021 (reopened):** `QAction.triggered` emits `checked: bool`, which bound positionally to `_open_ddl_project`'s optional `on_ready` parameter; `on_ready=False` passed the `is not None` guard and the code then called `False()`, so the `.pgtp` auto-open after project open was dead/raising on the only user-reachable path. Fixed by lambda-wrapping both project actions' `triggered` connections and hardening the guards from `is not None` to `callable(on_ready)`. Regression tests deliberately drive the *real* signal via `action.trigger()` rather than calling the slot directly, since a direct call cannot reproduce the bug. **BUG-026:** `db/compare.py` gains `xml_table_role_counts`; `TableCheck` gains `page_count`/`detail_count`/`lookup_count`; `check_db_against_xml`'s `ok` becomes `(page + detail + lookup) > 0` so a table referenced only as a lookup source stops being reported as a red DB→XML mismatch. `DbCheckPanel` renders `(P# D# L#)` on the DB→XML side while the XML→DB side keeps its existing `(xN)` count suffix. **BUG-027:** Customize Toolbar widened from a 7-entry hardcoded registry to the full set of menu commands, enumerated by walking the live menu bar; command ids are derived from the menu path; the toolbar hosts the menus' own `QAction` objects (so enable/disable and checked state stay in sync automatically); previously-saved toolbar layouts keep working through `LEGACY_ID_ALIASES`. **BUG-028:** the Caption Management active-filter banner now also represents the whole-row find filter, via a new `_find_filter_descriptor()`, backed by new proxy getters `find_mode()` / `find_case()`. Coverage checked per fix: signal-driven regression for both project actions plus the `callable()` guard rejecting a non-callable, role-count arithmetic including the lookup-only ok-flag flip and the two distinct panel suffix formats, menu-walk enumeration/id derivation/legacy alias migration/shared-QAction identity, and banner descriptor text for each find mode and case-sensitivity combination plus its absence when no find filter is active. No implementation bugs found in this pass; every added test passed against the implementation as written | tests/db/test_compare.py, tests/ui/test_toolbar.py, tests/ui/test_toolbar_registry.py, tests/ui/test_db_check_panel.py, tests/ui/test_caption_management_panel.py, tests/ui/test_ddl_project_wiring.py, tests/ui/test_customize_toolbar_dialog.py, tests/ui/test_icons.py (all 8 extended, ~+29 tests across the batch: the batch's targeted count went 279 -> 308) | PASS — 308 passed (the 8 files above, `-n 8` xdist, 14.59s) | NOT RUN — standing user instruction for this session: never run the full pytest suite (it is broken/unusably slow in this checkout, 10-30 min with no usable conclusion). Verification for this batch is the targeted run in the previous column only; cross-suite regression risk for these 5 implementation files is therefore unverified | (uncommitted) |
| 2026-08-05 | §18.1 "Tables branch widened to every table, plus click-to-Properties" (CONSOLIDATED_SPEC.md §18.1, the 2026-08-05 addendum right before §18.2) — verification pass on top of an already-substantially-tested implementation. `db/introspect.py`: `ColumnInfo.comment: str | None = None`, sourced via `pg_catalog.col_description(a.attrelid, a.attnum)` added to `_COLUMNS_SQL` and threaded through the one shared `_build_tables` assembly point (both `fetch_schema` and `fetch_routines_and_triggers` callers gain it for free). `ui/ddl_buffer_panel.py::BrowserPanel._build_tables_branch` now unions `schema.tables` with the existing `schema.triggers`-derived grouping — every table gets a node (bare leaf if no triggers, existing `(N)`-suffixed/nested presentation unchanged if it has triggers); new `table_selected = Signal(object)` on table-node click, carrying its `TableInfo`, distinct from the span-driven `navigate_requested`. `ui/properties_panel.py`: new `_rows_for_ddl_table(table) -> list[RowSpec]` (two rows per column — identity line name/type/nullability, detail line default/comment — both `target_line=None`), registered under `_ROW_BUILDERS["ddl_table"]`, with alternating shading per column-pair. `ui/main_window.py` wires `table_selected` to `self.properties_panel.show_node(table_info, "ddl_table")`. Verified against the spec's stated acceptance points: triggerless tables get a plain leaf (no `(N)` suffix, no children); trigger-owning tables keep exactly their prior presentation; the two data sources union correctly including a triggers-only table with no `TableInfo`; sort order by table name; table click emits `table_selected` and never `navigate_requested`, even for a trigger-owning table's own row (as opposed to its nested trigger leaves); end-to-end through the real `MainWindow`/`BrowserPanel` wiring the click populates the shared `PropertiesPanel` and leaves the center editor's tab/caret untouched (unlike a routine/trigger leaf click); `_rows_for_ddl_table`'s row content/order/non-navigation and the panel's alternating pair-shading (and that non-`ddl_table` kinds get no shading) were all already covered. **One genuine gap found and closed:** the spec's explicit "click-only, no context menu" acceptance point (a whole table has no single `DdlObjectSpan` for Edit…/Check Out to act on) had no direct test — table nodes carry only the new `_TABLE_ROLE`, never `_SPAN_ROLE`, so the existing span-gated `_on_context_menu` already no-ops correctly, but nothing proved it the way the sibling argument-leaf case already was. Added one test mirroring that existing pattern. No implementation bugs found — the gap-filling test passed against the implementation as written on the first run | tests/ui/test_ddl_buffer_panel.py (extended, +1 this pass: `test_context_menu_on_a_table_node_offers_no_edit`; the other 8 §18.1 tests already present — plain-leaf, union, sort, click emits table_selected not navigate_requested, trigger-owning table still emits table_selected, empty-schema safety — read and re-verified, not duplicated); tests/db/test_introspect.py, tests/ui/test_properties_panel.py, tests/ui/test_properties_panel_rows.py, tests/ui/test_ddl_explorer_wiring.py (pre-existing coverage for `ColumnInfo.comment`/`col_description` SQL wiring, `_rows_for_ddl_table` row content/order/non-navigation, alternating pair-shading, and the end-to-end MainWindow click-to-Properties + no-center-editor-jump wiring — read and re-verified, not duplicated) | PASS — 176 passed (tests/db/test_introspect.py + tests/ui/test_properties_panel.py + tests/ui/test_properties_panel_rows.py + tests/ui/test_ddl_buffer_panel.py + tests/ui/test_ddl_explorer_wiring.py) | PASS — 2996 passed, 45 skipped, 0 failed (`-n 10` xdist, 267.78s) | (uncommitted) |
| 2026-08-05 | 8-bug batch from `docs/BUGFIX_QUEUE.md` on branch `ddl-editing`: BUG-009 (Linux/KDE generic app icon — `apply_app_identity(app)` in `main.py` sets applicationName/organizationName/desktopFileName/windowIcon; new `resources/app_icon.{png,svg}` + `packaging/linux/`), BUG-018 (caption-mode row selection silently failing under a latched global Shift modifier — `_select_first_visible_row` now drives `QItemSelectionModel` directly instead of the gesture-interpreting `QTableView.selectRow`; new autouse `_reset_keyboard_modifiers` fixture), BUG-020 (invisible preset row-predicate filter in Caption Management — `set_row_predicate` gained a `label` param plus a visible active-filter banner), BUG-021 (opening a §18.2 project now auto-opens its linked `.pgtp` via `_auto_open_linked_pgtp`, handling zero/one/multiple-candidate cases), BUG-022 (Open Project folder chooser now passes `ShowDirsOnly` and gates on a new `is_project_dir()` marker check), BUG-023 (Caption-mode Unify gained a 3-way Filtered/Project-wide/Cancel scope prompt — `_confirm_unify_scope` + `restrict_to` param on `unify_from_row`), BUG-024 (standalone "Connection Setup…" now gated to projectless mode only via `_connection_setup_action`/`_refresh_project_dependent_actions()`, with `_run_db_check`/`_open_ddl_explorer`'s missing-connection paths rerouted to Project Settings when a project is active), BUG-025 (Project Settings dialog restructured into a `QTabWidget` — General/Connections/Git/Deploy manifest). This was a coverage-verification pass: the implementation already shipped with substantial test coverage per bug (confirmed against each queue entry's "Test impact" section); two genuine gaps found and closed — (1) `is_project_dir()` (BUG-022) had only indirect UI-wiring coverage, no direct unit test of the pure marker-check function itself; (2) `_run_db_check`'s missing-connection reroute through the shared `_prompt_missing_connection` helper (BUG-024) was covered for its sibling `_open_ddl_explorer` call site but not for `_run_db_check` itself, in either standalone or project-active mode. No implementation bugs found — all gap-filling tests passed against the implementation as written on the first run | tests/db/test_ddl_project.py (extended, +4: `test_is_project_dir_false_for_a_brand_new_empty_folder`, `test_is_project_dir_false_when_ddlproject_dir_exists_but_has_no_settings_file`, `test_is_project_dir_true_after_save_settings`, `test_is_project_dir_false_for_a_nonexistent_path`); tests/ui/test_db_check_wiring.py (extended, +2: `test_run_db_check_without_connection_opens_standalone_setup`, `test_run_db_check_without_connection_and_project_open_reroutes_to_project_settings`); pre-existing coverage read and re-verified, not duplicated, across tests/test_main_icon.py, tests/ui/test_caption_management_panel.py, tests/ui/test_main_window.py, tests/ui/test_ddl_project_wiring.py, tests/ui/test_database_menu.py, tests/ui/test_ddl_explorer_wiring.py, tests/ui/test_project_settings_dialog.py, tests/ui/conftest.py | PASS — 392 passed (tests/test_main_icon.py + test_caption_management_panel.py + test_main_window.py + test_ddl_project_wiring.py + test_database_menu.py + test_ddl_explorer_wiring.py + test_project_settings_dialog.py + tests/db/test_ddl_project.py + test_db_check_wiring.py, `-n 8`, 33.22s) | PASS — 2975 passed, 45 skipped, 0 failed (`-n 10` xdist, 252.24s) | (uncommitted) |
| 2026-08-04 | §18.6 "Schema-aware Ctrl+Space completion in the DDL object editor" (CONSOLIDATED_SPEC.md §18.6, right after §18.5) — verification pass on top of an already-substantially-tested implementation. `db/introspect.py::fetch_routines_and_triggers` widened via a new shared `_build_tables` helper (also used by `fetch_schema`, which is confirmed byte-for-byte unchanged — its own 3-query contract/tests untouched and DB Check still calls it directly) to additionally populate `DatabaseSchema.tables`. New `db/schema_index.py::SchemaIndex` (Qt-free query surface: `known_schemas`/`known_tables`/`known_columns`/`trigger_for_function`, built once per DDL Explorer connect/refresh). New `sql/caret_context.py::resolve_caret_context` (Qt-free dotted-path / `NEW.`/`OLD.` caret resolution, string/comment/dollar-quote aware via the existing tokenizer). `_CompletionPopup` cleanly extracted (verified via `git diff` to be a pure move, zero behavior change) from `ui/xml_editor.py` into `ui/completion_popup.py`, re-exported for compat. Ctrl+Space wired into `ui/ddl_object_editor.py::DdlObjectEditorPanel` (`set_schema_index`/`schema_index()` injection, mirroring `XmlEditor.set_schema_model`) for the three spec'd contexts: schema-qualified table completion, attached-trigger `NEW.`/`OLD.` column completion via `TriggerInfo.function_name` reverse lookup, and an unattached-trigger table prompt (`QInputDialog.getItem`) whose pick lives only in the in-memory `self._unattached_trigger_table` attribute. `main_window.py`/`center_stage.py` wiring builds one `SchemaIndex` per DDL Explorer connect/refresh (`self._ddl_schema_index`), pushed into already-open tabs via `CenterStage.ddl_object_panels()` and injected into both new-tab entry points (Edit… and Check Out for Versioning). **Verified: the panel never imports `db/introspect.py`** (`ddl_object_editor.py`'s own source has no `db.introspect`/`db import introspect` reference, pinned by an existing test); **verified the unattached-trigger pick is genuinely session-only** — a fresh `DdlObjectEditorPanel` for the identical `DdlObjectRef` starts with `_unattached_trigger_table is None`, and grep confirms nothing in the pick path touches `settings.json`/a sidecar file; **verified the read-only DDL Explorer viewer (`ui/ddl_editor_panel.py::EditorPanel`) and any other `CodeEditor` consumer (`ui/code_editor.py::CodeEditorDialog`) have zero diff this session** — out of scope, untouched, exactly as spec'd. Two real gaps found and closed (not padding): (1) the unattached-trigger prompt's "nothing to pick from" branch (`SchemaIndex` built from a tables-less `DatabaseSchema`) was untested — `_prompt_unattached_trigger_table` must skip opening the modal `QInputDialog.getItem` entirely rather than opening it with an empty list; (2) no test asserted that choosing a completion is a real, `dirty_changed`-firing edit rather than some non-tracked text-insertion bypass. No implementation bugs found — both gap-filling tests passed against the implementation as written on the first run | tests/db/test_schema_index.py (pre-existing, 13, read and re-verified — not duplicated), tests/sql/test_caret_context.py (pre-existing, 14, read and re-verified), tests/ui/test_ddl_schema_index_wiring.py (pre-existing, 5, read and re-verified), tests/db/test_introspect.py (pre-existing extension covering `_build_tables`/widened `fetch_routines_and_triggers`, read and re-verified), tests/ui/test_ddl_object_editor_completion.py (extended 17 → 19: +`test_unattached_trigger_prompt_skipped_when_no_tables_to_pick` — an empty-tables `SchemaIndex` must never reach `QInputDialog.getItem` at all; +`test_choosing_a_completion_marks_the_panel_dirty` — inserting a chosen completion fires the clean→dirty `dirty_changed` transition like any other edit, not a bypass) | PASS — 93 passed (tests/db/test_schema_index.py + tests/sql/test_caret_context.py + tests/ui/test_ddl_object_editor_completion.py + tests/ui/test_ddl_schema_index_wiring.py + tests/db/test_introspect.py) | PASS — 2812 passed, 45 skipped, 0 failed (`-n 10` xdist, 181.11s) | (uncommitted) |
| 2026-08-03 | §18.2 UX-consistency batch (no new spec doc — a 4-part refinement on top of the already-spec'd §18.2 local-DDL-project feature): (1) project-related menu actions (New/Open/Close Project, Project Settings…, Deploy .pgtp) moved from the Database menu into the File menu, between "Open Recent" and "Save" (`_build_file_menu`); (2) File ▸ Open on a `.pgtp` with no DDL project active now shows a New Project…/Open Project…/Edit Standalone chooser (`_prompt_pgtp_open_mode`, a `QMessageBox.addButton(..., ActionRole)` dialog) — skipped entirely when a project is already active; (3) `_dialog_default_dir()` (active project's folder, or `""` with none open) threaded into all ~9 Open/Save-type `QFileDialog` call sites reachable from `MainWindow` (Open .pgtp, Save Project As, Export XSD, Import XSD, the Compare/Merge Two Files source+target pickers, Compare This Page/Detail With, and the "Save DDL Object" resolver); (4) window title now shows `PGTP Editor — Project: <name>` when a DDL project is active, composing with the existing `- <pgtp filename>` and dirty `*` suffixes. Implementation was already complete and substantially tested going into this pass (menu relocation, the chooser's 3 branches + no-prompt-when-active case, window-title show/hide, and 2 of the ~9 dialog-default-dir call sites — Open .pgtp, Save Project As — all pre-existing with adequate coverage, read and re-verified, not duplicated). **Gap found and closed: 7 of the ~9 threaded call sites had no test asserting the actual `directory` argument reaching `QFileDialog`** (Export XSD, Import XSD, both Compare/Merge Two Files pickers, Compare This Page With, Compare This Detail With, Save DDL Object) — existing tests for those dialogs asserted only the return-value handling, never the prefill/starting-directory wiring itself, so a call site silently regressed to a hardcoded `""`/bare filename would not have been caught. Added 7 targeted tests confirming each site passes `_dialog_default_dir()`'s folder (or `str(folder / filename)` for the two that build a filename prefix) when a project is active; one of these (`_compare_merge_two_files`'s source picker) required discovering that it short-circuits after a cancelled first dialog when no `_current_project` is open, so a second test covers the target-picker path with a project already loaded. No implementation bugs found — every gap-filling test passed against the implementation as written on the first run (after two test-authoring fixes: `_compare_page_with`/`_compare_detail_with` need a `.pgtp` with an actual `<Page>` element, since this repo's local `_VALID_PGTP` fixture here is a bare `<Pages/>`; and the compare-merge source-picker call count) | tests/ui/test_ddl_project_wiring.py (extended, 66 total, +7 this pass: `test_export_xsd_dialog_defaults_to_the_project_folder`, `test_import_xsd_dialog_defaults_to_the_project_folder`, `test_compare_merge_two_files_dialogs_default_to_the_project_folder`, `test_compare_merge_two_files_target_dialog_also_defaults_to_the_project_folder`, `test_compare_page_with_dialog_defaults_to_the_project_folder`, `test_compare_detail_with_dialog_defaults_to_the_project_folder`, `test_save_ddl_object_dialog_defaults_to_the_project_folder`); already-adequate coverage read and re-verified, not duplicated, in tests/ui/test_menus.py (`test_file_menu_contents` — File-menu contents incl. the relocated project actions between "Open Recent" and "Save") and tests/ui/test_open_project.py (`test_open_action_triggers_file_dialog` deliberately bypasses the new chooser via `window._prompt_pgtp_open_mode = window.open_project_file`, since that test's scope predates the chooser and is not meant to exercise it) | PASS — 66 passed (tests/ui/test_ddl_project_wiring.py, run twice for stability); PASS — 183 passed, 1 skipped (tests/ui/test_menus.py + test_open_project.py + test_diff_merge_entry_points.py + test_edit_xsd_tab.py + test_edit_auto_xsd_tab.py + test_ddl_object_editor.py; the 1 skip is pre-existing and unrelated) | PASS — 2760 passed, 45 skipped, 0 failed (`-n 10` xdist, 217.64s) | (uncommitted) |
| 2026-08-03 | §18.2 follow-up: fixed the re-open-redirect bug the row below reported. Added `MainWindow._resolve_pgtp_project_path(path)`, called at the very top of `open_project_file` (before `load_project`/`_read_raw_text` run, so the tree, the raw buffer and `_current_project_path` all resolve consistently) -- if `path` matches an already-linked project's `pgtp.source_path`, it resolves to `pgtp.working_copy_path` instead, on **every** open, not just the first. `_link_pgtp_to_project_if_needed` is now purely the first-time-linking case (its own docstring updated to say so); first-time linking is unaffected since `_resolve_pgtp_project_path` passes an unlinked project's path through unchanged | tests/ui/test_ddl_project_wiring.py -- the two tests the row below added now pass unmodified (no test changes needed, the fix targets only main_window.py) | PASS -- 49 passed (tests/ui/test_ddl_project_wiring.py, including both previously-failing tests); PASS -- tests/ui/test_open_project.py + test_save_project.py + test_document_state.py: 50 passed, 1 skipped (pre-existing skip, unrelated) | PASS -- 2743 passed, 45 skipped, 0 failed (`-n 10` xdist, 204.10s) | (uncommitted) |
| 2026-08-03 | §18.2 "local DDL-versioning projects" (CONSOLIDATED_SPEC.md §18.2, the 2026-08-03 wholesale rewrite superseding the earlier git-repo-based design — see §28's four 2026-08-03 ledger rows; also §18.3's "project close is a reminder point" addition and §19's "output folder default when a local project is open" addition). New `db/ddl_project.py` (`ProjectSettings`/`PgtpLink`/`DeployedObject`/`GitConfig`, one centralized gitignored plaintext `settings.json` + idempotent `.gitignore` maintenance, `routine_ddl_paths`/`trigger_ddl_path` — the deterministic `_1`-suffix overload-naming scheme, pure and ordered by argtype tuple, never by introspection row order — `parse_checked_out_header` + `reconcile_routine_paths` for header-based rename detection, `content_hash`, `DriftMarkers`/`compute_drift_markers`). New `db/sandbox.py` slice (`SandboxCapabilities`+`probe()` only — verified `SandboxSession`/`build_baseline_sql`/the provisioning ladder are genuinely absent, matching the module's own docstring-stated scope). New `ui/new_project_dialog.py` (`NewProjectDialog`: folder + optional local-sandbox connection with a superuser-verifying Test button reusing `sandbox.probe` + optional inert git fields) and `ui/project_settings_dialog.py` (`ProjectSettingsDialog`, full-JSON-exposure). `ui/center_stage.py` gained an optional `key` override on `open_ddl_object_tab`/`update_ddl_object_tab` so a checked-out object keys on its resolved absolute `ddl/*.sql` path. `ui/ddl_buffer_panel.py`/`ui/ddl_editor_panel.py` gained `checkout_requested` + a "Check Out for Versioning" context-menu entry (second §18.5 D1 entry-point variant), and `BrowserPanel.set_schema` gained `drift_markers` rendering combinable `*`/`!` text. `ui/main_window.py`: New/Open/Close Project + Project Settings + Deploy .pgtp Database-menu actions, `_require_ddl_project` (Create…/Open…/Cancel gate), `_link_pgtp_to_project_if_needed`, `_is_ddl_project_pgtp_working_copy` (the `.bak`-suppression check inside `_write_project_text`), `_checkout_and_edit`/`_ddl_checkout_relpath`/`_report_ddl_checkout_drift`, `_offer_pgtp_deploy_on_close`/`_remind_pending_ddl_deploys_on_close`, `_project_output_folder_default` (§19, confirmed genuinely wired into both `getExistingDirectory` call sites in the Generate PHP flow, not spec-only as suspected going in), and `_open_ddl_explorer` computing drift markers via `compute_drift_markers` when a project is open. **Verified: no-`.bak` scoping is precisely gated** (`_is_ddl_project_pgtp_working_copy` compares against the tracked working-copy path only) and does not leak into no-project-mode `.pgtp` saves (`test_saving_a_no_project_pgtp_still_writes_bak_as_before` still passes). **Verified: the "never silently overwrite a checked-out local file from the live DB" invariant holds** — `_checkout_and_edit` reads from disk whenever the file already exists and only ever seeds from the live definition on first checkout. **One implementation bug found, reported below, not fixed here** (see next column) | tests/db/test_ddl_project.py (new, 50 tests), tests/db/test_sandbox.py (new, 13), tests/ui/test_new_project_dialog.py (new, 17), tests/ui/test_project_settings_dialog.py (new, 15), tests/ui/test_ddl_project_wiring.py (new, extended by this pass from 47 to 49 — added `test_reopening_the_same_already_linked_source_stays_on_the_working_copy` and `test_reopening_the_same_linked_source_then_saving_still_writes_no_bak`, both currently **failing** and deliberately left red per the "report, don't fix" triage rule), plus already-adequate extensions (read and re-verified, not duplicated) to tests/ui/test_center_stage.py, tests/ui/test_ddl_buffer_panel.py, tests/ui/test_ddl_editor_panel.py, tests/ui/test_ddl_explorer_wiring.py, and tests/ui/test_generation.py (the §19 output-folder-default coverage, present despite the dispatch prompt's suspicion it might be missing). **Implementation bug found and reported to the caller, not fixed here:** re-opening the SAME already-`.pgtp`-linked project's sshfs-mounted source path a second time (e.g. via File ▸ Open / Recent Files pointing at the source again) silently repoints `_current_project_path` back to the **source** path instead of staying on the working copy — `_link_pgtp_to_project_if_needed` correctly no-ops (the link's `working_copy_path` is unchanged in `_ddl_project_settings`), but `open_project_file` unconditionally sets `self._current_project_path = str(path)` (main_window.py, in `open_project_file`, right before the `_link_pgtp_to_project_if_needed()` call) using the caller-supplied `path` — which on this second call is the source, not the working copy — and nothing afterward corrects it back. Consequence, reproduced end-to-end: the very next Ctrl+S/Save writes straight to the sshfs-mounted source **with a `.bak` sidecar**, defeating the entire no-`.bak`/working-copy safety model this feature exists to build. Expected per spec ("Ordinary saves … write to this working copy, and there is deliberately no `.bak`"): `open_project_file` must detect that `path` matches an already-linked project's `pgtp.source_path` and redirect `_current_project_path` (and ideally the loaded buffer) to `pgtp.working_copy_path` instead, on every open, not just the first | PASS — 258 passed (first targeted run, before the two new tests were added: tests/db/test_ddl_project.py + tests/db/test_sandbox.py + tests/ui/test_new_project_dialog.py + tests/ui/test_project_settings_dialog.py + tests/ui/test_ddl_project_wiring.py + tests/ui/test_center_stage.py + tests/ui/test_ddl_buffer_panel.py + tests/ui/test_ddl_editor_panel.py + tests/ui/test_ddl_explorer_wiring.py); PASS — 22 passed (tests/ui/test_generation.py, confirming §19's output-folder wiring). After adding the two bug-reporting tests: tests/ui/test_ddl_project_wiring.py alone = 47 passed, 2 failed (the two new ones, as intended) | FAIL — 2741 passed, 45 skipped, 2 failed (`-n 10` xdist, 192.75s): `tests/ui/test_ddl_project_wiring.py::test_reopening_the_same_already_linked_source_stays_on_the_working_copy`, `tests/ui/test_ddl_project_wiring.py::test_reopening_the_same_linked_source_then_saving_still_writes_no_bak` — both are the newly-added tests documenting the bug above; no other test in the 2741-test suite regressed | (uncommitted) |
| 2026-08-02 | §18.5 v1 "DDL object editor" tab (CONSOLIDATED_SPEC.md §18.5, D1 + carve-outs; `docs/superpowers/plans/2026-08-02-ddl-object-editor-and-sandbox.md`) — editable per-object DDL tab layered onto §18.1's read-only DDL Explorer: new `ui/ddl_object_editor.py` (`DdlObjectRef`, `DdlObjectEditorPanel` — editable `CodeEditor` + own `FindReplaceBar`, dirty tracking, injected `resolve_save_path`/`remember_save_path` seam, Ctrl+Z/Ctrl+Y native-undo carve-out via an eventFilter on its own editor, Ctrl+Alt+F Format Selection with transient refusal underline + `format_refused` signal); `ui/center_stage.py` dynamic per-object tabs (key→widget map always appended after the fixed set, `ddl_object_close_requested`); `ui/ddl_buffer_panel.py` tree right-click ▸ Edit… + shared `resolve_edit_target` helper + `edit_requested`; `ui/ddl_editor_panel.py` read-only buffer's own right-click ▸ Edit…, retaining `_spans`/`_schema`; `ui/main_window.py` wiring (`_on_ddl_edit_requested`, Ctrl+S/Ctrl+F/bookmark dispatch, Save-As-on-first-save, close-confirmation incl. "cancelled Save As aborts the close", `[SQL]`-prefixed Audit reporting for Format Selection refusals per carve-out 6). Only v1 scope: the editable tab + Save As + Format Selection + the six settled carve-outs; **no sandbox/apply/Check button row, no §18.2 project coupling, no Find All** (explicitly out of scope, confirmed absent by reading D2/D3 and the code). **Verification finding, reported to the caller, no implementation change made:** the dispatching prompt's "mandatory Ctrl+Z-native-undo regression test" in `test_ddl_object_editor_wiring.py` does NOT actually discriminate the panel's eventFilter existing from it being absent — verified by temporarily removing `DdlObjectEditorPanel.__init__`'s `self.editor.installEventFilter(self)` call and re-running: the wiring test, the panel-level `test_ctrl_z_uses_the_editors_own_native_undo`, and a hand-written repro against the real `MainWindow` all still passed byte-for-byte identically. Root cause: under this Qt/PySide6 version (offscreen platform), a bare `QPlainTextEdit` already claims the `ShortcutOverride` for its own standard Ctrl+Z/Ctrl+Y bindings before any custom eventFilter is relevant, so the window-level ambiguous `QShortcut` never gets a chance either way — the carve-out-1 "silent trap" the spec describes is not reproducible via `QTest.keyClick`/`sendEvent` in this environment, with or without the panel's own code. The eventFilter code is not wrong, just not provably load-bearing by that particular test. Added true mechanism-level tests instead (see next column) that call `panel.eventFilter(...)` directly and DO fail when that logic is broken (verified by injecting a deliberate break and observing 3 failures, then restoring) | tests/ui/test_ddl_object_editor.py (extended, 34 → 40: +2 mechanism-level `test_event_filter_claims_ctrl_z_shortcut_override`/`_ctrl_y_shortcut_override` calling `panel.eventFilter()` directly; +1 `test_event_filter_ctrl_z_key_press_calls_the_panels_own_undo` proving the KeyPress branch drives the editor's own undo, never MainWindow's `_undo`; +1 `test_shortcut_override_claims_ctrl_alt_f` — genuinely discriminating since `QPlainTextEdit` has no native binding for that sequence, verified to FAIL if `installEventFilter` is removed; +1 `test_shortcut_override_does_not_claim_unrelated_keys` guarding against a blanket-accept; +1 `test_find_all_is_inert_and_never_reports_a_false_zero_matches`, carve-out 3). tests/ui/test_ddl_object_editor_wiring.py (extended, 12 → 13: strengthened `test_ctrl_z_with_ddl_object_tab_focused_touches_only_its_own_buffer`'s docstring to state the QPlainTextEdit-native-claim caveat honestly and added a `window._history.current_index` assertion — a stronger, `_undo()`-invocation-specific check alongside the existing byte-identical-text assertion; +1 new `test_reopening_ddl_explorer_leaves_open_object_tabs_untouched`, carve-out 5 — verified to FAIL when a deliberate violation (reloading open object tabs from `on_result`) is injected, then restored). tests/ui/test_center_stage.py, tests/ui/test_ddl_buffer_panel.py, tests/ui/test_ddl_editor_panel.py already extended by the implementing session with adequate coverage (dynamic-tab map/append-after-fixed-set/close-fallthrough; right-click context-menu construction via the established `_FakeMenu`/`_build_context_menu_at` no-real-`.exec()` idioms on both entry points; argument-leaf-offers-no-Edit; caret-moves-to-click-first) — read and re-verified, not duplicated | PASS — 221 passed (test_ddl_object_editor.py 40 + test_ddl_object_editor_wiring.py 13 + test_center_stage.py + test_ddl_buffer_panel.py + test_ddl_editor_panel.py + test_ddl_explorer_wiring.py + test_main_window.py) | PASS — 2577 passed, 45 skipped, 0 failed (`-n 10` xdist, 140.41s) | (uncommitted) |
| 2026-08-02 | §18.5 v1 follow-up: Format Selection's missing context-menu entry (`manual-maintainer` spotted the drift while documenting the feature above — CONSOLIDATED_SPEC.md §18.5 says "Ctrl+Alt+F, plus a context-menu item in this tab", but the just-shipped code only bound the shortcut). Added `DdlObjectEditorPanel._build_context_menu()` (extends `CodeEditor.createStandardContextMenu()` with a "Format Selection" entry, enabled only with a selection — same gate as the shortcut) and a `QEvent.Type.ContextMenu` branch in the existing eventFilter that execs it; manual.md's Format Selection paragraph corrected to mention both trigger paths | tests/ui/test_ddl_object_editor.py (extended, 40 → 43: menu carries exactly one "Format Selection" entry; disabled without a selection; enabled + actually reformats when triggered with one) | PASS — 43 passed (tests/ui/test_ddl_object_editor.py) | PASS — 2580 passed, 45 skipped, 0 failed (`-n 10` xdist, 144.32s) | (uncommitted) |
| 2026-08-02 | Deployment SQL generation — Tasks 1–3, the pure engine (`docs/superpowers/plans/2026-08-02-deployment-sql-generation.md`; CONSOLIDATED_SPEC.md §18.3). New `pgtp_editor/db/schema_diff.py` (`SchemaDifference{kind, object_kind, identity, old_def, new_def}` + `language` sibling; `diff_schemas(source, target) -> SchemaDiffResult`, a `list` subclass carrying `.unsupported`) and `pgtp_editor/db/migration_gen.py` (`generate_migration(differences, *, header="") -> str`, `UnsupportedDifference`, `connection_summary`). Task 4 (UI/menu/drift gate/preview) deliberately NOT implemented and out of scope for this run. **Implementation changed during this verification pass, on coordinator direction:** `SchemaDiffResult` was leaking its `.unsupported` sidecar under ordinary list operations (`result[:n]`, `a + result`, `.copy()` all returned a bare `list`, silently dropping the "table/column changes were not compared" notice — same silent-wrong-result class as the overload trap). Added `__getitem__`(slice)/`__add__`/`__radd__`/`.copy()` overrides carrying (and, result+result, unioning) the sidecar; `sorted()`/comprehensions cannot be fixed and are now documented in both `SchemaDiffResult` and `diff_schemas` docstrings plus pinned by a test. `diff_schemas(a, b) == []` equivalence preserved, so §18.3's signature is not overridden | tests/db/test_schema_diff.py (extended 22 → 35: identity comes from the object not the `DatabaseSchema` dict key — the key is `schema.name`, which cannot represent overloads; misleading/bogus dict keys never reach the identity; the removed/added argtype pair each carries only its own side's def; result is a real list with the sidecar; tables present on one side only, and non-`table` kinds like views, still land in `unsupported`; +7 sidecar-survival tests for slice/copy/concat-in-either-operand-order/union-of-two-results/plain-list equality, and one pinning that comprehension+`sorted()`+`list()` drop it by design). tests/db/test_migration_gen.py (extended 30 → 34: each trigger's `DROP TRIGGER IF EXISTS` stays paired immediately before its own create when several are emitted, rather than being hoisted into one block; an added routine emits no DROP at all; `connection_summary` never *reads* the `password` attribute — asserted with a property that raises if touched, stronger than "the string is absent"; no ConnectionParams password reaches the script by any header path while host/port/db/user do). tests/db/test_deployment_engine.py (**new**, 15: the `diff_schemas` → `generate_migration` seam, which is where the load-bearing guarantees are only observable end-to-end — R14 argtype change reaches the script as "create the new signature" + *commented* `-- DROP ROUTINE pr.calc_total(integer);` with every line mentioning the old signature verified to be a comment and no `CREATE OR REPLACE` of the old one; two live overloads migrated independently; untouched overload not re-emitted; full pipeline ordering routines → triggers → review blocks; identical schemas yield header-only output; the non-PL/pgSQL warning counts only emitted routines; table differences never reach the script yet are reported via `.unsupported`; a `column` difference mixed into a routine batch refuses the whole script; the realistic UI path — slice then generate — keeps the omission notice; **byte-determinism across processes** under `PYTHONHASHSEED=0` vs `12345`, which an in-process "run it twice" check cannot see because both modules iterate sets; static AST purity for both modules — no PySide6/PyQt/psycopg/socket/urllib, no `db.config` (it imports QSettings), no upward `pgtp_editor.ui` import, and no `datetime`/`time`/`pathlib`/`os`/`io` so "caller supplies the timestamp" is enforceable; fresh-interpreter import loads no psycopg). **Non-vacuity verified by mutation:** degrading `routine_identity` to `schema.name` → 16 failures; dropping `sorted()` in `_diff_group` → 5; unsorted `unsupported` union → the cross-process determinism test alone; uncommenting the review DROP → 4. Implementation restored and re-verified green after each mutation | PASS — `tests/db/test_schema_diff.py tests/db/test_migration_gen.py tests/db/test_deployment_engine.py`: 84 passed (35 + 34 + 15); whole `tests/db`: 160 passed (baseline before this pass: 128) | PASS — 2496 passed, 32 skipped, 0 failed (`-n 10` xdist, 162s). **One spec deviation found and NOT fixed here** (reported to the caller instead, no failing test added since the root cause is in an unchanged file): the two modules are psycopg-free and Qt-free *by direct import*, but `import pgtp_editor.db.schema_diff` still loads `PySide6.QtCore` transitively via `schema_diff → db.introspect → db.config (QSettings)` — 17 PySide6 modules in a fresh interpreter. `schema_diff` uses `DatabaseSchema`/`RoutineInfo`/`TriggerInfo` in annotations only and has `from __future__ import annotations`, so a `TYPE_CHECKING` guard would close it with no behaviour change; the runtime Qt-freedom assertion (mirroring `tests/sql/test_package_purity.py::test_importing_the_package_loads_no_pyside6_module`) is ready to add once that is decided | (uncommitted) |
| 2026-08-02 | BUG-017 fix (`docs/BUGFIX_QUEUE.md`) — parallel-gate flake and hard segfaults caused by a TEST, not by production code: `tests/ui/test_xml_editor.py::_count_highlight_calls` did `monkeypatch.setattr(module.XmlSyntaxHighlighter, "highlightBlock", counting)`. PySide6 binds virtual overrides **per instance at construction**, so every highlighter built under the patch kept a raw pointer to the `counting` closure that `monkeypatch.undo()` then freed; BUG-013's theme sweep, reaching editors leaked by pytest-qt teardown, later called through it — producing the masked nonsense `TypeError: Error calling Python override of QSyntaxHighlighter::highlightBlock(): <whatever landed in the freed slot>() takes 0 positional arguments but 2 were given` in unrelated tests, or segfaulting the xdist worker (exit 139). Fix is fix-proposal (1)+(2) only: the helper now patches the module-level NAME `pgtp_editor.ui.xml_editor.XmlSyntaxHighlighter` with a Python subclass overriding `highlightBlock` (override lives in the subclass `__dict__`; the instance keeps its type alive), with a docstring stating why to stop reintroduction. **No production code changed** — proposal (3) (sweep throttling) deliberately not taken | tests/ui/test_xml_editor.py (extended, 1 file, no new files): rewrote `_count_highlight_calls`; added `assert calls["n"] >= 1` to both BUG-016 tests (`test_quote_in_text_content_does_not_rehighlight_the_document`, `test_unterminated_quote_inside_a_tag_resyncs_at_the_next_tag`) before the counter reset, so their existing `<= 2`/`<= 4` bounds can no longer pass vacuously if the hook stops taking effect; +2 guard tests — `test_count_highlight_calls_never_patches_the_shiboken_class` (state half: counter works, yet `XmlSyntaxHighlighter.highlightBlock` is untouched and the editor's highlighter is a strict subclass instance) and `test_counting_highlighter_override_survives_monkeypatch_undo` (lifecycle half, added by this pass: builds the editor inside `monkeypatch.context()`, and after the undo asserts the override is still reachable via the instance's own type BEFORE calling `rehighlight()` — so a regression fails loudly instead of crashing the worker, and the post-undo rehighlight that used to segfault is exercised). Both guard tests verified to FAIL (`AssertionError`, no crash) when the old class-patching helper is temporarily reinstated | PASS — `tests/ui/test_xml_editor.py tests/ui/test_xml_editor_theme.py`: 112 passed (the exact BUG-017 repro that gave `1 failed, 107 passed, 2 errors`); `tests/ui/test_xml_editor.py tests/ui/test_xml_editor_nav_perf.py tests/ui/test_xml_editor_theme.py tests/ui/test_xml_structure.py`: 165 passed, 3/3 runs (previously `Fatal Python error: Segmentation fault`, exit 139, 3/3) | PASS — 2412 passed, 32 skipped, 0 failed (`-n 10` xdist, ~140s), runs 2 and 3 of 3. Run 1 had one **unrelated pre-existing** failure, `tests/ui/test_main_window.py::test_see_column_in_caption_filters_and_selects_row` — the load-dependent caption-mode flake documented under BUG-012, reproducible on baseline before this work and passing in isolation; no production code was touched by this change, so it cannot be attributable to it. No segfaults and no `test_xml_editor_theme.py` / `test_xml_structure.py` errors in any of the 3 runs | (uncommitted) |
| 2026-08-02 | SQL/plpgsql selection formatter (CONSOLIDATED_SPEC.md §18.4) — fixing the 2 bugs the verification run below pinned as 7 strict-xfail tests. Both produced `ok=True` with corrupted SQL, violating §18.4's "never a silent wrong result" contract. (a) **Decomposed (NFD) accented text was shredded and respaced** — a combining mark is neither `isalpha()` nor `isalnum()`, so `select * from tábla` (NFD) came back as `from ta ́ bla` and an NFD `$tág$` was not recognized as a dollar-quote at all, leaving its body reindented as code. `sql/tokenizer.py` gains `_is_combining_mark` (Unicode categories `Mn`/`Mc`) accepted by `_is_word_char` and therefore by `_dollar_tag_at`; `_is_word_start` deliberately still rejects marks, since a mark only ever continues the letter before it. (b) **The unary `+`/`-` glue rule manufactured a `--` line comment** — `where a = - -1 and b = 2` → `where a = --1 and b = 2`, silently commenting out `and b = 2`; `v := - -- flip sign` → `--- flip sign`, dropping the sign; `+ +1` → `++1`, which Postgres lexes as one undefined operator. Fixed as a general safety net rather than per-case: `sql/formatter.py::_space_before` splits into `_wants_glue` (the layout rules) gated by the new `_glue_is_lossless`, which permits dropping a space only when `prev.text + tok.text` still re-lexes as those same two tokens — so no glue rule, present or future, can fuse tokens. Also covers `- >` → `->` and `1 .` → `1.` on junk input | The 7 pinned assertions kept verbatim as regression tests with their `@pytest.mark.xfail(strict=True)` markers removed and each reason turned into a comment: tests/sql/test_tokenizer.py (`test_decomposed_nfd_identifier_is_a_single_word_token`, `test_decomposed_nfd_dollar_quote_tag_is_one_opaque_token`); tests/sql/test_formatter.py (`test_decomposed_nfd_identifier_is_never_respaced`, `test_decomposed_nfd_dollar_quote_tag_body_is_opaque`, `test_double_unary_minus_is_not_glued_into_a_line_comment`, `test_unary_sign_before_a_line_comment_keeps_its_space`, `test_adjacent_unary_plus_signs_keep_their_space`). No new test files. Note the property suite's `code()` helper re-tokenizes the formatter's output and compares the token stream in order, so it catches "the output lexes differently from the input" as a class — the whitespace-stripped comparison it replaced was structurally blind to both bugs | PASS — 590 passed, 0 xfailed (tests/sql); 88 passed (tests/ui/test_code_editor.py + tests/ui/test_ddl_editor_panel.py) | PASS — 2401 passed, 32 skipped, 0 failed, 0 xfailed (159.97s, `-n 10` xdist) | (uncommitted) |
| 2026-08-02 | SQL/plpgsql selection formatter core (CONSOLIDATED_SPEC.md §18.4) — final verification pass after the interrupted earlier run and the rebase onto `cebb1b0`. Qt-free core only, no UI consumer/shortcut/auto-mode: `pgtp_editor/sql/{__init__,keywords,issues,tokenizer,formatter}.py` (`format_selection(text) -> FormatResult`, tokenize/balance refusal as the sole gate, `Issue` with start/end span), plus `ui/code_editor.py` re-binding `_SQL_KEYWORDS = SQL_KEYWORDS` imported from `pgtp_editor.sql.keywords` (shared dialect source; UI-name identity preserved). Independently re-verified the two previously-pinned fixes: lone-CR line counting in `_Cursor.advance_to`/`_dominant_eol`, and Unicode-letter word boundaries (`str.isalpha()`/`isalnum()`) | tests/sql/test_tokenizer.py (+13 and 5 new TRICKY corpus rows: non-BMP letter identifiers, accented `$tág$` tag opacity + non-matching accented closer, `$1`/`$10` vs. dollar-quotes, mixed CR/LF/CRLF one-break-each, `_Cursor` CRLF split across two `advance_to` chunks, line numbers through a multi-line opaque token ×7, accented identifiers next to `::`/`.`/`%TYPE`/subscripts); tests/sql/test_formatter.py (+13: accented glue rules, accented ident inside a `$$` body, accented dollar-tag body opacity, non-BMP round-trip, mixed-EOL normalization to the dominant ending + all-three-in-one, refusal spans after a multi-line opaque token ×6, CR-only span on line 3, binary-minus/block-comment spacing safety); tests/sql/test_formatter_properties.py (+9 CORPUS rows, +11 ADVERSARIAL entries: accented/non-BMP/all-three-EOL/combining-mark inputs); tests/ui/test_code_editor.py (+2: `_SQL_KEYWORDS is pgtp_editor.sql.keywords.SQL_KEYWORDS` identity — the §18.4 shared-dialect-source seam the rebase touched — and highlighter/tokenizer keyword agreement). 7 strict-xfail tests pin 2 newly found implementation bugs reported back to the main session (NFD/decomposed identifiers + NFD dollar-quote tags are shredded and respaced; unary `+`/`-` glued to a following sign or line comment manufactures `--`, silently commenting out the rest of the line) | PASS — 583 passed, 7 xfailed (tests/sql); 88 passed (tests/ui/test_code_editor.py + tests/ui/test_ddl_editor_panel.py) | PASS — 2394 passed, 32 skipped, 7 xfailed, 0 failed (169.52s, `-n 10` xdist); shared-gutter rebase seam (`ui/editor_gutter.py`, `ui/code_editor.py`) clean | (uncommitted) |
| 2026-08-01 | BUG-016 (every parity-flipping `"` keystroke re-highlighted the WHOLE document — the residual split out of BUG-015, confirmed still reproducing after that fix at 5,972 `highlightBlock` calls / 45.5 ms for one quote on a 6,002-block file): `ui/xml_editor.py::XmlSyntaxHighlighter` — the odd-quote-count rule (`_has_unterminated_quote`, now deleted) never re-synchronised its parity, so one flipped block state flipped the next in turn and Qt's cascade ran to EOF. Replaced with a TAG-AWARE four-state machine (`STATE_NORMAL`=text content, `STATE_IN_UNCLOSED_STRING`=double-quoted value, `STATE_IN_TAG`, `STATE_IN_SINGLE_QUOTED`) computed by the new method `_end_state(text, state)`, scanning only state-changing characters via `_STATE_CHARS_RE = [<>"']` for speed: a quote delimits only INSIDE a tag, so quotes/apostrophes in text content (where .pgtp keeps its PHP event-handler bodies) never change state. Plus the RESYNC rule that actually bounds the cascade — a raw `<` inside a quoted value snaps the state back to `STATE_IN_TAG`, since `<` cannot appear in a well-formed attribute value; the triage verified tag-awareness ALONE still cascades to EOF for an unterminated in-tag quote. Continuation formatting via the new `_continued_string_end(text, quote)`; the four formatting regexes are unchanged, so inline `"..."` pairs in text content stay string-colored — only the propagated STATE changed. Helpers are methods so `debuglog.py`'s `XmlSyntaxHighlighter.` qualname-prefix flood exclusion still covers them | tests/ui/test_xml_editor.py (+4: `test_quote_in_text_content_does_not_rehighlight_the_document` — the regression test, one `"` typed into a 300-element document re-highlights ≤2 blocks (was 5,972), counting via a class-level `highlightBlock` patch installed BEFORE the editor is built; `test_unterminated_quote_inside_a_tag_resyncs_at_the_next_tag` — the legitimately-continuing case stays bounded to ≤4 blocks via the `<` resync; `test_apostrophes_in_event_handler_body_do_not_string_ify_the_rest` — PHP `'it\\'s'` / `"SELECT 1"` in text content leave a later `<Page` tag-colored; `test_end_state_transitions_are_tag_aware` — the state table directly, incl. a `"` not closing a single-quoted value and vice versa, `<A` leaving quote tracking on for the next line, and the `<` resync). The two pre-existing multi-line-attribute tests (`test_unclosed_quote_propagates_string_format_to_next_line`, `test_closing_the_quote_reverts_second_line_format`) still pass unmodified — their quote is opened inside a tag, which the new machine handles identically | PASS — 104 passed (tests/ui/test_xml_editor.py); measured on a 6,002-block document, one `"` in text content 45.5 → 2.3 ms/char and 5,972 → 1 `highlightBlock` call, one `"` inside a tag → 2 calls | PASS — 1818 passed, 32 skipped (`-n 10`) | (uncommitted) |
| 2026-08-01 | BUG-015 (typing in the Raw XML editor was painfully slow — every keystroke/newline ran the full-document structure rescan AND code-region rebuild inline): `ui/xml_editor.py` — the two O(document) `textChanged` handlers (`_rescan_structure`: full `toPlainText()` copy + whole-document `xml_structure.scan`; `_refresh_code_region_selections`: a second full copy + `event_body_line_ranges` walk + per-line ExtraSelection rebuild) are now DEBOUNCED behind a parented single-shot `_rescan_timer` (`_RESCAN_DEBOUNCE_MS = 250`, `QTimer(self)` not `QTimer.singleShot` per BUG-014), driven by the new `_on_text_changed_schedule_rescan` (which also absorbs the `_applying_theme` skip) and landing in `_rescan_now` (structure → code regions → matching-tag highlight → gutter repaint). **The triage's proposed fix was incomplete and would have fixed nothing on its own**: `_update_matching_tag_highlight` is wired to `cursorPositionChanged`, which fires on every keystroke because typing moves the caret, and it rescanned whenever it found the span cache stale — i.e. once per character straight back through the cursor path. It now SUPPRESSES the highlight while stale (clearing it, since the cached spans' offsets refer to pre-edit text and would paint a visibly wrong range) and `_rescan_now` re-invokes it once the debounce lands. Two further correctness guards: `setPlainText` is overridden to rescan SYNCHRONOUSLY (a document load/revert/rename-write-through must have exact spans immediately — the debounce is only for incremental typing), and `_toggle_fold` is overridden to flush the pending rescan first (folding is deliberate and must see exact spans; the flush deliberately does NOT live in `_foldable_region_starting_at`, which the gutter's `paintEvent` calls per visible block — rescanning there would fire on every repaint and silently undo the debounce) | tests/ui/test_xml_editor_nav_perf.py (+4, and 1 existing updated: `test_cursor_navigation_does_not_rescan_document`'s tail asserted `after_edit > baseline`, i.e. the per-keystroke inline rescan this fix removes — it now asserts the keystroke adds NO scan, the timer is active, and `_rescan_now()` produces exactly one. New: `test_typing_a_burst_coalesces_into_a_single_rescan` — 20 keystrokes → 0 scans, then exactly 1 on flush, spans revision-fresh; `test_cursor_moves_during_the_debounce_window_do_not_rescan` — the subtle cursor-path half: 4 caret moves inside the stale window scan 0 times, the matching-tag highlight is empty (suppressed, not stale-drawn), and returns correct (2 selections) after the flush; `test_rescan_timer_is_parented_and_single_shot` — BUG-014 regression guard; `test_set_plain_text_rescans_synchronously` — load leaves the timer idle, spans fresh, fold regions immediately derivable); tests/ui/test_xml_editor.py (+1, 1 updated: `test_folding_right_after_an_edit_flushes_the_debounced_rescan` — fold a just-typed element while the rescan is still pending; `test_xml_editor_gutter_and_bookmark_methods_come_from_the_shared_mixin` drops `_toggle_fold` from the no-re-declaration ban list and instead asserts the override is a thin flush-then-`super()` wrapper); tests/ui/test_code_editor.py (1 updated: `test_code_editor_and_xml_editor_share_one_gutter_implementation` — CodeEditor still uses the mixin's `_toggle_fold` verbatim, XmlEditor's wrapper must delegate via `super()`) | PASS — 108 passed (tests/ui/test_xml_editor.py + test_xml_editor_nav_perf.py); measured on a synthesized 1 MB / 21,002-block document, per-character typing cost **216.1 → 2.0 ms** plain text, 209.2 → 14.1 ms newlines, 236.6 → 28.6 ms attribute-with-quotes (≈108× / 15× / 8×), scans during a 25-char burst 25 → 0 (+1 on the debounce) | PASS — 1814 passed, 32 skipped (`-n 10`) | (uncommitted) |
| 2026-08-01 | BUG-014 (unparented `QTimer.singleShot` in `XmlEditor.apply_theme_colors` fired `_rehighlight_for_theme` on an already-deleted C++ editor — the 7 Find-All failures recorded FAIL in the DDL Explorer row below): the deferred step-2 kickoff now uses a `QTimer(self)` (`_theme_kickoff_timer`, single-shot, 0ms) PARENTED to the editor, so `~QWidget` cancels a pending tick instead of letting it fire on a dead object — mirrors the already-parented `_theme_sweep_timer` | tests/ui/test_xml_editor_theme.py (+1: `test_deferred_theme_rehighlight_does_not_fire_on_deleted_editor` — asserts the kickoff timer is a child of the editor, schedules the deferred rehighlight, force-deletes the C++ object via `shiboken6.delete`, then pumps the loop twice; the bug fired a `RuntimeError` caught by pytest-qt's event-loop hook, the fix cancels it. Verified red without the fix, green with it) | PASS — 6 passed (tests/ui/test_xml_editor_theme.py) | PASS — 1809 passed, 32 skipped (171.36s, `-n 10` xdist) — the DDL Explorer batch's 7 pre-existing Find-All failures are cleared | (uncommitted) |
| 2026-08-01 | DDL Explorer enhancements (CONSOLIDATED_SPEC.md §18.1 + §8) — three slices: (1) `db/introspect.py` `RoutineInfo.args: list[tuple[str,str]]` (IN/INOUT input name+type pairs, declared order, `""` for unnamed) sourced from a widened `_ROUTINES_SQL` (`COALESCE(proallargtypes, proargtypes::oid[])` + `proargnames` + `proargmodes::text[]`) correlated by the new pure `_input_args`, with `arg_types` retained for `build_ddl_text`'s banner; (2) `ui/ddl_buffer_panel.py` tree presentation — fully-qualified `schema.name` routine top line with three-way marker `[P]`/`[T]`/`[F]`, one `name (type)` child leaf per input arg (span-less, non-navigating), empty `()` only for zero-input routines, and the composite trigger label `schema.table.trigger [timing][event]...` in BOTH branches; (3) NEW `ui/editor_gutter.py` (`GutterBookmarkFoldMixin` + `_EditorGutter`) extracted out of `XmlEditor` and mixed into BOTH `XmlEditor` and `CodeEditor`, with `_foldable_region_starting_at` as the only pluggable hook, `CodeEditor.set_fold_regions()` fed by `EditorPanel` from the `DdlObjectSpan` index (banner→end_line), 4-char tab stop and top-aligned `navigate_to_line` gated on `language == "sql"`, `xml_editor.py` re-exporting the moved symbols, and `main_window.py` passing spans through to `set_ddl_text` | Verification pass on top of the implementing session's tests (which were already substantial), filling gaps — **+48 tests, all additive, zero existing tests modified or deleted**: tests/db/test_introspect.py (+9: OUT-only signature → no inputs; VARIADIC `v` dropped; NULL/empty per-entry mode reads as IN; names/modes arrays longer than the type array cannot invent arguments; declared order preserved with an OUT interleaved (the mis-correlation trap); fresh list per call; `RoutineInfo.args` defaults to `[]` and is not shared; `_ROUTINES_SQL` really selects proargnames/proargmodes/proallargtypes; end-to-end `fetch_routines_and_triggers` with an OUT arg keeps banner `arg_types` IN-only while `args` drops the OUTs); tests/ui/test_ddl_buffer_panel.py (+10: a trigger function that has args AND callers lists args first then triggers sorted by name; only the trigger children navigate, arg leaves emit nothing; unnamed arg renders `" (integer)"`; OUT-only routine (`arg_types` non-empty, `args == []`) renders `schema.name()`; `[P]` wins over `[T]` for a trigger-returning procedure; routine top line navigates to its own banner span; `set_schema` rebuild replaces rather than appends; unknown timing/event → `[?]`; zero-event trigger renders timing only; both trigger leaves carry the byte-identical composite label); tests/ui/test_code_editor.py (+14: the shared gutter's three mouse zones on a CodeEditor host (bookmark strip toggles, fold zone folds, line-number zone is a no-op); gutter paints with bookmark tag + collapsed/expanded chevron; theme colors follow the palette and a PaletteChange event repaints; `set_fold_regions` replaces the previous set and drops fold state; empty region set; `_toggle_fold` on a non-region block is a no-op; two sibling regions fold/expand independently; top-aligned navigation accounts for folded blocks above the target; `navigate_to_line(0)` clamps; the JS/PHP `CodeEditorDialog` gains the gutter+bookmarks (approved side effect) with folding inert and its save contract intact); tests/ui/test_ddl_editor_panel.py (+9: pure `_fold_regions_for_spans` banner→end_line translation, no-body spans skipped, one-line body still folds; folding one object then expanding it leaves the sibling collapsed; buffer reload resets folds and reinstalls regions; reload without spans clears the old regions; bookmarks survive a fold and reset on reload; `navigate_to_line` focuses the editor; clicking a span puts that object's banner at the top as the first visible block); tests/ui/test_xml_editor.py (+3: `xml_editor` re-exports the SAME `_EditorGutter`/`_BOOKMARK_STRIP_WIDTH`/`_FOLD_GLYPH_WIDTH` objects; no gutter/bookmark/fold method is re-declared on `XmlEditor` — all 9 resolve to the mixin's functions; XML folding still leaves the character stream fully intact); tests/ui/test_ddl_explorer_wiring.py (+3: opening the Explorer installs exactly one fold region per object (2 routines + 1 trigger) at the right banner blocks; reopening re-fetches and reinstalls regions with no stale fold state; a tree click scrolls that object's banner to the top of the viewport) | PASS — 219 → 267 passed across tests/db/test_introspect.py, tests/ui/test_code_editor.py, tests/ui/test_ddl_buffer_panel.py, tests/ui/test_ddl_editor_panel.py, tests/ui/test_xml_editor.py, tests/ui/test_ddl_explorer_wiring.py | FAIL — 7 failed, 1795 passed, 32 skipped (`QT_QPA_PLATFORM=offscreen ./venv/bin/python -m pytest -q -n 10`). All 7 are **pre-existing on the `ddl-editing` branch and unrelated to this batch**: tests/ui/test_main_window.py Find-All tests (`test_find_all_clears_only_prior_find_entries`, `test_clicking_find_result_navigates_editor_to_line`, `test_clicking_summary_line_is_a_noop`, `test_find_all_via_menu_populates_audit_panel`, `test_find_all_streaming_completes_and_reports_final_count`, `test_find_all_restart_does_not_leak_a_second_timer`, `test_find_all_populates_audit_panel_with_line_items_and_summary`) failing with `RuntimeError: libshiboken: Internal C++ object (XmlEditor) already deleted` in `_rehighlight_for_theme`, from the earlier BUG-013 theme-rehighlight commits and already in bug-triage. Baseline at clean HEAD with the batch stashed: 7 failed, 1717 passed, 32 skipped — the exact same 7 tests. This batch adds **no new failures** (+78 passed: 48 mine + 30 from the implementing session) | (uncommitted) |
| 2026-08-01 | BUG-011 (Database Check tab stays open with stale results after File ▸ Close), resolving the `docs/BUGFIX_QUEUE.md` entry: `ui/main_window.py::_close_project` — on the committed-close path only (after `_set_dirty(False)`, below the `cancel`/cancelled-save early returns) the close now hides the Database Check left tab (`left_tabs.setTabVisible(db_check_tab_index, False)`, the established project-tied-left-tab close gesture) and resets the three project-tied caches to `None` (`_last_db_check_direction`, `_last_db_schema`, `_last_db_summary`) so `_refresh_db_check_if_open`/rename re-runs can't act on the closed project's stale state; `_revert_project` deliberately untouched (keeps the project loaded); no `DbCheckPanel.clear()` added per the queue entry's guidance | Verification pass on top of the resolving session's 2 tests in tests/ui/test_db_check_wiring.py (committed `confirm="discard"` close hides the tab + nulls all three caches; cancelled close on a dirty buffer leaves tab visible + caches intact), filling gaps: tests/ui/test_db_check_wiring.py (+4: `test_close_clean_buffer_hides_db_check_tab_and_clears_caches` — closing a CLEAN buffer (`confirm=None`, treated as discard, no modal consulted) also tears down; `test_close_via_successful_save_hides_db_check_tab` — `confirm="save"` with a succeeding save (`_save_project` seam sets dirty False) commits the close and tears down; `test_close_via_cancelled_save_keeps_db_check_tab_and_caches` — a save-cancelled close (`_save_project` no-op, dirty stays True → early return) leaves the still-open project's tab and caches untouched; `test_fresh_db_check_after_close_reveals_and_repopulates` — after close-teardown, a fresh check on a newly opened project re-reveals the tab and repopulates panel + all three caches, direction `db_to_xml`). No implementation bugs found — every gap-filling test passed against the implementation as written on the first run | PASS — 33 passed (tests/ui/test_db_check_wiring.py) | PASS — 1724 passed, 32 skipped (157.25s, `-n 10` xdist; no caption-mode or async flakes this run) | (uncommitted) |
| 2026-08-01 | BUG-008 re-verification (supersedes the FAIL entry below) after the main session fixed the reported implementation bug: `XmlEditor.resolve_attribute_at`'s parent-map ancestor chain walk now climbs past any parent-map ancestor whose `depth != walker.depth - 1` (the same depth filter `parent_tag_span` applies), so '>'-truncated unclosed spans stuck on `build_parent_map`'s stack no longer pollute later elements' tag chains | Re-ran the 5 tests from the FAIL entry — `test_resolve_attribute_at_chain_unpolluted_by_earlier_gt_truncated_span` now passes; +1 new gap test tests/ui/test_xml_editor.py::`test_resolve_attribute_at_chain_unpolluted_by_multiple_truncated_spans` — TWO consecutive truncated (unclosed) spans inside a properly closed element leave two bogus ancestors at wrong depths on the stack; the depth filter must climb past BOTH in sequence, matching `attribute_at_position` (`Root/Pages/Page`). Note: consecutive truncated SIBLINGS at top level produce depth-CONSISTENT pollution (`Root/Pages/First/Second/Page`) in the from-scratch free function too — that is pre-existing shared behavior, equivalence holds, not a regression | PASS — 118 passed (tests/ui/test_xml_editor.py + test_xml_editor_nav_perf.py + test_properties_panel.py) | PASS — 1718 passed, 32 skipped (148.06s, `-n 10` xdist; no caption-mode or async flakes this run) | (uncommitted) |
| 2026-08-01 | BUG-008 (Project-tree selection → Properties panel O(rows × all-spans × text-scan) per click), verifying the resolving session's fix in `ui/xml_editor.py`: lazy revision-guarded `_resolution_index` (spans sorted by open_start + open_start keys for bisect + `build_parent_map`), invalidated in `_rescan_structure`, built on first `resolve_attribute_at`; module-level `_containing_open_tag` (bisect_right + O(depth) backwards walk, quote-aware `_opening_tag_end` recompute preserved); `resolve_attribute_at` rewritten to indexed containing-lookup + parent-map ancestor chain; free function `attribute_value_at_position` routed through `_containing_open_tag` with unchanged signature | Verification pass per the queue entry's Test impact section: tests/ui/test_xml_editor_nav_perf.py (+1: `test_properties_population_uses_indexed_resolution_not_per_row_walks` — PropertiesPanel.show_node on a 30-attribute node WITH a schema model injected (gotcha 1) never calls `xml_structure.scan`, never calls `xml_structure.parent_tag_span`, calls `build_parent_map` exactly ONCE per revision across three populations, decorated `value — label` output unchanged (gotcha 3), and after an edit rebuilds the index exactly once); tests/ui/test_xml_editor.py (+4: indexed-vs-`attribute_at_position` equivalence on attribute positions incl. attr name token, quoted value, deep nesting, a value containing '>', the attribute AFTER a '>'-in-quoted-value in the same tag; equivalence on None positions — tag name, text content, close tag, inter-attribute whitespace, document edges; `test_resolve_attribute_at_index_invalidated_by_edit` — index built lazily, dropped on edit, fresh resolve correct and equal to the free function; `test_resolve_attribute_at_chain_unpolluted_by_earlier_gt_truncated_span` — **FAILS, implementation bug**) | FAIL — 1 failed of 98 (tests/ui/test_xml_editor.py + test_xml_editor_nav_perf.py) | FAIL — 1 failed (`tests/ui/test_xml_editor.py::test_resolve_attribute_at_chain_unpolluted_by_earlier_gt_truncated_span`), 1716 passed, 32 skipped (153.35s, `-n 10` xdist, no flakes). Bug: when the scanner truncates a tag at a '>' inside a quoted attribute value (e.g. self-closing `<Editor caption="a > b" .../>`), that span is recorded unclosed (`close_end=None`); `build_parent_map` keeps it on the ancestor stack forever, so a LATER sibling element's tag_chain wrongly inherits it (`Root/Pages/Page/Editor/Page` instead of the pre-fix `parent_tag_span` depth-filtered result `Root/Pages/Page`) — `resolve_attribute_at` diverges from `attribute_at_position` and from pre-BUG-008 behavior, which can change curated-label lookups; violates the queue entry's "preserve that robustness so behavior does not regress on tags with '>' in attribute values" | (uncommitted) |
| 2026-08-01 | BUG-010 (dark-theme menu checkable indicators invisible — Fusion derives the indicator outline from darkened Window/Button palette roles, near-black on the #2B2B2B menu), resolving the `docs/BUGFIX_QUEUE.md` entry via the user-chosen QDarkStyleSheet adoption instead of the queue entry's hand-rolled QSS options: `ui/theme.py` — `_dark_stylesheet()` lazily loads + module-caches `qdarkstyle.load_stylesheet(qt_api="pyside6")` (lazy because qdarkstyle warns if loaded before a QApplication exists); `apply_theme(app, light)` now additionally does `app.setStyleSheet("" if light else _dark_stylesheet())` — `dark_palette()` is STILL applied under the QSS (XmlEditor's `apply_theme_colors` keys off palette Base lightness) and light ALWAYS assigns `""` so a round-trip leaves no stale QSS; `tests/ui/conftest.py` autouse reset extended to save/restore `app.styleSheet()`; `qdarkstyle>=3.2` added to requirements.txt + pyproject dependencies; QDarkStyleSheet credit added to `ui/about.py::ABOUT_TEXT`. Assertion note: an app-level QSS wraps the style in Qt's internal QStyleSheetStyle proxy whose objectName is "" — dark-mode tests assert stylesheet+palette instead of `style().objectName()=="fusion"` (light mode still asserts fusion) | Independent verification pass (dispatched per CLAUDE.md's mandatory feature-tester policy) on top of the resolving session's own tests (tests/ui/test_theme.py +2: dark applies a non-empty QSS mentioning `QMenu::indicator`, light clears it; 4 existing dark-mode assertions migrated off objectName; tests/ui/test_main_window_theme.py 2 same migrations), filling gaps: tests/ui/test_theme.py (+3: `test_apply_theme_dark_light_dark_reuses_cached_stylesheet` — dark→light→dark re-applies the byte-identical QSS and `_dark_stylesheet() is _dark_stylesheet()` proves the module cache is populated once; the conftest autouse stylesheet-reset exercised by the established leak-pair pattern — `test_dark_qss_left_applied_without_local_reset` deliberately leaves the dark QSS applied with no local reset fixture, and `test_previous_tests_dark_qss_did_not_leak` next in definition order asserts `QMenu::indicator` is gone from the app stylesheet); tests/ui/test_xml_editor_theme.py (+1: `test_app_wide_apply_theme_flips_editor_both_ways` — the REAL `apply_theme()` path (Fusion + palette + dark QSS together) drives a live XmlEditor's palette-keyed changeEvent via genuine Qt event delivery in both directions, light colors then back to dark; the pre-existing tests only injected hand-built QEvents, so the QSS-wrapping-the-style interaction was untested). No implementation bugs found — every gap-filling test passed against the implementation as written on the first run | PASS — 31 passed (tests/ui/test_theme.py + tests/ui/test_xml_editor_theme.py + tests/ui/test_main_window_theme.py) | PASS — 1712 passed, 32 skipped (253.72s, `-n 10` xdist; the known `test_run_async_delivers_result_via_real_pool` / caption-mode parallel flakes did not occur this run) | (uncommitted) |
| 2026-08-01 | DDL Explorer wiring increment (spec §18.1, completing the §18 "Not yet built" list except `db/routine_refs.py`): `ui/code_editor.py` — `_SQL_KEYWORDS` + `_CodeHighlighter` "sql" mode (case-INSENSITIVE keyword matching, `--` line comments, `''`-doubled single-quoted strings with no backslash escapes, double-quoted text = identifier left un-string-styled) plus two new `CodeEditor` methods `navigate_to_line(line)` (1-based, centerCursor) and `replace_current_selection(text)` (explicit read-only guard, since QTextCursor edits bypass setReadOnly); NEW `ui/ddl_editor_panel.py::EditorPanel` (read-only sql CodeEditor + own FindReplaceBar, `set_ddl_text`, `navigate_to_line`); `ui/center_stage.py` — "DDL Explorer" tab (`ddl_tab_index`, hidden by default, closable ✕ that hides directly — read-only, no dirty prompt), `show_ddl_explorer`/`hide_ddl_explorer`, `ddl_explorer_visibility_changed(bool)`; `ui/main_window.py` — hidden left "DDL Objects" tab hosting the pre-existing `BrowserPanel`, checkable Database ▸ "DDL Explorer" toggle after a separator, `_open_ddl_explorer` (seed_params from project tree or standalone settings; missing host → status + uncheck + Connection Setup; async `_fetch_ddl_schema` seam → `build_ddl_text` → EditorPanel + BrowserPanel + reveal; error → status + uncheck), bidirectional tab/left-tab/checkbox lockstep (`_on_ddl_explorer_visibility_changed`, BUG-007 lesson), `_on_ddl_navigate_requested` line jump | tests/ui/test_code_editor.py (extended, 21→40: `_SQL_KEYWORDS` non-trivial + all-lowercase contract; sql highlighter selected for language="sql"; case-insensitive SELECT/select both keyword-colored while JS keeps exact-case (`RETURN` unstyled); `--` comment overrides keywords to EOL and is NOT a comment in js/php; `'it''s'` is one string through the doubled quote; backslash does NOT escape (`'a\'` closes at the second quote); `"MyColumn"` not string-styled; `/* */` block comment still works in sql; `navigate_to_line` 1-based + clamps line 0 to first line; `replace_current_selection` replaces when editable / no-selection no-op / read-only guard leaves text untouched); tests/ui/test_ddl_editor_panel.py (NEW, 5: read-only sql editor with `_SQL_KEYWORDS` highlighter; `set_ddl_text` replaces-not-appends; `navigate_to_line` jump; own FindReplaceBar instance with `_editor is panel.editor`; bar's Replace path cannot edit the read-only buffer); tests/ui/test_center_stage.py (extended, +7: DDL tab holds the EditorPanel + hidden by default; show reveals/switches/emits True; hide hides/returns-to-Raw-XML/emits False; hide when not current doesn't steal the current tab; ✕ close hides DIRECTLY and emits False (unlike Edit XSD's signal-only ✕); DDL tab has a close button while structural tabs don't; implementer's `test_tabs_in_order` already updated to 6 tabs); tests/ui/test_ddl_explorer_wiring.py (NEW, 12, `_sync_run` + patched `_fetch_ddl_schema` seam mirroring test_db_check_wiring.py: menu item checkable + after a separator; toggle on populates EditorPanel banners + BrowserPanel 2 groups + reveals center & left tabs + counts in status; standalone mode works with settings-only connection and NO project; missing host unchecks + status + opens Connection Setup + never fetches; fetch error unchecks + "DDL Explorer failed" status + nothing revealed; deferred runner shows "Loading routines & triggers…" busy status with nothing revealed until on_result; `navigate_requested` emit re-activates the DDL tab + moves the caret; real tree-leaf click lands the caret on that object's banner line; ✕ close syncs left tab + menu checkbox; menu uncheck hides both tabs; direct `show_ddl_explorer()` re-checks the menu + reveals the left tab (other lockstep direction); reopen after close re-fetches live). No implementation bugs found — every new test passed against the implementation as written (one test-authoring fix: double-quoted SQL identifiers are only guaranteed not-STRING-styled; the keyword regex still colors keyword-words inside them, so the test asserts the actual guarantee) | PASS — 72 passed (test_code_editor.py 40 + test_ddl_editor_panel.py 5 + test_center_stage.py 15 + test_ddl_explorer_wiring.py 12) | FAIL — 2 failed (`tests/ui/test_main_window.py::test_enter_caption_mode_for_field_selects_row`, `tests/ui/test_async_task.py::test_run_async_delivers_result_via_real_pool`), 1704 passed, 32 skipped (22.03s, `-n 10` xdist). Both failures triaged as pre-existing parallel-load flakes UNRELATED to this feature: the failing set varied across three `-n 10` runs (run 1 failed the two caption tests instead), every failing test passes serially and in a `-n 10` run of tests/ui alone (1212 passed, 8 skipped, only the async flake red), caption/async code is untouched by this feature, and the stashed BASELINE (commit c19d0b2, no working-tree changes) also fails `test_run_async_delivers_result_via_real_pool` under `-n 10` (1 failed, 1667 passed, 32 skipped). The async test is the same real-threadpool timing flake already noted in the 2026-08-01 BUG-001/BUG-002 and DDL-BrowserPanel entries | (uncommitted) |
| 2026-08-01 | BUG-007 (View-menu dock checkboxes don't uncheck when a dock is closed via its own title-bar ✕), resolving the `docs/BUGFIX_QUEUE.md` entry: `ui/main_window.py::_build_view_menu` now wires the three real QDockWidgets bidirectionally — existing `action.toggled → dock.setVisible` plus new `dock.visibilityChanged → action.setChecked` for `tree_dock` ("Project Tree"), `properties_dock` ("Properties Panel"), `audit_dock` ("Audit/Problems Panel"); the three actions promoted to `self._tree_action`/`self._properties_action`/`self._audit_action`; no recursion guard (Qt only emits toggled/visibilityChanged on real state changes, same coalescing the Manual-tab sync relies on). "Find table reference" and "Raw XML Panel" are tabs, not docks — out of scope per the queue entry | Verification pass per the queue entry's Test impact section on top of the resolving session's three tests in tests/ui/test_menus.py (`test_closing_dock_directly_unchecks_view_action` — close() on each of the three docks unchecks its action; `test_reshowing_dock_rechecks_view_action` — show() re-checks; `test_dock_action_round_trip_does_not_oscillate` — trigger off/on settles with dock and checkbox in agreement), which already cover both directions the queue entry asks for; one real gap filled: tests/ui/test_menus.py (+1: `test_view_action_reopens_dock_after_direct_close` — the actual user recovery path: after a title-bar-✕-style `dock.close()` leaves the action unchecked, triggering that View-menu action re-shows the dock AND re-checks itself, for all three docks). No implementation bugs found — the gap-filling test passed against the implementation as written on the first run | PASS — 39 passed (tests/ui/test_menus.py) | PASS — 1668 passed, 32 skipped (22.33s, `-n 10` xdist) | (uncommitted) |
| 2026-08-01 | BUG-006 (calculated columns `isCalculated="true"` flagged as mismatches in the XML→Database check), resolving the `docs/BUGFIX_QUEUE.md` entry: `model/nodes.py::ColumnNode.is_calculated` property (exact lowercase `"true"` match, the parser's bool-string convention); `db/compare.py::xml_table_columns` return shape widened to `dict[str, dict[str, bool]]` (fieldName → is_calculated, OR-union across pages/details), `ColumnCheck` gains `is_calculated: bool = False` as the LAST field (positional call sites preserved), `check_xml_against_db` passes the flag while `ok` stays "does a matching DB column literally exist" (informational — consumers treat is_calculated as overriding ok), `check_db_against_xml` unchanged; `ui/db_check_panel.py` `_CALC_COLOR` (#d08a1a) + three-way `_make_column_item` (calculated → orange `~`, else ✓/✗), calculated columns excluded from `_mismatch_count` and the "Show only mismatches" filter, rename gating (`contextual_rename`/`_on_context_menu`) skips calculated columns, UserRole payload now a uniform `(kind, name, ok, is_calculated)` 4-tuple on table and column items | Verification pass per the queue entry's Test impact section (implementer had only adapted two existing tests to the new shapes; all NEW calculated-state coverage written here): tests/model/test_nodes.py (+3: `is_calculated` True for `"true"`, False when absent, False for `"false"`/`"True"`/`"TRUE"`/`"1"`/`""` — only the exact lowercase convention matches); tests/db/test_compare.py (+5, new `_calc_col` fixture: flag carried through `xml_table_columns`; OR precedence across the pages union tested in BOTH orders (calc-first and plain-first → still calculated); calculated flag survives nested-detail recursion; `check_xml_against_db` end-to-end — plain column False, calculated-no-DB-column `is_calculated=True`/`ok=False`/`info=None`, calculated-shadowing-a-real-DB-column keeps informational `ok=True`+info alongside `is_calculated=True`; `check_db_against_xml` never flags calculated and a DB-absent calculated XML column never appears as a DB row); tests/ui/test_db_check_panel.py (+5 plus 3 existing tests extended via a `calc_total` ok=False/is_calculated=True column added to `_checks()`: `~` marker with no ✓/✗ and `_CALC_COLOR` foreground; calculated overrides `ok=True` for display (shadow case renders `~` orange, not green ✓); UserRole 4-tuple carries `("column","calc_total",False,True)`; `contextual_rename` emits nothing for the calculated item while the sibling `gone` mismatch still offers rename; double-click on a calculated item still emits `jump_requested`; header count stays "3 mismatches" not 4 despite the extra ok=False column; mismatch-only filter still shows exactly the one `gone` child — calc_total hidden). One test-authoring mistake fixed on first run (my `"4" not in header` matched the "5432" port digit — tightened to `"4 mismatches"`); no implementation bugs found — every behavioral test passed against the implementation as written | PASS — 73 passed (tests/db/test_compare.py + tests/ui/test_db_check_panel.py + tests/model/test_nodes.py + tests/ui/test_db_check_wiring.py) | PASS — 1664 passed, 32 skipped (483.95s) | (uncommitted) |
| 2026-08-01 | BUG-004 (Light/Dark theme toggle has almost no visible effect app-wide), resolving the `docs/BUGFIX_QUEUE.md` entry: `ui/theme.py` gains a real `dark_palette()` mirroring `light_palette()`'s role coverage (Window/Base/Text/Button/Link/LinkVisited/Highlight + Disabled group) and `apply_theme(app, light)` is now symmetric — always Fusion style, always an explicit light or dark palette, no native/OS passthrough third state (the old `default_palette`/`default_style` parameters are gone); `main_window.py`'s `_restore_theme` applies the theme unconditionally for both states and re-tints toolbar icons, `_on_light_theme_toggled` simplified; NEW `tests/ui/conftest.py` autouse fixture resets the QApplication style+palette after every tests/ui test (every `MainWindow()` now mutates the process-wide app style); `tests/ui/_menu_helpers.py::_top_level_menus` filters the menu bar's phantom empty-titled overflow-chevron QMenu that exists immediately under Fusion (do NOT switch it to `menuBar().actions()`/`action.menu()` enumeration — PySide6/shiboken GC of the transient QAction wrappers deletes the underlying C++ QMenus; empirically verified, all 35 test_menus tests fail with "Internal C++ object already deleted") | Independent verification pass (dispatched per CLAUDE.md's mandatory feature-tester policy) on top of the resolving session's rewritten tests (`tests/ui/test_theme.py` 10 tests, `tests/ui/test_main_window_theme.py` 3 tests, `tests/ui/test_menus.py` phantom-filter coverage via the exact-ordered top-level-titles assertion), filling coverage gaps: tests/ui/test_main_window_theme.py (+8: "lightTheme" QSettings persistence round-trip via injected temp-ini settings — toggle on writes True and a NEW MainWindow on the same ini restores the checked action + light_palette(); on→off round-trip flips the stored bool back to False and a new window restores the explicit dark_palette(); a pre-seeded lightTheme=true ini makes a fresh window start light+checked; toolbar icon re-tint verified at the PIXEL level via rendered-pixmap color counting (test_icons.py's tolerance scheme) — fresh dark window tints icons to dark_palette's 0xE0E0E0 WindowText with zero near-0x1E1E1E pixels, toggling light re-tints to 0x1E1E1E with zero light pixels, toggling back re-tints to light-on-dark again; the autouse conftest reset exercised directly by a deliberately-leaky test (MainWindow constructed with NO local reset fixture, app left on the dark palette) followed in definition order by a leak-check test asserting the app palette is no longer dark_palette's window color — the exact leakage that broke test_menus.py before the conftest fixture existed). No implementation bugs found — every gap-filling test passed against the implementation as written on the first run | PASS — 21 passed (tests/ui/test_theme.py + tests/ui/test_main_window_theme.py); area run PASS — 1166 passed, 8 skipped (tests/ui, 366.90s) | PASS — 1651 passed, 32 skipped (380.36s) | 7ec792f (wip commit) + uncommitted working-tree follow-up incl. these tests |
| 2026-08-01 | BUG-003 (Properties panel re-scans the whole document per row/click instead of using XmlEditor's existing cache) + BUG-005 (Caption Management row-tint colors have no matching foreground, unreadable under Light Theme), resolving `docs/BUGFIX_QUEUE.md` entries as implemented in the resolving session: BUG-003 — `xml_editor.py`'s free functions `attribute_at_position`/`attribute_value_at_position` gained an optional `spans=` parameter (default `None` preserves from-scratch scanning for every existing caller); new `XmlEditor.resolve_attribute_at(pos)` rescans only if `document().revision() != self._spans_revision` then resolves against the cached `self._spans_text`/`self._spans`; `properties_panel.py::_display_value` and `_hint_for_help_pos` (hover tooltip) now both go through `resolve_attribute_at` instead of a fresh `toPlainText()` + free-function scan per call. BUG-005 — `caption_management_panel.py` gained `_INCONSISTENT_FOREGROUND`/`_CHANGED_FOREGROUND` paired with the existing dark backgrounds; `_CaptionTableModel.data()`'s `ForegroundRole` branch mirrors the `BackgroundRole` precedence (changed wins over inconsistency); both `set_new_values()`'s batched emit and `_emit_row_changed()`'s singular-edit emit now include `ForegroundRole` alongside `BackgroundRole` in their `dataChanged` role lists | Independent verification pass (dispatched per CLAUDE.md's mandatory feature-tester policy) on top of the resolving session's own tests, filling coverage gaps: tests/ui/test_xml_editor.py (+4: `resolve_attribute_at` matches the uncached free function's result for a position on an attribute; **rescans and resolves correctly after the document changes between two calls** — not just skips rescanning when unchanged, the case the resolving session's own test covered; returns `None` for a position not on an attribute; every existing caller that omits `spans=` — grepped all call sites in `pgtp_editor/` and `tests/` — still resolves identically to before, confirming the default is behavior-preserving); tests/ui/test_caption_management_panel.py (+2: `_recompute_inconsistency`'s group-of-3+ case, not just pairs, resolves the matching foreground for every divergent member via the real `_unify_entries` 3-row fixture; a row that WAS inconsistent-tinted reverts its foreground to `None` — not a lingering warm tint — once its group becomes fully consistent again, exercised through the batched `unify_from_row`/`set_new_values` path since that's how the app actually mutates rows in bulk). Contrast of the two new foreground/background pairs verified by WCAG relative-luminance calculation: inconsistent pair ~10.6:1, changed pair ~10.4:1, both far above the 4.5:1 AA threshold. No implementation bugs found — every gap-filling test passed against the implementation as written on the first run | PASS — 274 passed (tests/ui/test_caption_management_panel.py + tests/ui/test_properties_panel.py + tests/ui/test_xml_editor.py + tests/ui/test_xml_structure.py + tests/ui/test_xml_editor_hover.py + tests/ui/test_xml_editor_annotate.py) | PASS — 1638 passed, 32 skipped (21.74s) | (uncommitted) |
| 2026-08-01 | BUG-001 (Edit XSD tab has no close affordance) + BUG-002 (viewAbilityMode/editAbilityMode enum labels don't resolve), resolving `docs/BUGFIX_QUEUE.md` entries: BUG-001 — `center_stage.py` gains `xsd_close_requested` signal, the Edit XSD tab keeps its ✕ (previously stripped like the other structural tabs), `_on_tab_close_requested` emits the signal instead of hiding directly, new `hide_edit_xsd()` mirrors `hide_manual()`; `main_window.py`'s new `_on_xsd_close_requested` reuses the existing `_confirm_close_xsd()` save/discard/cancel flow. BUG-002 — `resources/curated.xsd` gains the previously-entirely-missing `editAbilityMode` attribute block (identical 0-3 Disable/Separated page/Inline mode/Modal window enum to its `viewAbilityMode`/`insertAbilityMode` siblings) in `Project_Presentation_Pages_Page_Type`; `xsd_verify.py`'s `_Checker` gains a duplicate-`xs:attribute`-name-within-one-complexType check (`_attr_names_seen`, reset per complexType start) that was previously silently absent, so a copy-pasted duplicate attribute block would drop labels with zero diagnostic | Independent verification pass (dispatched per CLAUDE.md's mandatory feature-tester policy) on top of the resolving session's own tests, filling coverage gaps: tests/ui/test_center_stage_manual.py (+1: `hide_edit_xsd()` when the XSD tab is visible but not the current tab must not steal focus back, mirroring `hide_manual`'s same guard); tests/ui/test_edit_xsd_tab.py (+2: the close-button flow works identically in "learned" mode i.e. the Edit AutoXSD variant of the shared tab — same `xsd_tab_index`/`xsd_close_requested` wiring, different backing file; `_on_xsd_close_requested` driven through the full MainWindow flow when the tab is visible-but-not-current does not force focus); tests/schema_learning/test_xsd_verify.py (+1: the new duplicate-attribute-name check is purely additive — when BOTH occurrences of a duplicated `sums="true"` attribute are independently unlabeled, the existing per-occurrence sums-missing-labels check still fires for each occurrence at its own line, alongside the one new duplicate-name issue at the second occurrence); tests/schema_learning/test_storage.py (+1: the real bundled `editAbilityMode` block itself verifies cleanly via `verify_curated` — no `duplicate attribute name` or other issue mentioning it, catching a copy/paste slip the broader `fatal-only` guard test wouldn't). No implementation bugs found — every gap-filling test passed against the implementation as written; `editAbilityMode`'s placement was independently confirmed correct (inside `Project_Presentation_Pages_Page_Type`, alongside `viewAbilityMode`/`insertAbilityMode`, not the separate nested Detail-Page complexType noted as deliberately out of scope) | PASS — 86 passed (tests/ui/test_center_stage_manual.py + tests/ui/test_edit_xsd_tab.py + tests/schema_learning/test_storage.py + tests/schema_learning/test_xsd_load.py + tests/schema_learning/test_xsd_verify.py) | PASS — 1626 passed, 32 skipped (23.97s); one unrelated pre-existing flake observed on the first full-suite run (`tests/ui/test_async_task.py::test_run_async_delivers_result_via_real_pool`, a real-threadpool timing test untouched by this session) that passed cleanly both in isolation and on full-suite re-run | (uncommitted) |
| 2026-08-01 | DDL Explorer — BrowserPanel tree + its data layer (spec §18.1 slice, introspection-only): `db/introspect.py` gains `RoutineInfo`/`TriggerInfo` dataclasses, `DatabaseSchema.routines`/`.triggers` fields (defaulted, backward compatible), `_decode_trigger_type(tgtype)` pure bitmask decoder, and `fetch_routines_and_triggers(params, runner=)` as a separate fetch path from `fetch_schema`; new pure `db/ddl_buffer.py::build_ddl_text(schema)→(text, [DdlObjectSpan])` synthesizing one banner-delimited text buffer in schema→kind→name order; new `ui/ddl_buffer_panel.py::BrowserPanel(QWidget)` — dual-grouped tree ("Tables" listing triggers per table, "Functions & Procedures" listing routines with reverse-matched calling triggers nested beneath, same-schema-only bare-name matching), a trigger renders as two leaves sharing one `DdlObjectSpan`, `navigate_requested(line)` fires on leaf/routine clicks only. Scope note: no `EditorPanel`, no SQL highlighter mode, no `main_window.py` wiring yet — those are a follow-up increment; `navigate_requested` is real but currently unconsumed by design | tests/db/test_introspect.py (extended, +11 beyond the implementer's routine/trigger tests: `_decode_trigger_type` all-zero bitmask, all-four-events-in-canonical-order, INSTEAD-precedence-over-BEFORE, BEFORE-with-no-event-bits; `DatabaseSchema` positional/keyword/no-arg construction all default `.routines`/`.triggers` to `{}`, confirming every pre-existing caller in `generation/`/`db/compare` tests stays unaffected); tests/db/test_ddl_buffer.py (extended, +3: multi-schema ordering — schema sorts before name even when name order would disagree; multiple triggers on the same table get distinct non-overlapping spans in name order; empty-source routine still yields a valid one-line span); tests/ui/test_ddl_buffer_panel.py (extended, +7: empty-schema still builds both top-level groups with 0 children; a trigger with a dangling/unmatched `function_name` appears under its table but is silently absent from Functions & Procedures — no crash; bare-name matching is scoped to the same schema, a same-named routine in a different schema is not a false match; click on the "Functions & Procedures" group header emits nothing; click on a routine item with no corresponding span (schema/spans desync) emits nothing rather than raising; a routine with zero calling triggers has zero children). No implementation bugs found — all gap-filling tests passed against the implementation as-is once test-side expectations were corrected (one authoring mistake in my own new test about which line `start_line` denotes, fixed before commit, not an implementation issue) | PASS — 46 passed (tests/db/test_introspect.py + tests/db/test_ddl_buffer.py + tests/ui/test_ddl_buffer_panel.py) | PASS — 1610 passed, 32 skipped (23.16s); one unrelated pre-existing flake observed on first run (`tests/ui/test_async_task.py::test_run_async_delivers_result_via_real_pool`, a real-threadpool timing test untouched by this session) that passed cleanly both in isolation and on full-suite re-run | (uncommitted) |
| 2026-07-31 | Edit AutoXSD + bundled Curated v1.2 + mode-aware Edit XSD tab (spec §11/§22): `storage.CURATED_BUNDLED_VERSION="1.2"` + `bundled_curated_xsd_text()` reads packaged `resources/curated.xsd`; `_ensure_curated_bootstrap` now seeds the user's absent curated.xsd by COPYING the bundled resource (audit "Seeded curated.xsd from the bundled schema (v1.2)"), no-op when it exists, falls back to generating from the learned model only when no bundled resource; mode-aware Edit-XSD tab — Schema ▸ Edit XSD (curated) / Edit AutoXSD (learned) share one tab with `_xsd_mode`, `_xsd_tab_label`, `_xsd_path_for_mode`; mode-switch with unsaved edits prompts save/discard/cancel; `_save_curated_xsd`→`_save_xsd` writes the active mode's path (curated feeds completion, learned does not); Verify/Export/Import target the active mode's file, Verify audit lines carry mode in UserRole+2; Schema menu now FIVE actions (Edit XSD, Edit AutoXSD, Verify, Export, Import) | tests/schema_learning/test_storage.py (extended, +3: bundled version constant, bundled text present+versioned, bundled curated.xsd loads via load_curated & verify_curated reports 0 fatal — guard against shipping a broken schema); tests/ui/test_curated_feed_wiring.py (rewrote the single from-learned bootstrap test into 5: __init__ seeds from bundled + audits version + feeds completion, seed-when-absent copies bundled verbatim, no-op when curated exists, fallback-to-learned when bundled monkeypatched None, no-op when neither bundled nor model; +updated first-run enrichment test to monkeypatch bundled→None); tests/ui/test_edit_auto_xsd_tab.py (NEW, 16: curated/learned mode+title, AutoXSD-absent skeleton+hint, Edit AutoXSD menu wired, learned dirty label base, same-mode reopen preserves edits, mode-switch cancel/discard/save/save-failure, learned save writes learned path & does not feed completion, curated save feeds, learned Verify tags UserRole+2 & click opens learned tab, learned Export default filename learned.xsd, learned Import backs up .bak & no completion feed); tests/ui/test_edit_xsd_tab.py (updated: `_save_curated_xsd`→`_save_xsd`, `_unseed` helper for absent-file paths since __init__ now seeds, "No Edit XSD file yet." messages, "Imported Edit XSD" notice); tests/ui/test_menus.py (Schema menu → 5 actions incl. Edit AutoXSD); tests/ui/test_schema_learning_wiring.py (clear audit panel before count asserts — __init__ now adds a seed line) | PASS — 119 passed (test_storage.py + test_curated_feed_wiring.py + test_edit_auto_xsd_tab.py + test_edit_xsd_tab.py + test_menus.py + test_schema_menu_entry_point.py + test_schema_learning_wiring.py) | PASS — 1574 passed, 32 skipped (54.45s) | b94f625 (feature) + this commit (tests) |
| 2026-07-27 | Curated XSD as the single schema source (spec §11 pivot): `schema_learning/xsd_load.py` (expat curated.xsd → CuratedSchema with line maps, DTD refused), `xsd_verify.py` (dialect verifier), `settings_index.py` rewrite (sums 2^n−1, hint attrs, value_label), `xsd_gen.generate_curated_xsd` bootstrap, storage curated/learned paths; MainWindow `_load_curated_schema` exclusive feed + last-good-on-error, one-time `_ensure_curated_bootstrap`, Edit XSD tab (per-tab dirty/save/find, Ctrl+S routing, closeEvent branch), Go To XSD (Ctrl+L), Verify XSD (clickable audit lines, auto-run on save), Export/Import XSD (verify-first, .bak); annotate popover / team sync / schema viewer DELETED | tests/ui/test_curated_feed_wiring.py (extended, 4→6: +first-run end-to-end — enrich with no curated.xsd and no model → learn → bootstrap fires → editor completion fed from fresh curated.xsd; +second enrichment never replaces the live curated schema object); tests/ui/test_edit_xsd_tab.py (extended, 26→36: +open Edit XSD with no file loads empty xs:schema skeleton, +Verify with no curated.xsd → status only, +Verify checks dirty tab text not saved file, +clicking a VERIFY line with dirty tab navigates WITHOUT clobbering unsaved edits, +Export early returns — no curated.xsd / dirty tab "save it first" — never open the dialog, +Export cancel no-op, +Export copyfile OSError → critical, +Import target write OSError → critical + dirty kept + file untouched, +Save write OSError → critical + dirty kept). Re-verified pre-existing feature tests: test_xsd_load.py, test_xsd_verify.py, test_settings_index.py, test_xsd_gen.py, test_storage.py, test_model.py, test_schema_learning_wiring.py, test_properties_panel.py, test_xml_editor_annotate.py, test_xml_editor_completion.py, test_xml_editor_hover.py, test_menus.py, test_schema_menu_entry_point.py, test_center_stage.py | PASS — 42 passed (test_edit_xsd_tab.py + test_curated_feed_wiring.py) | PASS — 1534 passed, 32 skipped (48.04s) | ce7e7b7..3921865 (feature) + this commit (tests) |
| 2026-07-24 | `--debug` tracer flood fix: the sys.monitoring auto-tracer traced the per-element recursive schema-learning enrichment (schema_learning.parser/model/types), flooding the trace log (46 MB in ~90s) until the process died before enrichment finished on a large real .pgtp. Fix adds those three modules to `debuglog.py` `_EXCLUSIONS`; the light query layer `schema_learning.settings_index` stays traced | tests/test_debuglog.py (extended, +1 behavioral beyond the TDD predicate test `test_schema_learning_enrichment_hot_modules_excluded`: `test_tracer_silences_schema_learning_enrichment_but_keeps_settings_index` runs setup(debug=True) and exercises parser.walk_element (recursive), Model.merge_element→types.infer_scalar_type/combine_type, and settings_index.attribute_kind, asserting the debug log has NO `schema_learning.parser`/`.model`/`.types` lines while settings_index still emits `>`/`<` — proving surgical silencing, not a blanket schema_learning shutoff). Re-verified the predicate test and `test_tracer_logs_traced_package_calls` | PASS — 34 passed (tests/test_debuglog.py) | PASS — 1636 passed, 0 skipped (77.24s) | 5971271 (fix) + this commit (tests) |
| 2026-07-24 | Unlabeled-value underline perf fix (O(n²)→O(n log n)): `xml_structure.build_parent_map(spans)` computes every span's parent in one ancestor-stack pass; `xml_editor.unlabeled_value_spans` precomputes it once and looks up parents O(1) instead of calling `parent_tag_span` per span (was ~69s / hang on the 37k-tag dev_Ferrara.pgtp) | tests/ui/test_xml_structure.py (extended, +1 beyond the TDD parity/top-level tests: build_parent_map vs parent_tag_span parity on deep mixed nesting — several levels, repeated sibling names, self-closing siblings interleaved with containers, whitespace); tests/ui/test_xml_editor_annotate.py (extended, +1 beyond the TDD scaling guard: unlabeled_value_spans builds the full 3-level A/B/C chain via the parent map and does not match the same attr at a shorter/wrong chain). Re-verified the pre-existing TDD tests (parity, top-level-no-parent, 15k-tag scaling guard, nested-chain) and the real-sample tests | PASS — 69 passed (test_xml_structure.py + test_xml_editor_annotate.py + test_apply_changes_to_target_real_sample.py) | PASS — 1634 passed, 0 skipped (78.00s) | 70200df (fix) + uncommitted tests |
| 2026-07-23 | Busy activity indicator (cosmetic status message + hourglass): `ui/busy.py` `busy_status(status_bar, message)` context manager (sticky gerund+"…" status message + Qt.WaitCursor + `processEvents(ExcludeUserInputEvents)` forced paint, cursor restored in `finally`) and `format_size(num_bytes)`; `main_window.py` wraps file open (`Opening {name} ({size})…`, size dropped on OSError, parse-failure dialog after the block), validate (`Validating {name}…`), reparse (`Reparsing…`, failure dialog after the block); generation reworded to `Generating PHP…` (stays async, no cursor by design) | tests/ui/test_busy.py (extended, 5→12: +format_size boundaries 0 bytes / 1023 bytes / exactly 1 KB / 1023 KB / exactly 1.0 MB, +message shown sticky with no timeout arg, +nested busy_status keeps the override-cursor stack balanced); tests/ui/test_open_project.py (extended, +1 beyond impl's 3: getsize OSError drops the size suffix but still shows `Opening <name>…`, reaches `Opened:`, and populates the tree); tests/ui/test_busy_wiring.py (extended, 2→3: +reparse parse-failure restores the cursor before the failure dialog). Re-verified tests/ui/test_generation.py | PASS — 59 passed (test_busy.py + test_busy_wiring.py + test_open_project.py + test_generation.py) | PASS — 1573 passed, 0 skipped (60.00s) | (uncommitted) |
| 2026-07-23 | Annotate-at-cursor schema labeling + team schema-model sharing (spec §11): annotate popover (labels/notes/kind/enum_mode), bit-flags derivation, union known_values, unlabeled-value underlines + Next Unlabeled Value, Publish/Fetch/Merge via schema_learning/merge.py + sync.py | tests/schema_learning/test_merge.py (extended, 9→14: +apply_resolution scalar field, +apply_resolution setdefaults a missing notes dict, +base overflow persists vs incoming values, +incoming overflow wipes values but keeps stale labels, +type widening via combine_type); tests/ui/test_xml_editor_annotate.py (extended, 12→20: +entity-value raw resolution, +inter-attribute gap → None, +nested-chain span lookup does not match wrong chain, +derived bitflag label counts as labeled, +goto False when all labeled, +set_schema_model(None) clears underlines, +annotate works read-only, +context menu hides Annotate without model); tests/ui/test_schema_sync_wiring.py (extended, 5→11: +publish without local model is a no-op with status hint, +fetch conflict keeps local label & CONFLICT audit line, +fetch failure leaves local model bytes untouched, +fetch with no master.json reports sync failure, +merge dialog Accepted applies use-incoming resolution into the pushed master, +merge with no user models skips push); tests/ui/test_annotate_popover.py (extended, 3→4: +note-Enter commits the default remove-everything payload). Re-verified pre-existing feature tests in test_settings_index.py, test_xsd_gen.py, test_sync.py, test_storage.py, test_annotate_wiring.py, test_merge_conflicts_dialog.py, test_team_sync_dialog.py | PASS — 48 passed (test_merge.py + test_xml_editor_annotate.py + test_schema_sync_wiring.py + test_annotate_popover.py) | PASS — 1572 passed, 32 skipped (30.70s) | 6fc68a0 (feature) + this commit (tests) |
| 2026-07-21 | Table references tab (replaces the modal Find Reused Tables window with a persistent, navigable left-dock tab): structured `TableReference(breadcrumb, node, kind, line, ref_type)` in the analyzer; `TableReferencesPanel` with `selection_changed`/`jump_requested`; MainWindow View-menu "Find table reference" toggle, selection->Properties, double-click->Raw XML jump, refresh-on-reparse; Tools "Find Reused Tables..." removed | tests/ui/test_table_references_panel.py (extended, 5->8: +double-click on a table row emits no jump, +double-click ref with line=None still emits None downstream, +multiple-table order & counts preserved); tests/ui/test_table_references_wiring.py (extended, 8->11: +lookup without OnTheFlyInsertPage renders "(lookup)" not "(lookup with insert)" through the full analyzer->panel path, +toggle off-then-on recomputes and refills the tab, +page's own-tableName reference selection drives Properties with kind="page"). Re-verified tests/analysis/test_reused_tables.py | PASS — 32 passed (tests/ui/test_table_references_panel.py + tests/ui/test_table_references_wiring.py + tests/analysis/test_reused_tables.py) | PASS — 1553 passed, 0 skipped (102.02s) | (uncommitted) |
| 2026-07-21 | Slim the Windows build — exclude unused deps (numpy/yaml) + prune unused Qt libraries, and fix the QtSvg exclusion that silently broke toolbar icons (optimized_build.py) | tests/test_build_excludes.py (extended, 2→4: existing static PySide6-import-vs-EXCLUDED_QT_MODULES guard + its anti-vacuous sanity check; +test_excluded_third_party_modules_not_imported scans the package for direct numpy/yaml imports and asserts none clash with EXCLUDED_MODULES; +test_excluded_modules_are_wired_into_build_args asserts both EXCLUDED_QT_MODULES and EXCLUDED_MODULES reach PyInstaller as --exclude-module via inspect.getsource(build)) | PASS — 4 passed (tests/test_build_excludes.py) | PASS — 1533 passed, 0 skipped (96.79s) | (uncommitted) |
| 2026-07-21 | Normalize `MainWindow._current_project_path` to `str` so revert works when `open_project_file` is handed a `pathlib.Path` (fixes TypeError from `Path + ".bak"` string concat in `_revert_project`; matches the already-normalized `str(path) + ".bak"` in `_write_project_text`) | tests/ui/test_open_project.py (extended, +2 beyond the test-first regression test: revert after a pathlib.Path open actually restores the distinct `.bak` content into editor+tree and keeps `_current_project_path` a str pointing at the real file; save after a pathlib.Path open overwrites the file and creates the `.bak` via `_write_project_text`'s own `str(path)+".bak"` without TypeError). Re-verified tests/ui/test_save_project.py | PASS — 30 passed (tests/ui/test_open_project.py + tests/ui/test_save_project.py) | PASS — 1529 passed, 0 skipped (64.81s) | (uncommitted) |
| 2026-07-21 | Open a .pgtp file passed on the command line at startup (Windows "Edit with PGTP Editor" right-click verb passes the clicked path as argv[1]): `parse_args` gained an optional positional `file` (nargs="?", default None); `main()` calls `window.open_project_file(args.file)` only when `os.path.isfile(args.file)`, else logs a "startup file not found" warning | tests/test_main.py (extended, 7→14: +3 parse_args cases — no file→None, positional file, --debug + file; +4 main() cases with fakes for QApplication/MainWindow/debuglog.setup/install_qt_handler — existing file opened once & window shown, missing path not opened + warning logged, no arg not opened, directory path not opened) | PASS — 14 passed (tests/test_main.py) | PASS — 1526 passed, 0 skipped (77s) | (uncommitted) |
| 2026-07-20 | Reparse refreshes open Database Check (cached schema) + Customize Toolbar lists all actions | tests/ui/test_db_check_wiring.py (extended, +6: db_to_xml direction refreshes via check_db_against_xml, reparse edit resolving a mismatch drops the count to 0, refresh does not mutate _last_db_check_direction/_last_db_schema[same object]/_last_db_summary cache, cached summary passed verbatim to panel, invalid buffer leaves panel untouched with QMessageBox.critical patched); tests/ui/test_customize_toolbar_dialog.py (extended, +2: adding all remaining commands leaves Available fully disabled not empty, result_ids order preserved through add/remove/move sequence) | PASS — 32 passed (test_db_check_wiring.py + test_customize_toolbar_dialog.py) | PASS — 1368 passed, 32 skipped (62s) | a0531c4 |
| 2026-07-20 | Ctrl+click (matching tag) / Alt+click (parent tag) caret navigation in Raw XML editor | tests/ui/test_xml_structure.py (extended, +6 pure tests: deep 4-level nesting resolves innermost partner & own open, grandchild Alt-target is immediate parent not root, mid-level open-tag parent, attribute-region resolves to close, repeated-sibling correct instance); tests/ui/test_xml_editor_click_nav.py (extended, +6 widget tests: Ctrl+click self-closing no-op, Ctrl+Shift+click & Ctrl+Alt+click fall-through, jump uses updated offsets after edit, jump to correct offset with an inner fold collapsed, Alt+click top-level no-op) | PASS — 55 passed (test_xml_structure.py + test_xml_editor_click_nav.py) | PASS — 1355 passed, 32 skipped (32s) | e4e1884 (feature committed; new tests uncommitted) |
| 2026-07-20 | DB tree double-click lists ALL occurrences (Find All) and F3 steps through them: `MainWindow._on_db_jump_requested(kind, name)` builds the `tableName="X"`/`fieldName="X"` token, switches to Raw XML, clears selection, seeds the Find bar, runs `_populate_find_all_results` (streams every occurrence into the Audit panel), reveals `audit_dock`, then `find_next()` selects the first; F3 (existing menu action -> `_find_next` -> `find_replace_bar.find_next()`) steps with wrap. Missing token -> status guard | tests/ui/test_db_check_wiring.py (extended, +5 edge tests: column-node fieldName token lists all + F3 1->2->3->wrap; single-occurrence F3 wraps to itself keeping selection; second double-click clears prior [Find] rows, no accumulation; missing token leaves Find field + selection + find_all_term untouched and shows status; hidden audit_dock revealed on jump). Re-verified prior 4 jump/F3 tests + test_find_replace_bar.py + test_main_window.py + test_menus.py | PASS — 140 passed (test_db_check_wiring.py + test_find_replace_bar.py + test_main_window.py + test_menus.py) | PASS — 1506 passed, 0 skipped (165.19s) | 0b19ec5 + uncommitted tests |
| 2026-07-20 | Create-from-DB-table generator defaults recalibrated against the REAL PHP Generator clean-defaults oracle (golden_newtable_1): `type_map.py` PAGE_DEFAULTS (editAbilityMode=3, deleteSelectedAbilityMode/highlightRowOnMouseHover/condensedTable) + ColumnPresentationSpec (emit_show_column_filter_false, view_extra) + scale-aware numeric (numberAfterDecimal via numeric_scale) + integer thousandSeparator + boolean displayType="image"/1572867; `from_table.py` showColumnFilter/canSetNull emission order + bare-table-name fileName; golden harness normalizes both sides through serializer and protects _REAL_ORACLES | tests/generation/test_oracle_parity.py (new, 10 tests: build_page reproduces the real oracle element-for-element with attribute order; page-root & per-column attribute order match oracle; integer Format has thousandSeparator and NOT decimalSeparator; scale-aware numeric numeric(10,2)->numberAfterDecimal="2"; boolean omits showColumnFilter + displayType="image"; showColumnFilter="false" on all non-boolean; canSetNull only on nullable; serialize/normalize preserve attribute order not alphabetical). Re-verified: test_type_map.py, test_from_table.py, test_golden_page.py, test_create_from_table_wiring.py | PASS — 120 passed (tests/generation + test_create_from_table_wiring.py + test_db_check_panel.py + test_introspect.py) | PASS — 1498 passed, 0 skipped (86.18s) | 57d918d + uncommitted tests |
| 2026-07-20 | Ctrl+click (matching tag) / Alt+click (parent tag) caret navigation in Raw XML editor | tests/ui/test_xml_structure.py (extended, +6 pure tests: deep 4-level nesting resolves innermost partner & own open, grandchild Alt-target is immediate parent not root, mid-level open-tag parent, attribute-region resolves to close, repeated-sibling correct instance); tests/ui/test_xml_editor_click_nav.py (extended, +6 widget tests: Ctrl+click self-closing no-op, Ctrl+Shift+click & Ctrl+Alt+click fall-through, jump uses updated offsets after edit, jump to correct offset with an inner fold collapsed, Alt+click top-level no-op) | PASS — 55 passed (test_xml_structure.py + test_xml_editor_click_nav.py) | PASS — 1355 passed, 32 skipped (32s) | ba579c7 |
| 2026-07-19 | Create Page/Detail/Lookup from a DB table or view (SP3): `generation/type_map.py` + `generation/from_table.py` builders (build_page/build_detail/build_lookup/serialize, FK inference), `db/introspect.py` FK-target capture, `DbCheckPanel` create_requested menu (db_to_xml), `MainWindow._on_db_create_requested` (insert page before `</Pages>` / copy detail-lookup to clipboard, fileName dedupe) | tests/generation/test_from_table.py (extended, 12→24: +composite-PK & no-PK lookup link empty, +display fallback to PK, +display skips-but-falls-back-to FK, +page for view kind, +no-PK hides nothing, +empty-columns raises, +multi-FK & unknown-target detail placeholders, +serialize attr/text escaping); tests/ui/test_create_from_table_wiring.py (extended, 7→12: +unknown-table guard, +tab-indented buffer splice depth, +fileName-only collision prompts, +missing-`</Pages>` status, +composite-PK lookup link empty); tests/ui/test_db_check_panel.py (extended, +view node offers create actions); tests/generation/test_type_map.py & tests/db/test_introspect.py (re-verified) | PASS — 105 passed (tests/generation + test_create_from_table_wiring.py + test_db_check_panel.py + test_introspect.py) | PASS — 1454 passed, 0 skipped (88.50s) | (uncommitted) |
| 2026-07-19 | panGen + rePHPgen gap analysis — Generation-menu wiring (Task 8): `_pangen`, `_re_phpgen_analyze` (pangen→analyze chaining), `_save_rejson`, `_locate_pangen_runtime`, `_prepare_generation_run` preamble, `_re_phpgen_runtime` (PYTHONPATH/cwd), Save-reJSON enable state machine | tests/ui/test_pangen_menu.py (extended, 11→22 tests: +in-flight guard, +no-open-project guard, +cancel-save-prompt, +cancel-output-folder, +SaveAll routes through Save-As, +panGen non-zero-exit warns/clears flag, +analyze-step failure warns & leaves Save disabled, +analyze-without-runtime guidance, +analyze --json/--ours/--vendor args match work path & _pangen sibling, +Save-reJSON cancel writes nothing, +Locate-runtime cancel is no-op) | PASS — 22 passed (tests/ui/test_pangen_menu.py) | PASS — 1409 passed, 0 skipped (111.67s) | f95b68c + uncommitted tests |
| 2026-07-19 | `GeneratorRunner` cwd/env support (Task 6 of panGen gap-analysis plan): `run()` gained optional `cwd` and `extra_env` kwargs, applying `QProcess.setWorkingDirectory` and a `QProcessEnvironment` layered on `systemEnvironment()` | tests/generation/test_runner.py (extended, 6→12 tests: +cwd/extra_env applied via real subprocess round-trip, +no-kwarg call path unchanged, +falsy cwd=""/extra_env={} treated as unset, +extra_env layers onto systemEnvironment() rather than replacing it, +explicit cwd=None/extra_env=None, +updated run() signature contract test) | PASS — 12 passed (tests/generation/test_runner.py); PASS — 1013 passed (tests/generation/ + tests/ui/) | PASS — 1382 passed, 0 skipped (76.82s) | (uncommitted) |
| 2026-07-19 | `re_phpgen_root` editor setting (Task 4 of panGen gap-analysis plan): new persisted config key alongside `executable_path` in `generator_config.json` | tests/generation/test_config.py (extended, 8→21 tests: +default-when-unset/absent, +malformed JSON, +non-dict JSON, +wrong-typed value, +empty-string value, +missing key, +directory creation, +preserves-executable-key, +preserves-unrelated-keys, +overwrite, +expected JSON shape) | PASS — 21 passed | PASS — 1371 passed, 0 skipped (78.89s) | (uncommitted) |

| 2026-07-19 | Cached document-text reuse in matching-tag highlight (nav-lag perf fix, follow-up: per-keystroke toPlainText() full-document copy removed) | tests/ui/test_xml_editor_nav_perf.py (extended, 2→3 tests: +toPlainText()-call-counting regression test proving cursor navigation no longer copies the document text) | PASS — 142 passed (test_xml_editor_nav_perf.py + test_xml_editor.py + test_xml_structure.py + test_xml_editor_completion.py) | PASS — 1326 passed, 32 skipped (43s) | (this commit) |
| 2026-07-19 | Cached tag-span reuse for matching-tag highlight (nav-lag perf fix) | tests/ui/test_xml_editor_nav_perf.py (new, 2 tests: enclosing_tag_span/enclosing_tag_span_from_spans equivalence, and a scan()-call-counting regression test proving cursor navigation no longer rescans); tests/ui/test_xml_editor.py and tests/ui/test_xml_structure.py (unchanged, re-verified) | PASS — 114 passed (tests/ui/test_xml_editor_nav_perf.py + test_xml_editor.py + test_xml_structure.py) | PASS — 1325 passed, 32 skipped (43s) | 5a6d8c7 |
| 2026-07-19 | Debug mode (--debug diagnostic logging) | tests/test_debuglog.py (26→33), tests/test_main.py (4→8), tests/ui/test_main_window_debug.py (6→7), tests/generation/test_runner.py (+1 seam test), tests/db/test_introspect.py (redaction, pre-existing) | PASS — 62 passed (targeted files) | PASS — 1315 passed, 32 skipped (30s) | e5a2e34 |
| 2026-07-19 | Ctrl+Space attribute autocomplete with value chaining (re-run after mouse-click-chooses fix in _CompletionPopup) | same as prior entry (tests/ui/test_xml_editor_completion.py 27 tests; tests/schema_learning/test_settings_index.py 33 tests) | PASS — 60 passed, 0 skipped | PASS — 1307 passed, 0 skipped (99s) | ed3c7a0 |
| 2026-07-19 | Ctrl+Space attribute autocomplete with value chaining | tests/ui/test_xml_editor_completion.py (extended, 20→27 tests); tests/schema_learning/test_settings_index.py (extended, +1 test) | FAIL — 1 failed (test_popup_mouse_click_chooses_row; 59 passed) | FAIL — 1 failed (tests/ui/test_xml_editor_completion.py::test_popup_mouse_click_chooses_row), 1306 passed (124s) | 206e01a |
| 2026-07-19 | (baseline — log established) | none (existing suite, 88 test files) | n/a | PASS — 1271 passed, 0 failed (128s) | 9399050 |

